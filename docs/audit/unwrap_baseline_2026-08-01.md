# unwrap / panic / expect 审计报告

- **生成日期**: 2026-08-01
- **扫描范围**: `packages/` 目录下所有 `.rs` 文件
- **审计工具**: `scripts/unwrap_audit.py`
- **总发现数**: 5080

## 摘要

| 危险等级 | 数量 | 说明 |
|----------|------|------|
| 🔴 CRITICAL | 161 | 运行时必然 panic，生产环境零容忍 |
| 🟠 HIGH | 4621 | Option/Result 强制解包，输入不满足假设时 panic |
| 🟡 MEDIUM | 159 | 测试代码中的 unwrap/expect（允许但需记录） |
| 🔵 LOW | 139 | 注释中的关键词（误报，仅提示） |
| ⚪ INFO | 0 | 其他信息 |

## 按文件分布（Top 20）

| 文件 | 发现数 |
|------|--------|
| `packages\sz-orm-sqlx\tests\real_db_jepsen.rs` | 112 |
| `packages\sz-orm-sqlx\tests\real_db_pool_tests.rs` | 108 |
| `packages\sz-orm-core\tests\integration_sqlite.rs` | 105 |
| `packages\sz-orm-queue\src\queue.rs` | 96 |
| `packages\sz-orm-core\tests\integration_mysql.rs` | 94 |
| `packages\sz-orm-core\tests\integration_pg.rs` | 93 |
| `packages\sz-orm-dtx\src\saga.rs` | 93 |
| `packages\sz-orm-sqlx\tests\sqlx_adapter_tests.rs` | 87 |
| `packages\sz-orm-limit\src\lib.rs` | 84 |
| `packages\sz-orm-scheduler\src\advanced.rs` | 81 |
| `packages\sz-orm-core\src\repository.rs` | 79 |
| `packages\sz-orm-es\src\extensions.rs` | 72 |
| `packages\sz-orm-ai\tests\ai_tests.rs` | 71 |
| `packages\sz-orm-crypto\src\lib.rs` | 70 |
| `packages\sz-orm-core\tests\e2e_transaction.rs` | 69 |
| `packages\sz-orm-core\tests\jepsen.rs` | 69 |
| `packages\sz-orm-sharding\src\lib.rs` | 68 |
| `packages\sz-orm-wasm\src\advanced.rs` | 61 |
| `packages\sz-orm-grpc\src\lib.rs` | 60 |
| `packages\sz-orm-timeseries\src\extensions.rs` | 59 |

## 按模式分布

| 模式 | 数量 |
|------|------|
| `unwrap()` | 4152 |
| `expect()` | 628 |
| `panic!` | 161 |
| `comment_panic` | 75 |
| `comment_unwrap` | 64 |

## 详细发现

### `packages\sz-orm-ai\src\nl2sql.rs`

#### 🔴 [CRITICAL] `panic!` @ L1893:C23

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
Err(e) => panic!("期望 InvalidQuery，实际: {:?}", e),
```

#### 🔴 [CRITICAL] `panic!` @ L1894:C22

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
Ok(_) => panic!("期望错误"),
```

#### 🔴 [CRITICAL] `panic!` @ L1906:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("期望 SchemaError"),
```

#### 🔴 [CRITICAL] `panic!` @ L2005:C22

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
other => panic!("期望 ConfigError，实际: {:?}", other),
```

### `packages\sz-orm-ai\src\real_embedding.rs`

#### 🔴 [CRITICAL] `panic!` @ L252:C22

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
other => panic!("expected AiError::ConfigError, got {:?}", other),
```

#### 🔴 [CRITICAL] `panic!` @ L260:C22

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
other => panic!("expected AiError::ConfigError, got {:?}", other),
```

### `packages\sz-orm-ai\tests\ai_tests.rs`

#### 🔴 [CRITICAL] `panic!` @ L78:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
other => panic!("expected AiError::Config, got {:?}", other),
```

#### 🔴 [CRITICAL] `panic!` @ L86:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
other => panic!("expected AiError::Config, got {:?}", other),
```

### `packages\sz-orm-batch\src\lib.rs`

#### 🔴 [CRITICAL] `panic!` @ L1491:C22

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
other => panic!("expected Columns, got {:?}", other),
```

#### 🔴 [CRITICAL] `panic!` @ L1501:C22

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
other => panic!("expected Constraint, got {:?}", other),
```

#### 🔴 [CRITICAL] `panic!` @ L1719:C22

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
other => panic!("expected Columns, got {:?}", other),
```

### `packages\sz-orm-core\src\behaviors.rs`

#### 🔴 [CRITICAL] `panic!` @ L797:C22

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
other => panic!("expected TenantError, got {:?}", other),
```

#### 🔴 [CRITICAL] `panic!` @ L846:C22

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
other => panic!("expected TenantError, got {:?}", other),
```

#### 🔴 [CRITICAL] `panic!` @ L862:C22

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
other => panic!("expected TenantError, got {:?}", other),
```

#### 🔴 [CRITICAL] `panic!` @ L927:C22

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
other => panic!("expected TenantError, got {:?}", other),
```

### `packages\sz-orm-core\src\dialect.rs`

#### 🔴 [CRITICAL] `panic!` @ L3016:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("Expected DbError::InvalidInput"),
```

### `packages\sz-orm-core\src\hooks.rs`

#### 🔴 [CRITICAL] `panic!` @ L1090:C22

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
other => panic!("期望 Validation 错误，得到 {:?}", other),
```

### `packages\sz-orm-core\src\hydration_plugin.rs`

#### 🔴 [CRITICAL] `panic!` @ L1079:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("expected EmptyRow error"),
```

#### 🔴 [CRITICAL] `panic!` @ L1101:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("expected SingleScalarRequiresSingleRow"),
```

#### 🔴 [CRITICAL] `panic!` @ L1124:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("expected SingleScalarRequiresSingleRow"),
```

#### 🔴 [CRITICAL] `panic!` @ L1134:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("expected EmptyRow"),
```

#### 🔴 [CRITICAL] `panic!` @ L1178:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("expected ColumnNotFound"),
```

#### 🔴 [CRITICAL] `panic!` @ L1454:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("expected Abort"),
```

#### 🔴 [CRITICAL] `panic!` @ L1602:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("expected Modified"),
```

#### 🔴 [CRITICAL] `panic!` @ L1625:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("expected Abort"),
```

### `packages\sz-orm-core\src\model.rs`

#### 🔴 [CRITICAL] `panic!` @ L1269:C13

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
panic!("Expected Array");
```

#### 🔴 [CRITICAL] `panic!` @ L1383:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("Expected RelationNotFound"),
```

#### 🔴 [CRITICAL] `panic!` @ L1396:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("Expected NotLoaded"),
```

#### 🔴 [CRITICAL] `panic!` @ L1420:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("Expected Array"),
```

#### 🔴 [CRITICAL] `panic!` @ L1481:C13

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
panic!("Expected Object");
```

#### 🔴 [CRITICAL] `panic!` @ L1687:C13

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
panic!("Expected MorphMany");
```

#### 🔴 [CRITICAL] `panic!` @ L1697:C13

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
panic!("Expected MorphTo");
```

#### 🔴 [CRITICAL] `panic!` @ L1727:C13

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
panic!("Expected Array");
```

#### 🔴 [CRITICAL] `panic!` @ L1767:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("Expected empty Array"),
```

#### 🔴 [CRITICAL] `panic!` @ L1824:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("Expected NotLoaded"),
```

### `packages\sz-orm-core\src\observer.rs`

#### 🔴 [CRITICAL] `panic!` @ L1302:C22

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
other => panic!("expected Failed, got {:?}", other),
```

#### 🔴 [CRITICAL] `panic!` @ L1306:C22

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
other => panic!("expected Failed, got {:?}", other),
```

### `packages\sz-orm-core\src\result_map.rs`

#### 🔴 [CRITICAL] `panic!` @ L1234:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("expected MapNotFound"),
```

#### 🔴 [CRITICAL] `panic!` @ L1341:C13

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
panic!("dept should be an Object");
```

#### 🔴 [CRITICAL] `panic!` @ L1767:C13

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
panic!("dept should be an Object");
```

#### 🔴 [CRITICAL] `panic!` @ L1774:C13

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
panic!("roles should be an Array");
```

### `packages\sz-orm-core\src\transaction.rs`

#### 🔴 [CRITICAL] `panic!` @ L745:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("Expected NotActive error"),
```

#### 🔴 [CRITICAL] `panic!` @ L765:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("Expected NotActive error"),
```

#### 🔴 [CRITICAL] `panic!` @ L780:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("Expected NotActive error"),
```

#### 🔴 [CRITICAL] `panic!` @ L795:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("Expected NotActive error"),
```

#### 🔴 [CRITICAL] `panic!` @ L826:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("Expected InvalidSavepointName error"),
```

#### 🔴 [CRITICAL] `panic!` @ L834:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("Expected InvalidSavepointName error"),
```

#### 🔴 [CRITICAL] `panic!` @ L842:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("Expected InvalidSavepointName error"),
```

#### 🔴 [CRITICAL] `panic!` @ L860:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("Expected NotActive error"),
```

#### 🔴 [CRITICAL] `panic!` @ L873:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("Expected ConnectionTaken error"),
```

#### 🔴 [CRITICAL] `panic!` @ L1066:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("Expected MaxNestingDepthExceeded error"),
```

#### 🔴 [CRITICAL] `panic!` @ L1093:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("Expected MaxNestingDepthExceeded error"),
```

#### 🔴 [CRITICAL] `panic!` @ L1114:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("Expected MaxNestingDepthExceeded error"),
```

#### 🔴 [CRITICAL] `panic!` @ L1143:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("Expected MaxNestingDepthExceeded error"),
```

### `packages\sz-orm-core\src\type_handler.rs`

#### 🔴 [CRITICAL] `panic!` @ L1144:C22

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
other => panic!("expected TypeMismatch, got {:?}", other),
```

#### 🔴 [CRITICAL] `panic!` @ L1168:C22

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
other => panic!("expected TypeMismatch, got {:?}", other),
```

### `packages\sz-orm-core\tests\chaos_pool.rs`

#### 🔴 [CRITICAL] `panic!` @ L200:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
Ok(_) => panic!("第 3 个 acquire 应失败返回 Timeout"),
```

#### 🔴 [CRITICAL] `panic!` @ L265:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
Ok(_) => panic!("连接创建超时应返回 PoolError::Timeout"),
```

#### 🔴 [CRITICAL] `panic!` @ L348:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
Ok(_) => panic!("关闭后 acquire 应返回 PoolError::Closed"),
```

### `packages\sz-orm-core\tests\contracts\dialect_contract.rs`

#### 🔴 [CRITICAL] `panic!` @ L149:C22

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
Ok(_) => panic!("{:?} 应返回 Err（无方言）", db),
```

### `packages\sz-orm-core\tests\contracts\dynamic_sql_contract.rs`

#### 🔴 [CRITICAL] `panic!` @ L23:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
other => panic!("期望 ParamValue::String，实际: {:?}", other),
```

#### 🔴 [CRITICAL] `panic!` @ L33:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
other => panic!("期望 ParamValue::Int(18)，实际: {:?}", other),
```

#### 🔴 [CRITICAL] `panic!` @ L43:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
other => panic!("期望 ParamValue::Float，实际: {:?}", other),
```

#### 🔴 [CRITICAL] `panic!` @ L53:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
other => panic!("期望 ParamValue::Bool(true)，实际: {:?}", other),
```

#### 🔴 [CRITICAL] `panic!` @ L63:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
other => panic!("期望 ParamValue::Null，实际: {:?}", other),
```

#### 🔴 [CRITICAL] `panic!` @ L76:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
other => panic!("期望 ParamValue::Array，实际: {:?}", other),
```

### `packages\sz-orm-core\tests\contracts\error_contract.rs`

#### 🔴 [CRITICAL] `panic!` @ L203:C13

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
panic!("v0.2.0 契约违反：savepoint() 后 commit 应返回 NotActive，而非 SavepointError");
```

#### 🔴 [CRITICAL] `panic!` @ L205:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
other => panic!("期望 NotActive，实际: {:?}", other),
```

### `packages\sz-orm-core\tests\contracts\pool_contract.rs`

#### 🔴 [CRITICAL] `panic!` @ L44:C23

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
Err(other) => panic!(
```

#### 🔴 [CRITICAL] `panic!` @ L48:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
Ok(_) => panic!("close_all 后 acquire 必须返回 Err，实际返回 Ok"),
```

#### 🔴 [CRITICAL] `panic!` @ L102:C23

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
Err(other) => panic!("期望 PoolError::Timeout，实际: {:?}", other),
```

#### 🔴 [CRITICAL] `panic!` @ L103:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
Ok(_) => panic!("应超时失败，实际返回 Ok"),
```

#### 🔴 [CRITICAL] `panic!` @ L141:C23

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
Err(other) => panic!("期望 InvalidConfig，实际: {:?}", other),
```

#### 🔴 [CRITICAL] `panic!` @ L142:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
Ok(_) => panic!("max_size=0 必须被拒绝，实际返回 Ok"),
```

#### 🔴 [CRITICAL] `panic!` @ L151:C23

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
Err(other) => panic!("期望 InvalidConfig，实际: {:?}", other),
```

#### 🔴 [CRITICAL] `panic!` @ L152:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
Ok(_) => panic!("min_idle > max_size 必须被拒绝，实际返回 Ok"),
```

### `packages\sz-orm-core\tests\contracts\queryable_contract.rs`

#### 🔴 [CRITICAL] `panic!` @ L26:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
other => panic!("期望 ColumnCountMismatch {{1, 0}}，实际: {:?}", other),
```

#### 🔴 [CRITICAL] `panic!` @ L39:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
other => panic!("期望 ColumnCountMismatch {{1, 2}}，实际: {:?}", other),
```

#### 🔴 [CRITICAL] `panic!` @ L62:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
other => panic!("期望 ColumnCountMismatch {{2, 1}}，实际: {:?}", other),
```

#### 🔴 [CRITICAL] `panic!` @ L87:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
other => panic!("期望 ColumnCountMismatch {{3, 2}}，实际: {:?}", other),
```

#### 🔴 [CRITICAL] `panic!` @ L121:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
other => panic!("期望 ColumnCountMismatch {{3, 2}}，实际: {:?}", other),
```

#### 🔴 [CRITICAL] `panic!` @ L173:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
other => panic!("期望 MissingColumn {{name}}，实际: {:?}", other),
```

### `packages\sz-orm-core\tests\contracts\transaction_contract.rs`

#### 🔴 [CRITICAL] `panic!` @ L66:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
other => panic!("期望 NotActive(Committed)，实际: {:?}", other),
```

#### 🔴 [CRITICAL] `panic!` @ L81:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
other => panic!("期望 NotActive(Committed)，实际: {:?}", other),
```

#### 🔴 [CRITICAL] `panic!` @ L122:C13

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
panic!("v0.2.0 契约违反：commit 后 savepoint 应返回 NotActive，而非 SavepointError");
```

#### 🔴 [CRITICAL] `panic!` @ L124:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
other => panic!("期望 NotActive(Committed)，实际: {:?}", other),
```

#### 🔴 [CRITICAL] `panic!` @ L226:C23

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
Err(other) => panic!("期望 NotActive(Active)，实际: {:?}", other),
```

#### 🔴 [CRITICAL] `panic!` @ L227:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
Ok(_) => panic!("Active 状态 take_connection 必须返回 Err，实际返回 Ok"),
```

#### 🔴 [CRITICAL] `panic!` @ L261:C23

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
Err(other) => panic!("期望 ConnectionTaken，实际: {:?}", other),
```

#### 🔴 [CRITICAL] `panic!` @ L262:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
Ok(_) => panic!("重复 take_connection 必须返回 Err，实际返回 Ok"),
```

### `packages\sz-orm-core\tests\contracts\value_contract.rs`

#### 🔴 [CRITICAL] `panic!` @ L164:C14

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("Expected Value::Array"),
```

### `packages\sz-orm-core\tests\core.rs`

#### 🔴 [CRITICAL] `panic!` @ L152:C13

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
panic!("Expected array");
```

### `packages\sz-orm-core\tests\e2e_eager_cycle.rs`

#### 🔴 [CRITICAL] `panic!` @ L361:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
Ok(_) => panic!("期望 Err，得到 Ok"),
```

#### 🔴 [CRITICAL] `panic!` @ L384:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
Ok(_) => panic!("期望 Err，得到 Ok"),
```

#### 🔴 [CRITICAL] `panic!` @ L423:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
Ok(_) => panic!("期望 Err，得到 Ok"),
```

### `packages\sz-orm-core\tests\e2e_transaction.rs`

#### 🔴 [CRITICAL] `panic!` @ L370:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
other => panic!("期望 CommitFailed(timeout)，实际: {:?}", other),
```

#### 🔴 [CRITICAL] `panic!` @ L412:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
other => panic!("期望 MaxNestingDepthExceeded，实际: {:?}", other),
```

### `packages\sz-orm-core\tests\fuzz.rs`

#### 🔴 [CRITICAL] `panic!` @ L70:C25

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
panic!(
```

### `packages\sz-orm-core\tests\stress.rs`

#### 🔴 [CRITICAL] `panic!` @ L175:C27

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
Err(e) => panic!("unexpected error: {:?}", e),
```

### `packages\sz-orm-dtx\src\saga.rs`

#### 🔴 [CRITICAL] `panic!` @ L1084:C22

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
other => panic!("expected Compensated, got {:?}", other),
```

#### 🔴 [CRITICAL] `panic!` @ L1181:C22

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
other => panic!("expected CompensationFailed, got {:?}", other),
```

### `packages\sz-orm-dtx\src\tcc.rs`

#### 🔴 [CRITICAL] `panic!` @ L1285:C22

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
other => panic!("期望 TryFailed 错误，得到 {:?}", other),
```

#### 🔴 [CRITICAL] `panic!` @ L1439:C22

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
other => panic!("期望 InvalidState 错误，得到 {:?}", other),
```

### `packages\sz-orm-logger\src\advanced.rs`

#### 🔴 [CRITICAL] `panic!` @ L822:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("expected Size policy"),
```

### `packages\sz-orm-mqtt\src\will.rs`

#### 🔴 [CRITICAL] `panic!` @ L369:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("expected Delivered, got {:?}", result),
```

#### 🔴 [CRITICAL] `panic!` @ L407:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("expected Scheduled"),
```

### `packages\sz-orm-mssql\src\lib.rs`

#### 🔴 [CRITICAL] `panic!` @ L975:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("expected I64(Some(42))"),
```

#### 🔴 [CRITICAL] `panic!` @ L985:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("expected String"),
```

#### 🔴 [CRITICAL] `panic!` @ L1004:C13

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
panic!("expected F64");
```

#### 🔴 [CRITICAL] `panic!` @ L1022:C13

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
panic!("expected Bytes");
```

#### 🔴 [CRITICAL] `panic!` @ L1039:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("expected String(Some)"),
```

### `packages\sz-orm-postgis\src\extensions.rs`

#### 🔴 [CRITICAL] `panic!` @ L1095:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("expected MultiPoint"),
```

#### 🔴 [CRITICAL] `panic!` @ L1142:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("expected Polygon"),
```

#### 🔴 [CRITICAL] `panic!` @ L1188:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("expected Polygon"),
```

#### 🔴 [CRITICAL] `panic!` @ L1224:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("expected Point"),
```

#### 🔴 [CRITICAL] `panic!` @ L1243:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("expected Point"),
```

#### 🔴 [CRITICAL] `panic!` @ L1257:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("expected Point"),
```

#### 🔴 [CRITICAL] `panic!` @ L1282:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("expected LineString"),
```

### `packages\sz-orm-postgis\src\memory.rs`

#### 🔴 [CRITICAL] `panic!` @ L237:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("expected Polygon"),
```

#### 🔴 [CRITICAL] `panic!` @ L249:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("expected MultiPoint"),
```

### `packages\sz-orm-postgis\tests\integration.rs`

#### 🔴 [CRITICAL] `panic!` @ L31:C33

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
.unwrap_or_else(|e| panic!("distance {}-{} failed: {}", i, j, e));
```

#### 🔴 [CRITICAL] `panic!` @ L106:C14

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("expected Polygon"),
```

#### 🔴 [CRITICAL] `panic!` @ L142:C14

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("expected Stub variant"),
```

#### 🔴 [CRITICAL] `panic!` @ L230:C14

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("expected MultiPoint"),
```

### `packages\sz-orm-queue\src\queue.rs`

#### 🔴 [CRITICAL] `panic!` @ L902:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("Expected MqError::Publish with overflow message"),
```

#### 🔴 [CRITICAL] `panic!` @ L1029:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("Expected MqError::NotSupported"),
```

#### 🔴 [CRITICAL] `panic!` @ L1184:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("Expected MqError::NotSupported"),
```

#### 🔴 [CRITICAL] `panic!` @ L1351:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("Expected MqError::Connection"),
```

#### 🔴 [CRITICAL] `panic!` @ L1372:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("Expected MqError::Publish"),
```

### `packages\sz-orm-sharding\src\cross_shard_tx.rs`

#### 🔴 [CRITICAL] `panic!` @ L432:C22

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
other => panic!("expected CommitFailed, got {:?}", other),
```

### `packages\sz-orm-sharding\src\lib.rs`

#### 🔴 [CRITICAL] `panic!` @ L688:C13

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
panic!("expected NoMappingForKey");
```

### `packages\sz-orm-sharding\src\scatter.rs`

#### 🔴 [CRITICAL] `panic!` @ L305:C22

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("unexpected shard: {}", shard),
```

### `packages\sz-orm-storage\src\advanced.rs`

#### 🔴 [CRITICAL] `panic!` @ L1127:C13

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
panic!("expected Transition action");
```

### `packages\sz-orm-storage\tests\stress.rs`

#### 🔴 [CRITICAL] `panic!` @ L115:C25

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
panic!("file {} still readable after delete and 30 retries", key);
```

#### 🔴 [CRITICAL] `panic!` @ L266:C14

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("expected Local variant"),
```

### `packages\sz-orm-swagger\src\lib.rs`

#### 🔴 [CRITICAL] `panic!` @ L1482:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("Expected Ref variant"),
```

#### 🔴 [CRITICAL] `panic!` @ L1496:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("Expected Primitive variant"),
```

#### 🔴 [CRITICAL] `panic!` @ L1508:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("Expected Primitive variant"),
```

#### 🔴 [CRITICAL] `panic!` @ L1520:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("Expected Primitive variant"),
```

#### 🔴 [CRITICAL] `panic!` @ L1531:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("Expected Primitive variant"),
```

#### 🔴 [CRITICAL] `panic!` @ L1656:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("Expected Http variant"),
```

#### 🔴 [CRITICAL] `panic!` @ L1674:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("Expected Http variant"),
```

#### 🔴 [CRITICAL] `panic!` @ L1685:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("Expected Http variant"),
```

#### 🔴 [CRITICAL] `panic!` @ L1697:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("Expected ApiKey variant"),
```

#### 🔴 [CRITICAL] `panic!` @ L1708:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("Expected ApiKey variant"),
```

#### 🔴 [CRITICAL] `panic!` @ L1719:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("Expected ApiKey variant"),
```

#### 🔴 [CRITICAL] `panic!` @ L1741:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("Expected OAuth2 variant"),
```

#### 🔴 [CRITICAL] `panic!` @ L1752:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("Expected Http variant"),
```

### `packages\sz-orm-wasm\src\advanced.rs`

#### 🔴 [CRITICAL] `panic!` @ L1297:C22

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
other => panic!("expected TooManyTables, got {:?}", other),
```

#### 🔴 [CRITICAL] `panic!` @ L1364:C22

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
other => panic!("expected RowTooLarge, got {:?}", other),
```

#### 🔴 [CRITICAL] `panic!` @ L1472:C22

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
other => panic!("expected AccessDenied, got {:?}", other),
```

### `packages\sz-orm-websocket\src\compression.rs`

#### 🔴 [CRITICAL] `panic!` @ L567:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("expected Accepted, got {:?}", result),
```

#### 🔴 [CRITICAL] `panic!` @ L581:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("expected Accepted, got {:?}", result),
```

#### 🔴 [CRITICAL] `panic!` @ L594:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("expected Accepted, got {:?}", result),
```

#### 🔴 [CRITICAL] `panic!` @ L614:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("expected Accepted, got {:?}", result),
```

#### 🔴 [CRITICAL] `panic!` @ L629:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("expected Accepted, got {:?}", result),
```

#### 🔴 [CRITICAL] `panic!` @ L641:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("expected Accepted, got {:?}", result),
```

### `packages\sz-orm-websocket\src\pool.rs`

#### 🔴 [CRITICAL] `panic!` @ L381:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("expected EvictedAndAdmitted, got {:?}", result),
```

#### 🔴 [CRITICAL] `panic!` @ L402:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("expected c2 to be evicted"),
```

### `packages\sz-orm-websocket\src\subprotocol.rs`

#### 🔴 [CRITICAL] `panic!` @ L633:C18

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
_ => panic!("expected NoMatch"),
```

#### 🔴 [CRITICAL] `panic!` @ L756:C13

- **上下文**: 运行时 panic，进程终止
- **建议**: 返回 Result/Error，或使用 expect() 附带明确错误信息

```rust
panic!("expected Accepted");
```

### `packages\sz-orm-ai\src\embedding\mod.rs`

#### 🟠 [HIGH] `unwrap()` @ L624:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let v = model.embed("hello world").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L634:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let v = model.embed("").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L641:C65

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let v1 = model.embed("rust programming language").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L642:C65

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let v2 = model.embed("rust programming language").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L649:C65

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let v1 = model.embed("the quick brown fox jumps").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L650:C59

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let v2 = model.embed("the quick brown fox").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L654:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L668:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let vecs = model.embed_batch(&texts).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L700:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let v1 = caching.embed("hello world").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L701:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let v2 = caching.embed("hello world").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L715:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let v1 = caching.embed("hello").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L716:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let v2 = caching.embed("world").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L732:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let v1_miss = caching.embed("a").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L733:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let v1_hit = caching.embed("a").await.unwrap(); // hit
```

#### 🟠 [HIGH] `unwrap()` @ L734:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let v2_miss = caching.embed("b").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L735:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let v1_hit2 = caching.embed("a").await.unwrap(); // hit
```

#### 🟠 [HIGH] `unwrap()` @ L758:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let v1 = caching.embed("hello").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L765:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let v2 = caching.embed("hello").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L776:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let seed = caching.embed("hello").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L780:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let results = caching.embed_batch(&texts).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L798:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let seed_hello = caching.embed("hello").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L799:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let seed_world = caching.embed("world").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L803:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let results = caching.embed_batch(&texts).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L835:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let v = normalized.embed("hello world").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L847:C60

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let vectors = normalized.embed_batch(&texts).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L906:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let v = reduced.embed("hello world").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L916:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let vectors = reduced.embed_batch(&texts).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L927:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let v = reduced.embed("hello world").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1004:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let v1 = logging.embed("hello").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1005:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let v2 = logging.embed("world").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1022:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let results = logging.embed_batch(&texts).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1065:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let v1 = normalized.embed("hello world").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1066:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let v2 = normalized.embed("hello world").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1080:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let v1 = caching.embed("hello").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1081:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let v2 = caching.embed("hello").await.unwrap(); // cache hit
```

### `packages\sz-orm-ai\src\nl2sql.rs`

#### 🟠 [HIGH] `unwrap()` @ L1772:C71

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = engine.generate("show all users", &schema).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1785:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1793:C71

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = engine.generate("how many users", &schema).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1804:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1816:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1827:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1838:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1846:C71

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = engine.generate("first 10 users", &schema).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1857:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1870:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1881:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1918:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(engine.validate(&query).await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L1941:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!engine.validate(&query).await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L1948:C73

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = engine.generate("show all persons", &schema).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1970:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L2184:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let hint = select_star_hint.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L2186:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let suggested = hint.suggested_sql.as_ref().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L2201:C31

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(where_hint.unwrap().severity, HintSeverity::Critical);
```

#### 🟠 [HIGH] `unwrap()` @ L2214:C31

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let hint = limit_hint.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L2216:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(hint.suggested_sql.as_ref().unwrap().contains("LIMIT 100"));
```

#### 🟠 [HIGH] `unwrap()` @ L2293:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(join_hint.unwrap().severity, HintSeverity::Critical);
```

#### 🟠 [HIGH] `unwrap()` @ L2359:C28

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(index_hint.unwrap().title.contains("age"));
```

### `packages\sz-orm-ai\src\real_embedding.rs`

#### 🟠 [HIGH] `expect()` @ L269:C55

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let api_key = std::env::var("OPENAI_API_KEY").expect("OPENAI_API_KEY 未设置");
```

#### 🟠 [HIGH] `expect()` @ L271:C51

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let v = client.embed("hello world").await.expect("embed 失败");
```

#### 🟠 [HIGH] `expect()` @ L279:C55

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let api_key = std::env::var("OPENAI_API_KEY").expect("OPENAI_API_KEY 未设置");
```

#### 🟠 [HIGH] `expect()` @ L282:C53

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let vecs = client.embed_batch(&texts).await.expect("batch embed 失败");
```

### `packages\sz-orm-ai\src\vector\hnsw.rs`

#### 🟠 [HIGH] `unwrap()` @ L803:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
index.insert("v1", vec![1.0, 0.0, 0.0]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L815:C18

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L831:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
index.insert("v1", vec![1.0, 0.0, 0.0]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L839:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
index.insert("v1", vec![1.0, 0.0, 0.0]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L840:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
index.insert("v1", vec![0.0, 1.0, 0.0]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L843:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let vector = index.get("v1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L850:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let results = index.search(&[1.0, 0.0], 5, 50).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L857:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
index.insert("v1", vec![1.0, 0.0, 0.0]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L858:C61

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let results = index.search(&[1.0, 0.0, 0.0], 1, 50).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L870:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
index.insert("a", vec![1.0, 0.0, 0.0]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L871:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
index.insert("b", vec![0.0, 1.0, 0.0]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L872:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
index.insert("c", vec![1.0, 1.0, 0.0]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L875:C61

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let results = index.search(&[1.0, 0.0, 0.0], 2, 50).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L888:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
index.insert("a", vec![1.0, 0.0]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L889:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
index.insert("b", vec![5.0, 0.0]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L890:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
index.insert("c", vec![10.0, 0.0]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L892:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let results = index.search(&[1.0, 0.0], 2, 50).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L902:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
index.insert("a", vec![1.0, 1.0]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L903:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
index.insert("b", vec![2.0, 2.0]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L904:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
index.insert("c", vec![0.1, 0.1]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L906:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let results = index.search(&[1.0, 1.0], 3, 50).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L917:C18

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L920:C61

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let results = index.search(&[5.0, 0.0, 0.0], 5, 50).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L932:C63

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
index.insert(&format!("v{}", i), vec![x, y, 0.0]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L938:C62

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let results = index.search(&[0.0, 1.0, 0.0], 5, 100).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L947:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
index.insert("a", vec![1.0, 0.0]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L948:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
index.insert("b", vec![0.0, 1.0]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L949:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
index.insert("c", vec![1.0, 1.0]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L952:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let deleted = index.delete("b").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L959:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let results = index.search(&[0.0, 1.0], 3, 50).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L968:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = index.delete("nonexistent").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L975:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
index.insert("v1", vec![1.0, 2.0, 3.0]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L977:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let vector = index.get("v1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L987:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
index.insert("v1", vec![1.0, 0.0]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L988:C28

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
index.delete("v1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L996:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
index.insert("a", vec![1.0, 0.0]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L997:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
index.insert("b", vec![0.0, 1.0]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L998:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
index.insert("c", vec![1.0, 1.0]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1005:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
index.delete("b").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1014:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
index.insert("v1", vec![1.0, 0.0]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1022:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
index.insert("v1", vec![1.0, 0.0, 0.0]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1097:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
index.insert("a", vec![1.0, 0.0]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1098:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
index.insert("b", vec![0.0, 1.0]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1101:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
index.delete("a").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1105:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
index.insert("a", vec![1.0, 0.0]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1109:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let results = index.search(&[1.0, 0.0], 2, 50).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1122:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
index.insert(&format!("v{}", i), vector).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1133:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let results = index.search(&query, 3, 50).unwrap();
```

### `packages\sz-orm-ai\src\vector\mod.rs`

#### 🟠 [HIGH] `unwrap()` @ L494:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.create_collection("docs", 4, None).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L495:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(store.count("docs").await.unwrap(), 0);
```

#### 🟠 [HIGH] `unwrap()` @ L497:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.delete_collection("docs").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L499:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(store.count("docs").await.unwrap(), 0);
```

#### 🟠 [HIGH] `unwrap()` @ L505:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.create_collection("docs", 3, None).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L507:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.insert("docs", vec![rec]).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L508:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(store.count("docs").await.unwrap(), 1);
```

#### 🟠 [HIGH] `unwrap()` @ L510:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let fetched = store.get("docs", "r1").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L510:C62

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let fetched = store.get("docs", "r1").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L514:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(store.get("docs", "missing").await.unwrap().is_none());
```

#### 🟠 [HIGH] `unwrap()` @ L520:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.create_collection("docs", 3, None).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L529:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.create_collection("docs", 2, None).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L533:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L537:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L539:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(store.count("docs").await.unwrap(), 1);
```

#### 🟠 [HIGH] `unwrap()` @ L540:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let fetched = store.get("docs", "r1").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L540:C62

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let fetched = store.get("docs", "r1").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L550:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L556:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.insert("docs", records).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L561:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L571:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.create_collection("docs", 2, None).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L579:C18

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L581:C72

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let results = store.search("docs", &[0.0, 1.0], 3, None).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L588:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.create_collection("docs", 2, None).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L599:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L603:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L605:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(store.count("docs").await.unwrap(), 1);
```

#### 🟠 [HIGH] `unwrap()` @ L611:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.create_collection("docs", 2, None).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L618:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.insert("docs", vec![r1, r2]).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L628:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L637:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let count = store.count("nonexistent").await.unwrap();
```

### `packages\sz-orm-ai\tests\ai_tests.rs`

#### 🟠 [HIGH] `unwrap()` @ L174:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let v = model.embed("hello world hello").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L189:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let v = model.embed("").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L194:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let v2 = model.embed("!@#$%^&*()").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L200:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let v3 = zero_model.embed("anything").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L209:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let a = model.embed("rust async programming").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L210:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let b = model.embed("rust async programming").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L218:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let batch = model.embed_batch(&texts).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L221:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let single = model.embed(t).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L229:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let empty_batch = model.embed_batch(&[]).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L237:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let v_same_a = model.embed("rust rust rust").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L238:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let v_same_b = model.embed("rust rust rust").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L243:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let v_diff = model.embed("zzz qqq xxx").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L334:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.create_collection("docs", 3, None).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L335:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(store.count("docs").await.unwrap(), 0);
```

#### 🟠 [HIGH] `unwrap()` @ L342:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.insert("docs", records).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L343:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(store.count("docs").await.unwrap(), 2);
```

#### 🟠 [HIGH] `unwrap()` @ L346:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let fetched = store.get("docs", "a").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L346:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let fetched = store.get("docs", "a").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L351:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(store.get("docs", "missing").await.unwrap().is_none());
```

#### 🟠 [HIGH] `unwrap()` @ L357:C69

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let removed = store.delete("docs", vec!["a".to_string()]).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L359:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(store.count("docs").await.unwrap(), 1);
```

#### 🟠 [HIGH] `unwrap()` @ L365:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.delete_collection("docs").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L366:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(store.count("docs").await.unwrap(), 0);
```

#### 🟠 [HIGH] `unwrap()` @ L369:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(store.count("never_exists").await.unwrap(), 0);
```

#### 🟠 [HIGH] `unwrap()` @ L383:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.create_collection("docs", 3, None).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L404:C61

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.create_collection("c", 3, Some(metric)).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L410:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.insert("c", records).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L412:C64

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let results = store.search("c", &query, 3, None).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L431:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.create_collection("c", 2, None).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L437:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L441:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L442:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(store.count("c").await.unwrap(), 1);
```

#### 🟠 [HIGH] `unwrap()` @ L443:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let fetched = store.get("c", "r1").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L443:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let fetched = store.get("c", "r1").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L457:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L459:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(store.count("c").await.unwrap(), 5);
```

#### 🟠 [HIGH] `unwrap()` @ L462:C67

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let results = store.search("c", &[0.0, 1.0], 100, None).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L466:C65

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let results = store.search("c", &[0.0, 1.0], 0, None).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L473:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.create_collection("c", 2, None).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L486:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.insert("c", vec![r1, r2, r3]).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L492:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L500:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L508:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L516:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L523:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L530:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L534:C63

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let res = store.search("c", &[1.0, 0.0], 100, None).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L542:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.create_collection("c", 3, None).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L546:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L548:C58

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let results = store.search("c", &[], 10, None).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L625:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let indexed = engine.index_documents(docs).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L629:C59

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let results = engine.search("rust async", None).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L652:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let indexed = engine.index_documents(vec![]).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L656:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let results = engine.search("anything", None).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L660:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let deleted = engine.delete_document("d1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L676:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let n = engine.index_documents(docs).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L680:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let results = engine.search("hello", None).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L696:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let indexed = engine.index_documents(docs).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L701:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let deleted = engine.delete_document("d1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L705:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let deleted2 = engine.delete_document("d1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L709:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let results = engine.search("rust", None).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L734:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let indexed = engine.index_documents(docs).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L742:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L746:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let results = engine.search("alpha", None).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L778:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let n = engine.index_documents(docs).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L782:C69

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let results = engine.search("programming language", None).await.unwrap();
```

### `packages\sz-orm-audit\src\lib.rs`

#### 🟠 [HIGH] `expect()` @ L126:C18

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("i < bytes.len() guarantees non-empty slice");
```

#### 🟠 [HIGH] `unwrap()` @ L1032:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let path_str = path.to_str().unwrap();
```

#### 🟠 [HIGH] `expect()` @ L1033:C39

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let count = a.flush(path_str).expect("flush should succeed");
```

#### 🟠 [HIGH] `expect()` @ L1036:C57

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let content = std::fs::read_to_string(path_str).expect("file should be readable");
```

#### 🟠 [HIGH] `expect()` @ L1038:C44

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
serde_json::from_str(&content).expect("should parse as JSON array");
```

#### 🟠 [HIGH] `unwrap()` @ L1052:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let path_str = path.to_str().unwrap();
```

#### 🟠 [HIGH] `expect()` @ L1053:C39

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let count = a.flush(path_str).expect("flush should succeed");
```

#### 🟠 [HIGH] `expect()` @ L1055:C57

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let content = std::fs::read_to_string(path_str).expect("file should be readable");
```

#### 🟠 [HIGH] `unwrap()` @ L1257:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1260:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L1261:C38

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let logs = writer.shutdown().expect("shutdown should succeed");
```

#### 🟠 [HIGH] `unwrap()` @ L1272:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1273:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let logs = writer.shutdown().unwrap();
```

#### 🟠 [HIGH] `expect()` @ L1281:C38

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let logs = writer.shutdown().expect("shutdown should succeed");
```

#### 🟠 [HIGH] `unwrap()` @ L1288:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let _ = writer.shutdown().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1296:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
writer.log(&ctx("SELECT 1", "u", 1)).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1297:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let logs = writer.shutdown().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1424:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let path_str = path.to_str().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1431:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1434:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1436:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let logs = store.read_all().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1449:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let path_str = path.to_str().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1459:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1461:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let logs = store.read_all().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1474:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let path_str = path.to_str().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1478:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.append(&ctx("SELECT 1", "u", 1)).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1479:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.append(&ctx("SELECT 2", "u", 2)).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1480:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(store.read_all().unwrap().len(), 2);
```

#### 🟠 [HIGH] `unwrap()` @ L1482:C23

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.clear().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1484:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(store.read_all().unwrap().len(), 0);
```

#### 🟠 [HIGH] `unwrap()` @ L1493:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let path_str = path.to_str().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1503:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let path_str = path.to_str().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1508:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let logs = store.read_all().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1517:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let path_str = path.to_str().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1521:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.append(&ctx("SELECT 1", "u", 1)).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1527:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1529:C24

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
writeln!(file).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1530:C31

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
writeln!(file, "   ").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1533:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.append(&ctx("SELECT 2", "u", 2)).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1535:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let logs = store.read_all().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1552:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let path_str = path.to_str().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1562:C26

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1567:C22

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
h.join().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1571:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let logs = store.read_all().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1581:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let path_str = path.to_str().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1585:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.append(&ctx("SELECT 1", "u", 1)).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1586:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let logs = store.read_all().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1732:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let path_str = path.to_str().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1733:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let count = auditor.flush(path_str).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1737:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let content = std::fs::read_to_string(path_str).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1760:C22

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
h.join().unwrap();
```

### `packages\sz-orm-auth\src\auth.rs`

#### 🟠 [HIGH] `expect()` @ L393:C47

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let token = auth.authenticate(&creds).expect("authenticate");
```

#### 🟠 [HIGH] `unwrap()` @ L402:C69

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let refresh_parts: Vec<&str> = token.refresh_token.as_ref().unwrap().split('.').collect();
```

#### 🟠 [HIGH] `expect()` @ L419:C47

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let token = auth.authenticate(&creds).expect("authenticate");
```

#### 🟠 [HIGH] `expect()` @ L420:C59

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let user = auth.verify_token(&token.access_token).expect("verify");
```

#### 🟠 [HIGH] `unwrap()` @ L447:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L455:C73

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let token = auth.authenticate(&Credentials::new("carol", "pw")).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L458:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.refresh_token(token.refresh_token.as_ref().unwrap())
```

#### 🟠 [HIGH] `expect()` @ L459:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("refresh");
```

#### 🟠 [HIGH] `expect()` @ L463:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("verify refreshed");
```

#### 🟠 [HIGH] `unwrap()` @ L484:C72

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let token = auth.authenticate(&Credentials::new("dave", "pw")).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L485:C59

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let user = auth.verify_token(&token.access_token).unwrap();
```

### `packages\sz-orm-auth\src\authorizer.rs`

#### 🟠 [HIGH] `unwrap()` @ L239:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(authz.can(&user, "read", "posts").unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L240:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(authz.can(&user, "delete", "users").unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L241:C58

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(authz.can(&user, "anything", "anything").unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L249:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(authz.can(&user, "read", "posts").unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L251:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!authz.can(&user, "read", "users").unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L252:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!authz.can(&user, "delete", "posts").unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L260:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(authz.can(&user, "read", "posts").unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L261:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(authz.can(&user, "read", "users").unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L262:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!authz.can(&user, "write", "posts").unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L274:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(authz.can(&editor, "write", "posts").unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L275:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!authz.can(&editor, "delete", "posts").unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L277:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(authz.can(&viewer, "read", "posts").unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L278:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!authz.can(&viewer, "write", "posts").unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L286:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(authz.can(&user, "write", "posts").unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L289:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!authz.can(&user, "write", "posts").unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L296:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!authz.can(&user, "read", "anything").unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L319:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(authz.can(&user, "read", "posts").unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L320:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(authz.can(&user, "write", "posts").unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L330:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(authz.can(&user, "read", "posts").unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L331:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(authz.can(&user, "write", "posts").unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L332:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!authz.can(&user, "delete", "posts").unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L355:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(authz.can(&editor, "read", "posts").unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L356:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(authz.can(&editor, "write", "posts").unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L365:C65

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(authz.can(&super_admin, "anything", "anything").unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L366:C58

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(authz.can(&super_admin, "delete", "all").unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L380:C59

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(authz.can(&admin_custom, "read", "posts").unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L381:C60

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(authz.can(&admin_custom, "write", "posts").unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L382:C62

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!authz.can(&admin_custom, "delete", "posts").unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L506:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(authz.can(&user, "read", "posts").unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L507:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!authz.can(&user, "delete", "posts").unwrap());
```

### `packages\sz-orm-auth\src\jwt.rs`

#### 🟠 [HIGH] `expect()` @ L250:C51

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let mut mac = HmacSha256::new_from_slice(key).expect("HMAC accepts any key length");
```

#### 🟠 [HIGH] `unwrap()` @ L341:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(base64_url_decode("").unwrap(), b"");
```

#### 🟠 [HIGH] `unwrap()` @ L342:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(base64_url_decode("Zg").unwrap(), b"f");
```

#### 🟠 [HIGH] `unwrap()` @ L343:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(base64_url_decode("Zm8").unwrap(), b"fo");
```

#### 🟠 [HIGH] `unwrap()` @ L344:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(base64_url_decode("Zm9v").unwrap(), b"foo");
```

#### 🟠 [HIGH] `unwrap()` @ L345:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(base64_url_decode("Zm9vYg").unwrap(), b"foob");
```

#### 🟠 [HIGH] `unwrap()` @ L346:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(base64_url_decode("Zm9vYmE").unwrap(), b"fooba");
```

#### 🟠 [HIGH] `unwrap()` @ L347:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(base64_url_decode("Zm9vYmFy").unwrap(), b"foobar");
```

#### 🟠 [HIGH] `unwrap()` @ L365:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let decoded = base64_url_decode(&encoded).unwrap();
```

#### 🟠 [HIGH] `expect()` @ L390:C45

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let token = encoder.encode(&claims).expect("encode");
```

#### 🟠 [HIGH] `expect()` @ L396:C46

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let decoded = encoder.decode(&token).expect("decode");
```

#### 🟠 [HIGH] `unwrap()` @ L413:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let token = encoder.encode(&claims).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L418:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let header_bytes = base64_url_decode(parts[0]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L419:C71

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let header: JwtHeader = serde_json::from_slice(&header_bytes).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L430:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let token_a = encoder_a.encode(&claims).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L431:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let token_b = encoder_b.encode(&claims).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L447:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let token = encoder_a.encode(&claims).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L456:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let token = encoder.encode(&claims).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L482:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let token = encoder.encode(&claims).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L498:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let token = encoder.encode(&claims).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L503:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let first = sig.chars().next().unwrap();
```

### `packages\sz-orm-auth\src\lib.rs`

#### 🟠 [HIGH] `unwrap()` @ L57:C26

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(can_read.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L58:C29

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!can_delete.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L68:C33

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(can_do_anything.unwrap());
```

#### 🟠 [HIGH] `expect()` @ L78:C47

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let token = auth.authenticate(&creds).expect("authenticate");
```

#### 🟠 [HIGH] `expect()` @ L81:C59

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let user = auth.verify_token(&token.access_token).expect("verify");
```

#### 🟠 [HIGH] `expect()` @ L91:C45

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let token = encoder.encode(&claims).expect("encode");
```

#### 🟠 [HIGH] `expect()` @ L92:C46

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let decoded = encoder.decode(&token).expect("decode");
```

### `packages\sz-orm-auth\src\mfa.rs`

#### 🟠 [HIGH] `unwrap()` @ L331:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let decoded = base32_decode(&encoded).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L349:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(base32_decode("").unwrap(), b"");
```

#### 🟠 [HIGH] `unwrap()` @ L350:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(base32_decode("MY").unwrap(), b"f");
```

#### 🟠 [HIGH] `unwrap()` @ L351:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(base32_decode("MZXQ").unwrap(), b"fo");
```

#### 🟠 [HIGH] `unwrap()` @ L352:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(base32_decode("MZXW6").unwrap(), b"foo");
```

#### 🟠 [HIGH] `unwrap()` @ L357:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(base32_decode("mzxw6").unwrap(), b"foo");
```

#### 🟠 [HIGH] `unwrap()` @ L499:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let code = mgr.generate_code("user1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L500:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(mgr.verify("user1", &code).unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L507:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!mgr.verify("user1", "000000").unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L539:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let code = mgr.generate_code("bob").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L540:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(mgr.verify("bob", &code).unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L547:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let uri = mgr.get_uri("user1").unwrap();
```

### `packages\sz-orm-auth\src\oauth2.rs`

#### 🟠 [HIGH] `unwrap()` @ L246:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
codes.get_mut(&req.code).unwrap().used = true;
```

#### 🟠 [HIGH] `unwrap()` @ L340:C63

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let code = server.create_authorization_code(&req, 42).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L371:C63

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let code = server.create_authorization_code(&req, 99).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L373:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = server.exchange_code(&token_req).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L390:C62

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let code = server.create_authorization_code(&req, 1).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L393:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
server.exchange_code(&token_req).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L403:C62

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let code = server.create_authorization_code(&req, 1).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L413:C62

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let code = server.create_authorization_code(&req, 1).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L431:C62

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let code = server.create_authorization_code(&req, 1).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L433:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
server.exchange_code(&token_req).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L444:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
server.create_authorization_code(&req, 1).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L481:C61

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let c1 = server.create_authorization_code(&req1, 1).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L482:C61

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let c2 = server.create_authorization_code(&req2, 2).unwrap();
```

### `packages\sz-orm-auth\src\token_store.rs`

#### 🟠 [HIGH] `unwrap()` @ L540:C58

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let stored = store.issue_family("refresh1", 100).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L551:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.issue_family("refresh1", 100).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L553:C63

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let new_token = store.refresh("refresh1", "refresh2").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L557:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.get_token("refresh1").unwrap().family_id
```

#### 🟠 [HIGH] `unwrap()` @ L562:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let old = store.get_token("refresh1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L577:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.issue_family("refresh1", 100).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L580:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.refresh("refresh1", "refresh2").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L587:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let family_id = store.get_token("refresh1").unwrap().family_id;
```

#### 🟠 [HIGH] `unwrap()` @ L591:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r2 = store.get_token("refresh2").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L599:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.issue_family("refresh1", 100).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L604:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tokens.get_mut("refresh1").unwrap().expires_at = current_secs() - 100;
```

#### 🟠 [HIGH] `unwrap()` @ L614:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.issue_family("refresh1", 100).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L615:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.revoke_token("refresh1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L624:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.issue_family("refresh1", 100).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L627:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.revoke_token("refresh1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L641:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.issue_family("refresh1", 100).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L642:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let family_id = store.get_token("refresh1").unwrap().family_id;
```

#### 🟠 [HIGH] `unwrap()` @ L645:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.refresh("refresh1", "refresh2").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L649:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let count = store.revoke_family(&family_id).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L667:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.issue_family("refresh1", 100).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L668:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.issue_family("refresh3", 100).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L669:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.issue_family("refresh5", 200).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L690:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.issue_family("refresh1", 100).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L698:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.issue_family("refresh1", 100).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L699:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let stored = store.get_token("refresh1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L707:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.issue_family("refresh1", 100).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L708:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let family_id = store.get_token("refresh1").unwrap().family_id;
```

#### 🟠 [HIGH] `unwrap()` @ L710:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.refresh("refresh1", "refresh2").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L711:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.refresh("refresh2", "refresh3").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L727:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.issue_family("refresh1", 100).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L728:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let family_id = store.get_token("refresh1").unwrap().family_id;
```

#### 🟠 [HIGH] `unwrap()` @ L731:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.revoke_family(&family_id).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L739:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.issue_family("refresh1", 100).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L740:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.issue_family("refresh2", 200).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L745:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tokens.get_mut("refresh1").unwrap().expires_at = current_secs() - 100;
```

#### 🟠 [HIGH] `unwrap()` @ L756:C58

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let stored = store.issue_family("refresh1", 100).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L767:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.issue_family("r1", 1).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L769:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r2 = store.refresh("r1", "r2").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L770:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r3 = store.refresh("r2", "r3").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L771:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r4 = store.refresh("r3", "r4").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L778:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(store.get_token("r1").unwrap().used);
```

#### 🟠 [HIGH] `unwrap()` @ L779:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(store.get_token("r2").unwrap().used);
```

#### 🟠 [HIGH] `unwrap()` @ L780:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(store.get_token("r3").unwrap().used);
```

#### 🟠 [HIGH] `unwrap()` @ L782:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!store.get_token("r4").unwrap().used);
```

#### 🟠 [HIGH] `unwrap()` @ L790:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.issue_family("r1", 1).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L791:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.refresh("r1", "r2").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L792:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.refresh("r2", "r3").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L799:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let family_id = store.get_token("r1").unwrap().family_id;
```

#### 🟠 [HIGH] `unwrap()` @ L816:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.issue_family("r1", 1).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L818:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.refresh("r1", "r2").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L826:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.issue_family("r1", 1).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L828:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.issue_family("r2", 2).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L831:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.refresh("r1", "r3").unwrap();
```

### `packages\sz-orm-back\src\advanced.rs`

#### 🟠 [HIGH] `unwrap()` @ L642:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let compressed = compress(data, &config).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L644:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let decompressed = decompress(&compressed).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L654:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let compressed = compress(data, &config).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L658:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let decompressed = decompress(&compressed).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L668:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let compressed = compress(data, &config).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L672:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let decompressed = decompress(&compressed).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L683:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let compressed = compress(&data, &config).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L699:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let compressed = compress(data, &config).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L710:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let compressed = compress(data, &config).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L720:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let compressed = compress(data, &gzip_config).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L722:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let decompressed = decompress(&compressed).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L732:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let compressed = compress(data, &zstd_config).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L734:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let decompressed = decompress(&compressed).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L741:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let decompressed = decompress(data).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L792:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let encrypted = encrypt(data, &config).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L794:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let decrypted = decrypt(&encrypted, &config).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L803:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let encrypted = encrypt(data, &config).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L806:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let decrypted = decrypt(&encrypted, &config).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L818:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let encrypted = encrypt(data, &enc_config).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L857:C61

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let encoded = encode_with_encryption(data, &config).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L867:C61

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let encoded = encode_with_encryption(data, &config).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L869:C65

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let decoded = decode_with_encryption(&encoded, &config).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L877:C61

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let decoded = decode_with_encryption(data, &config).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L885:C61

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let encoded = encode_with_encryption(data, &config).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1225:C59

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let compressed = compress(original, &comp_config).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1228:C72

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let encoded = encode_with_encryption(&compressed, &enc_config).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1232:C71

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let decrypted = decode_with_encryption(&encoded, &enc_config).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1235:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let restored = decompress(&decrypted).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1246:C59

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let compressed = compress(original, &comp_config).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1247:C72

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let encoded = encode_with_encryption(&compressed, &enc_config).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1249:C71

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let decrypted = decode_with_encryption(&encoded, &enc_config).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1250:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let restored = decompress(&decrypted).unwrap();
```

### `packages\sz-orm-back\src\lib.rs`

#### 🟠 [HIGH] `expect()` @ L415:C18

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("gzip decode in test helper must succeed");
```

#### 🟠 [HIGH] `unwrap()` @ L508:C29

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = result.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L520:C18

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `unwrap()` @ L536:C67

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = manager.backup("prod_pool", &temp_dir).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L551:C64

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let bytes = tokio::fs::read(&result.output_path).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L553:C73

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let manifest: BackupManifest = serde_json::from_slice(&decoded).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L575:C18

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `unwrap()` @ L588:C72

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let backup_result = manager.backup("rt_pool", &temp_dir).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L595:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L612:C18

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `unwrap()` @ L616:C71

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let backup_result = manager.backup("pool_a", &temp_dir).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L641:C18

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `unwrap()` @ L644:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tokio::fs::create_dir_all(&temp_dir).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L651:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L672:C18

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `unwrap()` @ L675:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tokio::fs::create_dir_all(&temp_dir).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L679:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L699:C18

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `unwrap()` @ L711:C66

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = manager.export_sql("prod", &temp_dir).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L720:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L736:C18

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `unwrap()` @ L745:C64

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let export = manager.export_sql("rt", &temp_dir).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L748:C74

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let import = restore.import_sql("rt", &export.output_path).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L765:C18

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `unwrap()` @ L768:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tokio::fs::create_dir_all(&temp_dir).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L775:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L778:C63

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let import = restore.import_sql("p", &sql_path).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L817:C18

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `unwrap()` @ L821:C68

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = manager.backup("empty_pool", &temp_dir).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L827:C64

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let bytes = tokio::fs::read(&result.output_path).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L829:C73

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let manifest: BackupManifest = serde_json::from_slice(&decoded).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L869:C18

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `unwrap()` @ L875:C65

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = manager.backup("gz_pool", &temp_dir).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L878:C64

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let bytes = tokio::fs::read(&result.output_path).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L904:C18

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `unwrap()` @ L910:C68

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = manager.backup("plain_pool", &temp_dir).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L913:C64

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let bytes = tokio::fs::read(&result.output_path).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L919:C71

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let manifest: BackupManifest = serde_json::from_slice(&bytes).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L932:C18

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `unwrap()` @ L945:C74

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let backup_result = manager.backup("gzrt_pool", &temp_dir).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L952:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L968:C18

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `unwrap()` @ L981:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L985:C80

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let plain_result = manager_plain.backup("plain_pool", &temp_dir).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1092:C64

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let ok = restore.verify_checksum(&manifest, &expected).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1113:C61

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let ok = restore.verify_checksum(&manifest, &wrong).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1128:C64

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let manifest = manager.incremental_backup(since).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1156:C64

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let manifest = manager.incremental_backup(since).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1174:C64

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let manifest = manager.incremental_backup(since).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1191:C64

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let manifest = manager.incremental_backup(since).await.unwrap();
```

### `packages\sz-orm-batch\src\lib.rs`

#### 🟠 [HIGH] `unwrap()` @ L1394:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let json = serde_json::to_string(stage).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1395:C64

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let back: BatchStage = serde_json::from_str(&json).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1553:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(results[0].sql.as_ref().unwrap().contains("2 rows"));
```

#### 🟠 [HIGH] `unwrap()` @ L1806:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let json = serde_json::to_string(&r).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1807:C68

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let back: ChunkProcessResult = serde_json::from_str(&json).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1826:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let json = serde_json::to_string(&r).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1827:C62

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let back: UpsertResult = serde_json::from_str(&json).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1843:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let json = serde_json::to_string(&p).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1844:C63

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let back: BatchProgress = serde_json::from_str(&json).unwrap();
```

### `packages\sz-orm-config\src\env_source.rs`

#### 🟠 [HIGH] `unwrap()` @ L123:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let data = src.load().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L142:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let data = src.load().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L150:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let data = src.load().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L158:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let data = src.load().await.unwrap();
```

### `packages\sz-orm-config\src\file_source.rs`

#### 🟠 [HIGH] `unwrap()` @ L191:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
std::fs::write(path, content).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L199:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let data = src.load().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L212:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let data = src.load().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L256:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
src.watch(Box::new(move |m| { *r.lock().unwrap() = Some(m); }))
```

#### 🟠 [HIGH] `unwrap()` @ L258:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L265:C32

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
if received.lock().unwrap().is_some() {
```

#### 🟠 [HIGH] `unwrap()` @ L269:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let guard = received.lock().unwrap();
```

#### 🟠 [HIGH] `expect()` @ L270:C35

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let data = guard.as_ref().expect("应收到变更回调");
```

### `packages\sz-orm-config\src\lib.rs`

#### 🟠 [HIGH] `unwrap()` @ L1027:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(*last_value.lock().unwrap(), "v1");
```

#### 🟠 [HIGH] `unwrap()` @ L1031:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(*last_value.lock().unwrap(), "v2");
```

#### 🟠 [HIGH] `unwrap()` @ L1053:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(*last_value.lock().unwrap(), ""); // delete sends empty
```

#### 🟠 [HIGH] `unwrap()` @ L1128:C26

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
r.lock().unwrap().push((key.to_string(), value.to_string()));
```

#### 🟠 [HIGH] `unwrap()` @ L1133:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let received = received.lock().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1206:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(*received.lock().unwrap(), "8080");
```

#### 🟠 [HIGH] `unwrap()` @ L1251:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(*deleted_value.lock().unwrap(), "");
```

#### 🟠 [HIGH] `unwrap()` @ L1368:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1573:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let decrypted = enc.decrypt(&ciphertext).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1587:C59

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = enc.decrypt_if_needed("plain_value").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1595:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = enc.decrypt_if_needed(&ciphertext).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1614:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let decrypted = enc.decrypt_config(&config).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1642:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let decoded = base64_decode(&encoded).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1663:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = base64_decode("").unwrap();
```

### `packages\sz-orm-core\benches\core_bench.rs`

#### 🟠 [HIGH] `unwrap()` @ L123:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mysql = get_dialect(DbType::MySQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L124:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pg = get_dialect(DbType::PostgreSQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L125:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sqlite = get_dialect(DbType::Sqlite).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L155:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mysql = get_dialect(DbType::MySQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L156:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pg = get_dialect(DbType::PostgreSQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L157:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sqlite = get_dialect(DbType::Sqlite).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L203:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mysql = get_dialect(DbType::MySQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L204:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pg = get_dialect(DbType::PostgreSQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L205:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sqlite = get_dialect(DbType::Sqlite).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L304:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let rt = tokio::runtime::Runtime::new().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L321:C59

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool = Pool::new(config, factory).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L324:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let conn = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L452:C79

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
b.iter(|| serde_json::from_str::<serde_json::Value>(black_box(small)).unwrap())
```

#### 🟠 [HIGH] `unwrap()` @ L457:C81

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
b.iter(|| serde_json::from_str::<serde_json::Value>(black_box(&medium)).unwrap())
```

#### 🟠 [HIGH] `unwrap()` @ L462:C80

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
b.iter(|| serde_json::from_str::<serde_json::Value>(black_box(&large)).unwrap())
```

### `packages\sz-orm-core\src\access_control.rs`

#### 🟠 [HIGH] `unwrap()` @ L407:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let filter = built.row_filter("orders").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L425:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let filter = built.row_filter("profiles").unwrap();
```

### `packages\sz-orm-core\src\accessors.rs`

#### 🟠 [HIGH] `unwrap()` @ L855:C70

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parsed: serde_json::Value = serde_json::from_str(&s).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L876:C70

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parsed: serde_json::Value = serde_json::from_str(&s).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L976:C70

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parsed: serde_json::Value = serde_json::from_str(&s).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L994:C70

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parsed: serde_json::Value = serde_json::from_str(&s).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1005:C70

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parsed: serde_json::Value = serde_json::from_str(&s).unwrap();
```

### `packages\sz-orm-core\src\behaviors.rs`

#### 🟠 [HIGH] `unwrap()` @ L676:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
b.before_insert(&ctx, &mut attrs).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L686:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
b.before_update(&ctx, &mut attrs).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L697:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
b.before_insert(&ctx, &mut attrs).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L715:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
b.before_insert(&ctx, &mut attrs).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L725:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
b.before_update(&ctx, &mut attrs).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L736:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
b.before_insert(&ctx, &mut attrs).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L759:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
b.before_insert(&ctx, &mut attrs).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L770:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
b.before_insert(&ctx, &mut attrs).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L807:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
b.before_insert(&ctx, &mut attrs).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L941:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
r.before_insert(&ctx, &mut attrs).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L959:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
r.before_update(&ctx, &mut attrs).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L987:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
r.before_insert(&ctx, &mut attrs).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1018:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
b.before_insert(&ctx, &mut attrs).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1033:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
b.before_update(&ctx, &mut attrs).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1085:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
r.before_insert(&ctx, &mut attrs).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1102:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
r.before_update(&ctx, &mut attrs).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1151:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
r.before_insert(&ctx1, &mut attrs1).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1160:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
r.before_update(&ctx2, &mut attrs2).unwrap();
```

### `packages\sz-orm-core\src\cache.rs`

#### 🟠 [HIGH] `unwrap()` @ L480:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.set("key1", b"value1".to_vec(), None).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L481:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let val = cache.get("key1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L488:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.set("key1", b"value1".to_vec(), None).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L489:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.delete("key1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L490:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let val = cache.get("key1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L497:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.set("key1", b"value1".to_vec(), None).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L498:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let exists = cache.exists("key1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L500:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let exists2 = cache.exists("nonexistent").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L507:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.set("key1", b"value1".to_vec(), None).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L508:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.set("key2", b"value2".to_vec(), None).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L509:C23

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.clear().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L510:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let val = cache.get("key1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L517:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.set("key1", b"value1".to_vec(), None).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L518:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let val = cache.get("key1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L537:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
multi.set("key1", b"value1".to_vec(), None).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L538:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let val = multi.get("key1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L541:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
multi.delete("key1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L542:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let val = multi.get("key1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L550:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
inner.set("key1", b"value1".to_vec(), None).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L552:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = cache.get_or_negative("key1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L561:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = cache.get_or_negative("missing").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L564:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = cache.get_or_negative("missing").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L573:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.get_or_negative("key1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L575:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.get_or_negative("key1").unwrap(),
```

#### 🟠 [HIGH] `unwrap()` @ L579:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.set("key1", b"value1".to_vec(), None).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L581:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.get_or_negative("key1").unwrap(),
```

#### 🟠 [HIGH] `unwrap()` @ L589:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
inner.set("key1", b"value1".to_vec(), None).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L592:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.delete("key1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L593:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = cache.get_or_negative("key1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L602:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.get_or_negative("key1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L604:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.get_or_negative("key1").unwrap(),
```

#### 🟠 [HIGH] `unwrap()` @ L610:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = cache.get_or_negative("key1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L618:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.get_or_negative("key1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L619:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.get_or_negative("key2").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L620:C23

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.clear().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L622:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(cache.get_or_negative("key1").unwrap(), CacheLookup::Miss);
```

#### 🟠 [HIGH] `unwrap()` @ L623:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(cache.get_or_negative("key2").unwrap(), CacheLookup::Miss);
```

#### 🟠 [HIGH] `unwrap()` @ L630:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.get_or_negative("key1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L631:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.get_or_negative("key2").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L633:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let purged = cache.purge_expired().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L642:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let val = cache.get("missing").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L645:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let val = cache.get("missing").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L648:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.set("key1", b"value1".to_vec(), None).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L649:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let val = cache.get("key1").unwrap();
```

### `packages\sz-orm-core\src\data_permission.rs`

#### 🟠 [HIGH] `unwrap()` @ L681:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let clause = rule.apply(&ctx).unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L681:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let clause = rule.apply(&ctx).unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L691:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let clause = rule.apply(&ctx).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L710:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let clause = rule.apply(&ctx).unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L710:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let clause = rule.apply(&ctx).unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L720:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let clause = rule.apply(&ctx).unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L720:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let clause = rule.apply(&ctx).unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L730:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let clause = rule.apply(&ctx).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L751:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let clause = rule.apply(&ctx).unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L751:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let clause = rule.apply(&ctx).unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L759:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let clause = rule.apply(&ctx).unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L759:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let clause = rule.apply(&ctx).unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L769:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let clause = rule.apply(&ctx).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L785:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let clause = rule.apply(&ctx).unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L785:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let clause = rule.apply(&ctx).unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L799:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let clause = rule.apply(&ctx).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L811:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L823:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L836:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L850:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L870:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L883:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L896:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L945:C58

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let where_idx = sql.to_uppercase().find("WHERE").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L946:C64

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let group_by_idx = sql.to_uppercase().find("GROUP BY").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L956:C58

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let where_idx = sql.to_uppercase().find("WHERE").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L957:C64

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let order_by_idx = sql.to_uppercase().find("ORDER BY").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L967:C58

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let where_idx = sql.to_uppercase().find("WHERE").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L968:C58

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let limit_idx = sql.to_uppercase().find("LIMIT").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1034:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pos = find_keyword(sql, "WHERE").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1053:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pos = find_keyword(sql, "LIMIT").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1064:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pos = find_keyword(sql, "WHERE").unwrap();
```

### `packages\sz-orm-core\src\dialect.rs`

#### 🟠 [HIGH] `unwrap()` @ L2290:C31

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dialect = dialect.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L2574:C31

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dialect = dialect.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L2588:C31

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dialect = dialect.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L2632:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dialect = get_dialect(DbType::MariaDB).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L2644:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dialect = get_dialect(DbType::TiDB).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L2654:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dialect = get_dialect(DbType::Dameng).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L2670:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dialect = get_dialect(DbType::Kingbase).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L2686:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dialect = get_dialect(DbType::PolarDB).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L2695:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dialect = get_dialect(DbType::GaussDB).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L2704:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dialect = get_dialect(DbType::GBase).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L2712:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dialect = get_dialect(DbType::Sybase).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L2722:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dialect = get_dialect(DbType::Db2).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L2808:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dialect = get_dialect(DbType::ClickHouse).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L2995:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(dialect.quote_checked("users").unwrap(), "`users`");
```

#### 🟠 [HIGH] `unwrap()` @ L2996:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(dialect.quote_checked("user_id").unwrap(), "`user_id`");
```

#### 🟠 [HIGH] `unwrap()` @ L3045:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(dialect.quote_checked("users").unwrap(), "\"users\"");
```

#### 🟠 [HIGH] `unwrap()` @ L3052:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(dialect.quote_checked("users").unwrap(), "\"users\"");
```

#### 🟠 [HIGH] `unwrap()` @ L3059:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(dialect.quote_checked("users").unwrap(), "\"users\"");
```

#### 🟠 [HIGH] `unwrap()` @ L3066:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(dialect.quote_checked("users").unwrap(), "[users]");
```

### `packages\sz-orm-core\src\dirty_attributes.rs`

#### 🟠 [HIGH] `unwrap()` @ L577:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dialect = get_dialect(DbType::MySQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L587:C92

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sql = build_dynamic_update(&*dialect, "users", "id", &Value::I64(1), &tracker).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L595:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let set_clause = sql.split("WHERE").next().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L601:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dialect = get_dialect(DbType::MySQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L613:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dialect = get_dialect(DbType::PostgreSQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L621:C92

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sql = build_dynamic_update(&*dialect, "users", "id", &Value::I64(1), &tracker).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L630:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dialect = get_dialect(DbType::MySQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L640:C92

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sql = build_dynamic_update(&*dialect, "users", "id", &Value::I64(1), &tracker).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L649:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dialect = get_dialect(DbType::MySQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L666:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dialect = get_dialect(DbType::MySQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L672:C67

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sql = build_dynamic_insert(&*dialect, "users", &data).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L684:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dialect = get_dialect(DbType::MySQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L695:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dialect = get_dialect(DbType::MySQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L703:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dialect = get_dialect(DbType::PostgreSQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L708:C67

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sql = build_dynamic_insert(&*dialect, "users", &data).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L719:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dialect = get_dialect(DbType::MySQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L725:C66

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sql = build_dynamic_insert(&*dialect, "test", &data).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L728:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let cols_start = sql.find('(').unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L729:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let cols_end = sql.find(") VALUES").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L731:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let vals_start = sql.rfind('(').unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L732:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let vals_end = sql.rfind(')').unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L743:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dialect = get_dialect(DbType::MySQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L748:C67

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sql = build_dynamic_insert(&*dialect, "users", &data).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L757:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dialect = get_dialect(DbType::MySQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L776:C92

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sql = build_dynamic_update(&*dialect, "users", "id", &Value::I64(1), &tracker).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L779:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let set_clause = sql.split("WHERE").next().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L795:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dialect = get_dialect(DbType::MySQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L807:C67

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sql = build_dynamic_insert(&*dialect, "users", &data).unwrap();
```

### `packages\sz-orm-core\src\dynamic_filter.rs`

#### 🟠 [HIGH] `unwrap()` @ L699:C29

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(rendered.unwrap(), "status = 'active'");
```

#### 🟠 [HIGH] `unwrap()` @ L708:C29

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(rendered.unwrap(), "age >= 18");
```

#### 🟠 [HIGH] `unwrap()` @ L723:C22

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
rendered.unwrap(),
```

#### 🟠 [HIGH] `unwrap()` @ L732:C29

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(rendered.unwrap(), "1 = 1");
```

#### 🟠 [HIGH] `unwrap()` @ L751:C66

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let rendered = render_condition("name = :name", &params).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L759:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let eq_pos = rendered.find('=').unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L790:C66

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let rendered = render_condition("name = :name", &params).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L795:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let eq_pos = rendered.find('=').unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L825:C29

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(rendered.unwrap(), "user_id = 100");
```

#### 🟠 [HIGH] `unwrap()` @ L895:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
r.enable("active", params).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L931:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
r.enable("f1", HashMap::new()).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L944:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
r.enable("f1", HashMap::new()).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L947:C25

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
r.disable("f1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L968:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
r.enable("f1", HashMap::new()).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L969:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
r.enable("f2", HashMap::new()).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L995:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
r.enable("active", HashMap::new()).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1015:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
r.enable("active", HashMap::new()).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1016:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
r.enable("adult", HashMap::new()).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1032:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
r.enable("active", HashMap::new()).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1048:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
r.enable("active", HashMap::new()).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1051:C58

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let where_idx = sql.to_uppercase().find("WHERE").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1052:C64

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let group_by_idx = sql.to_uppercase().find("GROUP BY").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1064:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
r.enable("active", HashMap::new()).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1067:C58

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let where_idx = sql.to_uppercase().find("WHERE").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1068:C64

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let order_by_idx = sql.to_uppercase().find("ORDER BY").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1080:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
r.enable("active", HashMap::new()).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1083:C58

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let where_idx = sql.to_uppercase().find("WHERE").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1084:C58

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let limit_idx = sql.to_uppercase().find("LIMIT").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1097:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
r.enable("range", HashMap::new()).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1112:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
r.enable("active", HashMap::new()).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1113:C29

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
r.disable("active").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1169:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
r.enable("active", HashMap::new()).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1174:C29

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
r.disable("active").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1181:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
r.enable("active", params).unwrap();
```

### `packages\sz-orm-core\src\dynamic_sql.rs`

#### 🟠 [HIGH] `unwrap()` @ L977:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parser = DynamicSqlParser::from_xml(xml).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L979:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sql = parser.build("all", &params).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L986:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parser = DynamicSqlParser::from_xml(xml).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L989:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sql = parser.build("by_id", &params).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L996:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parser = DynamicSqlParser::from_xml(xml).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L999:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sql = parser.build("by_table", &params).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1008:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parser = DynamicSqlParser::from_xml(xml).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1011:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sql = parser.build("q", &params).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1018:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parser = DynamicSqlParser::from_xml(xml).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1020:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sql = parser.build("q", &params).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1027:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parser = DynamicSqlParser::from_xml(xml).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1030:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sql = parser.build("q", &params).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1035:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sql = parser.build("q", &params).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1042:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parser = DynamicSqlParser::from_xml(xml).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1046:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sql = parser.build("q", &params).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1051:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sql = parser.build("q", &params).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1058:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parser = DynamicSqlParser::from_xml(xml).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1062:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sql = parser.build("q", &params).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1067:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sql = parser.build("q", &params).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1074:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parser = DynamicSqlParser::from_xml(xml).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1079:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sql = parser.build("q", &params).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1084:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sql = parser.build("q", &params).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1093:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parser = DynamicSqlParser::from_xml(xml).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1096:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sql = parser.build("q", &params).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1104:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parser = DynamicSqlParser::from_xml(xml).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1106:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sql = parser.build("q", &params).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1116:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parser = DynamicSqlParser::from_xml(xml).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1120:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sql = parser.build("q", &params).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1129:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parser = DynamicSqlParser::from_xml(xml).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1134:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sql = parser.build("u", &params).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1142:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parser = DynamicSqlParser::from_xml(xml).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1146:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sql = parser.build("u", &params).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1155:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parser = DynamicSqlParser::from_xml(xml).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1161:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sql = parser.build("q", &params).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1169:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parser = DynamicSqlParser::from_xml(xml).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1171:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sql = parser.build("q", &params).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1178:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parser = DynamicSqlParser::from_xml(xml).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1187:C66

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let (sql, binds) = parser.build_with_binds("q", &params).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1200:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parser = DynamicSqlParser::from_xml(xml).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1203:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sql = parser.build("q", &params).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1214:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parser = DynamicSqlParser::from_xml(xml).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1217:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sql = parser.build("q", &params).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1226:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parser = DynamicSqlParser::from_xml(xml).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1229:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sql = parser.build("q", &params).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1236:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parser = DynamicSqlParser::from_xml(xml).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1238:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sql = parser.build("q", &params).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1247:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parser = DynamicSqlParser::from_xml(xml).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1256:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parser = DynamicSqlParser::from_xml(xml).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1292:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parser = DynamicSqlParser::from_xml(xml).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1307:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parser = DynamicSqlParser::from_xml(xml).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1309:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sql = parser.build("q", &params).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1328:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parser = DynamicSqlParser::from_xml(xml).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1334:C71

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let (sql, binds) = parser.build_with_binds("search", &params).unwrap();
```

### `packages\sz-orm-core\src\entity_graph.rs`

#### 🟠 [HIGH] `unwrap()` @ L951:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(g.edges()[0].sub_graph.as_ref().unwrap().edge_count(), 1);
```

#### 🟠 [HIGH] `unwrap()` @ L1188:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(*call_count.lock().unwrap(), 1);
```

#### 🟠 [HIGH] `unwrap()` @ L1192:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(*call_count.lock().unwrap(), 1); // 未增加
```

#### 🟠 [HIGH] `unwrap()` @ L1196:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(*call_count.lock().unwrap(), 2);
```

#### 🟠 [HIGH] `unwrap()` @ L1213:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(*call_count.lock().unwrap(), 1);
```

#### 🟠 [HIGH] `unwrap()` @ L1218:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(*call_count.lock().unwrap(), 2); // 只为 4, 5 调用一次
```

#### 🟠 [HIGH] `unwrap()` @ L1554:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(*query_count.lock().unwrap(), 3);
```

### `packages\sz-orm-core\src\error.rs`

#### 🟠 [HIGH] `expect()` @ L797:C38

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let source1 = outer.source().expect("outer should have source");
```

#### 🟠 [HIGH] `expect()` @ L802:C41

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let ctx_chain = outer.context().expect("outer should have context");
```

#### 🟠 [HIGH] `expect()` @ L807:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("should have previous context");
```

### `packages\sz-orm-core\src\find_with_related.rs`

#### 🟠 [HIGH] `expect()` @ L82:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("invalid SQL identifier in FindWithRelated::new");
```

#### 🟠 [HIGH] `expect()` @ L282:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("invalid SQL identifier in find_with_related_eager_sql");
```

#### 🟠 [HIGH] `expect()` @ L320:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("invalid SQL identifier in find_with_related_subquery");
```

#### 🟠 [HIGH] `expect()` @ L406:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("invalid SQL identifier in WithRelation::new");
```

#### 🟠 [HIGH] `expect()` @ L426:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("invalid SQL identifier in with_has_many");
```

#### 🟠 [HIGH] `expect()` @ L451:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("invalid SQL identifier in with_has_one");
```

#### 🟠 [HIGH] `expect()` @ L476:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("invalid SQL identifier in with_belongs_to");
```

#### 🟠 [HIGH] `expect()` @ L643:C22

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("invalid id value in related_sql_with_ids");
```

#### 🟠 [HIGH] `expect()` @ L707:C36

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
get_dialect(DbType::MySQL).expect("MySQL dialect")
```

#### 🟠 [HIGH] `expect()` @ L711:C41

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
get_dialect(DbType::PostgreSQL).expect("PG dialect")
```

#### 🟠 [HIGH] `expect()` @ L715:C37

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
get_dialect(DbType::Sqlite).expect("SQLite dialect")
```

#### 🟠 [HIGH] `expect()` @ L825:C54

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let info = inspect_relation(&rels, "orders").expect("relation exists");
```

#### 🟠 [HIGH] `expect()` @ L843:C55

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let info = inspect_relation(&rels, "profile").expect("relation exists");
```

#### 🟠 [HIGH] `expect()` @ L859:C52

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let info = inspect_relation(&rels, "user").expect("relation exists");
```

#### 🟠 [HIGH] `expect()` @ L877:C53

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let info = inspect_relation(&rels, "roles").expect("relation exists");
```

#### 🟠 [HIGH] `expect()` @ L894:C56

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let info = inspect_relation(&rels, "comments").expect("relation exists");
```

#### 🟠 [HIGH] `expect()` @ L910:C59

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let info = inspect_relation(&rels, "commentable").expect("relation exists");
```

#### 🟠 [HIGH] `expect()` @ L957:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("load_eager should succeed for non-duplicate relations");
```

#### 🟠 [HIGH] `unwrap()` @ L964:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
loader.related_sql("orders").unwrap(),
```

#### 🟠 [HIGH] `unwrap()` @ L971:C18

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap(),
```

#### 🟠 [HIGH] `unwrap()` @ L977:C18

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap(),
```

#### 🟠 [HIGH] `expect()` @ L988:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("load_join should succeed for non-duplicate relations");
```

#### 🟠 [HIGH] `expect()` @ L1000:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("load_join should succeed for non-duplicate relations");
```

#### 🟠 [HIGH] `expect()` @ L1015:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("load_eager should succeed for non-duplicate relations");
```

#### 🟠 [HIGH] `expect()` @ L1031:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("load_eager should succeed for non-duplicate relations");
```

#### 🟠 [HIGH] `unwrap()` @ L1035:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L1048:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("load_eager should succeed for non-duplicate relations");
```

#### 🟠 [HIGH] `unwrap()` @ L1055:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
loader.related_sql("orders").unwrap(),
```

#### 🟠 [HIGH] `unwrap()` @ L1060:C62

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
loader.related_sql_with_ids("orders", [100_i64]).unwrap(),
```

#### 🟠 [HIGH] `expect()` @ L1071:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("load_join should succeed for non-duplicate relations");
```

#### 🟠 [HIGH] `expect()` @ L1084:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("load_eager should succeed for non-duplicate relations");
```

#### 🟠 [HIGH] `expect()` @ L1095:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("load_eager should succeed for non-duplicate relations");
```

#### 🟠 [HIGH] `expect()` @ L1106:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("load_eager should succeed for non-duplicate relations");
```

#### 🟠 [HIGH] `expect()` @ L1117:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("load_eager should succeed for non-duplicate relations");
```

#### 🟠 [HIGH] `expect()` @ L1128:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("load_eager should succeed for non-duplicate relations");
```

### `packages\sz-orm-core\src\hooks.rs`

#### 🟠 [HIGH] `unwrap()` @ L731:C58

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
registry.dispatch(HookEvent::BeforeInsert, &ctx).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L732:C58

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
registry.dispatch(HookEvent::BeforeInsert, &ctx).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L890:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
registry.dispatch(event, &ctx).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1008:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let _guard = HOOK_TEST_LOCK.lock().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1014:C23

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(id.unwrap(), 42);
```

#### 🟠 [HIGH] `unwrap()` @ L1096:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let _guard = HOOK_TEST_LOCK.lock().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1131:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let _guard = HOOK_TEST_LOCK.lock().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1258:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
registry.dispatch(event, &ctx).unwrap();
```

### `packages\sz-orm-core\src\hydration_plugin.rs`

#### 🟠 [HIGH] `expect()` @ L157:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("sorted_columns is non-empty after is_empty check");
```

#### 🟠 [HIGH] `expect()` @ L177:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("sorted_columns is non-empty after is_empty check");
```

#### 🟠 [HIGH] `expect()` @ L219:C18

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("sorted_columns is non-empty after is_empty check")
```

#### 🟠 [HIGH] `expect()` @ L727:C18

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("i < bytes.len() guarantees non-empty slice");
```

#### 🟠 [HIGH] `unwrap()` @ L986:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = hydrate_object(&[row]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1010:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = hydrate_object(&rows).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1016:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = hydrate_object(&[]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1028:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = hydrate_array(&[row]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1038:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = hydrate_array(&[]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1049:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = hydrate_scalar(&[row]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1069:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = hydrate_scalar(&rows).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1090:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = hydrate_single_scalar(&[row]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1157:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = hydrate_column(&rows, "name").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1184:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = hydrate_column(&[], "name").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1195:C61

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = hydrate(&[row], HydrationMode::Scalar).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1204:C67

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = hydrate(&[row], HydrationMode::SingleScalar).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1223:C60

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = hydrate(&rows, HydrationMode::Column).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1300:C32

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(ctx.elapsed.unwrap().as_millis(), 50);
```

#### 🟠 [HIGH] `unwrap()` @ L1307:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(ctx.affected_rows.unwrap(), 10);
```

#### 🟠 [HIGH] `unwrap()` @ L1716:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = hydrate_single_scalar(&[row]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1740:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = hydrate_object(&rows).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1758:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = hydrate_array(&rows).unwrap();
```

### `packages\sz-orm-core\src\json_query.rs`

#### 🟠 [HIGH] `expect()` @ L792:C50

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let dialect = get_dialect(DbType::MySQL).expect("MySQL");
```

#### 🟠 [HIGH] `expect()` @ L810:C51

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let _dialect = get_dialect(DbType::MySQL).expect("MySQL");
```

### `packages\sz-orm-core\src\l2_cache.rs`

#### 🟠 [HIGH] `unwrap()` @ L2175:C22

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
h.join().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L2193:C22

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
h.join().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L2272:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
writer.write(b"k1", b"v1", None).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L2273:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
writer.write(b"k2", b"v2", None).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L2274:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
writer.write(b"k3", b"v3", None).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L2277:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let v1 = backend.get("k1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L2284:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
writer.flush().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L2297:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
writer.write(b"k1", b"v1", None).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L2298:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(backend.get("k1").await.unwrap().is_some());
```

#### 🟠 [HIGH] `unwrap()` @ L2299:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
writer.delete(b"k1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L2301:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(backend.get("k1").await.unwrap().is_none());
```

#### 🟠 [HIGH] `unwrap()` @ L2304:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
writer.flush().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L2317:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
writer.write(b"k1", b"v1", None).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L2331:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
writer.flush().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L2349:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
writer.write(b"k1", b"v1", None).await.unwrap();
```

### `packages\sz-orm-core\src\lambda.rs`

#### 🟠 [HIGH] `unwrap()` @ L1397:C76

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pg_dialect: Box<dyn Dialect> = get_dialect(DbType::PostgreSQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1408:C68

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dialect: Box<dyn Dialect> = get_dialect(DbType::MySQL).unwrap();
```

### `packages\sz-orm-core\src\migration.rs`

#### 🟠 [HIGH] `unwrap()` @ L1058:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sql = fk.build(DbType::MySQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1067:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sql = fk.build(DbType::MySQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1106:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sql = schema.build(DbType::MySQL).unwrap();
```

### `packages\sz-orm-core\src\model.rs`

#### 🟠 [HIGH] `unwrap()` @ L1263:C62

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let user = user.with("orders").load(&mut conn).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1287:C63

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let user = user.with("profile").load(&mut conn).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1309:C60

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let user = user.with("team").load(&mut conn).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1334:C61

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let user = user.with("roles").load(&mut conn).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1364:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1440:C69

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let user = make_user().with("orders").load(&mut conn).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1441:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let orders = user.get_has_many("orders").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1444:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
orders[0].get("total").unwrap(),
```

#### 🟠 [HIGH] `unwrap()` @ L1462:C70

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let user = make_user().with("profile").load(&mut conn).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1463:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let profile = user.get_has_one("profile").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1466:C21

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
profile.unwrap().get("bio").unwrap(),
```

#### 🟠 [HIGH] `unwrap()` @ L1466:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
profile.unwrap().get("bio").unwrap(),
```

#### 🟠 [HIGH] `unwrap()` @ L1479:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(m.get("key").unwrap(), &Value::String("value".to_string()));
```

#### 🟠 [HIGH] `unwrap()` @ L1721:C64

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let user = post.with("comments").load(&mut conn).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1746:C73

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let comment = comment.with("commentable").load(&mut conn).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1763:C73

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let comment = comment.with("commentable").load(&mut conn).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1764:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let data = comment.get_relation("commentable").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1785:C71

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let user = make_user().with("comments").load(&mut conn).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1786:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let comments = user.get_morph_many("comments").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1789:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
comments[0].get("body").unwrap(),
```

#### 🟠 [HIGH] `unwrap()` @ L1808:C73

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let comment = comment.with("commentable").load(&mut conn).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1809:C58

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parent = comment.get_morph_to("commentable").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1812:C20

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
parent.unwrap().get("name").unwrap(),
```

#### 🟠 [HIGH] `unwrap()` @ L1812:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
parent.unwrap().get("name").unwrap(),
```

### `packages\sz-orm-core\src\observer.rs`

#### 🟠 [HIGH] `unwrap()` @ L858:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(*insert.lock().unwrap(), 2);
```

#### 🟠 [HIGH] `unwrap()` @ L859:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(*update.lock().unwrap(), 1);
```

#### 🟠 [HIGH] `unwrap()` @ L860:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(*delete.lock().unwrap(), 1);
```

#### 🟠 [HIGH] `unwrap()` @ L886:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L943:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(*called.lock().unwrap(), 2);
```

#### 🟠 [HIGH] `unwrap()` @ L1035:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(*called.lock().unwrap(), 1);
```

#### 🟠 [HIGH] `unwrap()` @ L1059:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
d.dispatch_after_find(&ctx, &mut attrs).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1139:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(*sub1_called.lock().unwrap(), 1);
```

#### 🟠 [HIGH] `unwrap()` @ L1140:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(*sub2_called.lock().unwrap(), 1);
```

#### 🟠 [HIGH] `unwrap()` @ L1141:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(*obs_called.lock().unwrap(), 1);
```

#### 🟠 [HIGH] `unwrap()` @ L1278:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut n = self.0.lock().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1358:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(*second_called.lock().unwrap(), 0);
```

### `packages\sz-orm-core\src\optimistic_lock.rs`

#### 🟠 [HIGH] `unwrap()` @ L412:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dialect = get_dialect(DbType::MySQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L437:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dialect = get_dialect(DbType::PostgreSQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L461:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dialect = get_dialect(DbType::MySQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L481:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dialect = get_dialect(DbType::MySQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L504:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dialect = get_dialect(DbType::MySQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L519:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dialect = get_dialect(DbType::PostgreSQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L568:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let v = extract_version(&row, "version").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L576:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let v = extract_version(&row, "version").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L584:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let v = extract_version(&row, "version").unwrap();
```

### `packages\sz-orm-core\src\phinx_migration.rs`

#### 🟠 [HIGH] `expect()` @ L385:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("invalid table name");
```

#### 🟠 [HIGH] `expect()` @ L389:C22

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("invalid primary key column name");
```

#### 🟠 [HIGH] `expect()` @ L423:C18

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("invalid foreign key column name");
```

#### 🟠 [HIGH] `expect()` @ L428:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("invalid foreign key referenced table name");
```

#### 🟠 [HIGH] `expect()` @ L433:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("invalid foreign key referenced column name");
```

#### 🟠 [HIGH] `expect()` @ L435:C66

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
crate::sql_safety::validate_fk_action(on_delete).expect("invalid ON DELETE action");
```

#### 🟠 [HIGH] `expect()` @ L438:C66

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
crate::sql_safety::validate_fk_action(on_update).expect("invalid ON UPDATE action");
```

### `packages\sz-orm-core\src\queryable.rs`

#### 🟠 [HIGH] `unwrap()` @ L173:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
Ok(values.into_iter().next().unwrap())
```

#### 🟠 [HIGH] `unwrap()` @ L187:C25

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
Ok((iter.next().unwrap(), iter.next().unwrap()))
```

#### 🟠 [HIGH] `unwrap()` @ L187:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
Ok((iter.next().unwrap(), iter.next().unwrap()))
```

#### 🟠 [HIGH] `unwrap()` @ L202:C25

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
iter.next().unwrap(),
```

#### 🟠 [HIGH] `unwrap()` @ L203:C25

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
iter.next().unwrap(),
```

#### 🟠 [HIGH] `unwrap()` @ L204:C25

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
iter.next().unwrap(),
```

#### 🟠 [HIGH] `unwrap()` @ L354:C87

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
UserRow::from_values(vec![Value::I64(42), Value::String("Alice".into())]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L385:C18

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L408:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let row = UserRow::from_row(map).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L431:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let row = UserRow::from_row(map).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L439:C58

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let v = Value::from_values(vec![Value::I64(42)]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L456:C18

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L468:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L505:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let row = UserRow::from_values(values).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L524:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let row = UserRow::from_row(map).unwrap();
```

### `packages\sz-orm-core\src\quick_query.rs`

#### 🟠 [HIGH] `expect()` @ L379:C36

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
get_dialect(DbType::MySQL).expect("MySQL dialect")
```

#### 🟠 [HIGH] `expect()` @ L383:C41

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
get_dialect(DbType::PostgreSQL).expect("PG dialect")
```

### `packages\sz-orm-core\src\rate_limiter.rs`

#### 🟠 [HIGH] `unwrap()` @ L180:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r1 = limiter.acquire("k").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L182:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r2 = limiter.acquire("k").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L184:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r3 = limiter.acquire("k").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L191:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(limiter.acquire("k").unwrap().allowed);
```

#### 🟠 [HIGH] `unwrap()` @ L192:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!limiter.acquire("k").unwrap().allowed);
```

#### 🟠 [HIGH] `unwrap()` @ L193:C28

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
limiter.reset("k").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L194:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(limiter.acquire("k").unwrap().allowed);
```

#### 🟠 [HIGH] `unwrap()` @ L201:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r1 = limiter.try_acquire("k").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L203:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r2 = limiter.try_acquire("k").unwrap();
```

### `packages\sz-orm-core\src\repository.rs`

#### 🟠 [HIGH] `unwrap()` @ L1178:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let saved = repo.save(user.clone()).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1181:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let found = repo.find_by_id(&Value::I64(1)).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1188:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let found = repo.find_by_id(&Value::I64(999)).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1200:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let saved = repo.save_many(users.clone()).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1252:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = repo.batch_update(updates).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1258:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let alice = repo.find_by_id(&Value::I64(1)).unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1258:C62

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let alice = repo.find_by_id(&Value::I64(1)).unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1261:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let bob = repo.find_by_id(&Value::I64(2)).unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1261:C60

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let bob = repo.find_by_id(&Value::I64(2)).unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1276:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = repo.batch_update(updates).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1282:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let alice = repo.find_by_id(&Value::I64(1)).unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1282:C62

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let alice = repo.find_by_id(&Value::I64(1)).unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1285:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(repo.find_by_id(&Value::I64(999)).unwrap().is_none());
```

#### 🟠 [HIGH] `unwrap()` @ L1297:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = repo.batch_update(updates).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1306:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = repo.batch_update(vec![]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1326:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let saved = repo1.save_many(updates.clone()).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1331:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = repo2.batch_update(updates).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1341:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1345:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1348:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let found = repo.find_by_id(&Value::I64(1)).unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1348:C62

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let found = repo.find_by_id(&Value::I64(1)).unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1359:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let all = repo.find_all().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1366:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let all = repo.find_all().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1376:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let deleted = repo.delete(&Value::I64(1)).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1384:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let deleted = repo.delete(&Value::I64(999)).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1396:C33

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(repo.count().unwrap(), 3);
```

#### 🟠 [HIGH] `unwrap()` @ L1402:C33

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(repo.count().unwrap(), 0);
```

#### 🟠 [HIGH] `unwrap()` @ L1408:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(repo.exists(&Value::I64(1)).unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L1409:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!repo.exists(&Value::I64(999)).unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L1424:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1438:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1456:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1476:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1486:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1514:C61

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = repo.find_by_with_or_filter(&and, &or).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1545:C61

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = repo.find_by_with_or_filter(&and, &or).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1566:C61

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = repo.find_by_with_or_filter(&and, &or).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1586:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1604:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1624:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1641:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1659:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1661:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(result.unwrap().id, 2);
```

#### 🟠 [HIGH] `unwrap()` @ L1674:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1688:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1702:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1716:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let page = repo.paginate(1, 10).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1733:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let page = repo.paginate(3, 10).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1746:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let page = repo.paginate(10, 10).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1765:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1779:C33

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
repo.save(user.clone()).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1782:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let found = repo.find_by_id(&1).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1793:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let deleted = repo.delete(&1).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1797:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let remaining = repo.find_all().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1811:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1822:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let page = repo.paginate(2, 10).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1834:C33

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(repo.count().unwrap(), 2);
```

#### 🟠 [HIGH] `unwrap()` @ L1841:C33

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(repo.exists(&1).unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L1842:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!repo.exists(&999).unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L1849:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let re = simple_regex::compile("^abc$").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1856:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let re = simple_regex::compile("^Ali.*$").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1864:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let re = simple_regex::compile("ab").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1882:C31

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
repo.save_many(users).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1883:C33

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(repo.count().unwrap(), 5);
```

#### 🟠 [HIGH] `unwrap()` @ L1888:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1892:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let page1 = repo.paginate(1, 2).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1896:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let page2 = repo.paginate(2, 2).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1899:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let page3 = repo.paginate(3, 2).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1909:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1915:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1916:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let updated = repo.find_by_id(&Value::I64(1)).unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1916:C64

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let updated = repo.find_by_id(&Value::I64(1)).unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1921:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let deleted = repo.delete(&Value::I64(2)).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1923:C33

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(repo.count().unwrap(), 4);
```

#### 🟠 [HIGH] `unwrap()` @ L1924:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!repo.exists(&Value::I64(2)).unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L1929:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1931:C33

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(repo.count().unwrap(), 1); // 仅剩 Dave(28)
```

#### 🟠 [HIGH] `unwrap()` @ L1944:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let page = repo.paginate(current_page, 10).unwrap();
```

### `packages\sz-orm-core\src\result_map.rs`

#### 🟠 [HIGH] `unwrap()` @ L1069:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(rm.discriminator.as_ref().unwrap().column, "user_type");
```

#### 🟠 [HIGH] `unwrap()` @ L1131:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let rm = registry.get("userMap").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1207:C66

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let attrs = apply_result_map(&registry, "userMap", &row).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1221:C66

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let attrs = apply_result_map(&registry, "userMap", &row).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1263:C66

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let attrs = apply_result_map(&registry, "userMap", &row).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1301:C66

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let attrs = apply_result_map(&registry, "userMap", &row).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1330:C66

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let attrs = apply_result_map(&registry, "userMap", &row).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1386:C66

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let attrs = apply_result_map(&registry, "baseMap", &row).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1396:C68

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let attrs2 = apply_result_map(&registry, "baseMap", &row2).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1420:C66

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let attrs = apply_result_map(&registry, "baseMap", &row).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1463:C73

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = apply_result_map_many(&registry, "userMap", &rows).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1510:C73

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = apply_result_map_many(&registry, "userMap", &rows).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1522:C71

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = apply_result_map_many(&registry, "userMap", &[]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1753:C73

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = apply_result_map_many(&registry, "userMap", &rows).unwrap();
```

### `packages\sz-orm-core\src\retry.rs`

#### 🟠 [HIGH] `unwrap()` @ L142:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(result.unwrap(), 42);
```

#### 🟠 [HIGH] `unwrap()` @ L169:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(result.unwrap(), 42);
```

### `packages\sz-orm-core\src\schema_gen.rs`

#### 🟠 [HIGH] `unwrap()` @ L419:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let users_end = output.find("}").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L420:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let orders_start = output.find("table orders").unwrap();
```

### `packages\sz-orm-core\src\type_handler.rs`

#### 🟠 [HIGH] `unwrap()` @ L755:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L769:C69

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parsed: String = registry.handle("created_at", &stored).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L780:C69

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parsed: String = registry.handle("created_at", &stored).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L790:C74

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parsed: String = registry.handle("created_at", &Value::Null).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L816:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let value = registry.to_value("id", &uuid).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L827:C61

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parsed: String = registry.handle("id", &stored).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L838:C61

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parsed: String = registry.handle("id", &stored).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L851:C58

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let value = registry.to_value("settings", &json).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L862:C67

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parsed: String = registry.handle("settings", &stored).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L872:C72

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parsed: String = registry.handle("settings", &Value::Null).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L884:C59

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let value = registry.to_value("price", &12345i64).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L895:C61

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parsed: i64 = registry.handle("price", &stored).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L906:C61

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parsed: i64 = registry.handle("price", &stored).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L917:C61

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parsed: i64 = registry.handle("price", &stored).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L927:C66

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parsed: i64 = registry.handle("price", &Value::Null).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L939:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let value = registry.to_value("active", &true).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L949:C74

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parsed: bool = registry.handle("active", &Value::Bool(true)).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L952:C75

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parsed: bool = registry.handle("active", &Value::Bool(false)).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L962:C70

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parsed: bool = registry.handle("active", &Value::I64(1)).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L965:C70

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parsed: bool = registry.handle("active", &Value::I64(0)).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L979:C18

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L987:C18

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1015:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let value = registry.to_value("tags", &input).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1031:C67

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parsed: Vec<Value> = registry.handle("tags", &stored).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1043:C72

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parsed: Vec<Value> = registry.handle("tags", &Value::Null).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1054:C75

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parsed: Vec<Value> = registry.handle("tags", &Value::I64(42)).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1066:C58

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let value = registry.to_value("tags", &original).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1067:C66

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parsed: Vec<Value> = registry.handle("tags", &value).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1208:C63

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let value = registry.to_value("price", &Money(12345)).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1218:C71

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parsed: Money = registry.handle("price", &Value::I64(99)).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1230:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1288:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1291:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1308:C75

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let id_value = registry.to_value("id", &String::from("user-123")).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1311:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1312:C63

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let active_value = registry.to_value("active", &true).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1319:C59

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let id: String = registry.handle("id", &id_value).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1320:C67

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let ts: String = registry.handle("created_at", &ts_value).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1321:C69

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let active: bool = registry.handle("active", &active_value).unwrap();
```

### `packages\sz-orm-core\tests\chaos.rs`

#### 🟠 [HIGH] `unwrap()` @ L303:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let conns = self.conns.lock().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L314:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
self.conns.lock().unwrap().push(flag.clone());
```

#### 🟠 [HIGH] `unwrap()` @ L349:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L350:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool = Pool::new(config, factory).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L366:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conns.push(pool.acquire().await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L380:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let conn = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L406:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.execute("INSERT 1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L460:C17

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
h.await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L489:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.execute("INSERT 1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L504:C25

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.rollback().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L517:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.execute("INSERT").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L541:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let conn1 = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L543:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.execute("INSERT 1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L550:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn2 = conn2.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L582:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L583:C77

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool: &'static Pool = Box::leak(Box::new(Pool::new(config, factory).unwrap()));
```

#### 🟠 [HIGH] `unwrap()` @ L586:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let conn = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L593:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let conn = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L617:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L618:C77

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool: &'static Pool = Box::leak(Box::new(Pool::new(config, factory).unwrap()));
```

#### 🟠 [HIGH] `unwrap()` @ L623:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conns.push(pool.acquire().await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L656:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let conn1 = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L664:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let conn2 = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L677:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let conn3 = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L707:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let conn = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L725:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
result.is_err() || result.unwrap().is_err(),
```

#### 🟠 [HIGH] `unwrap()` @ L760:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let conn1 = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L761:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let conn2 = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L762:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let conn3 = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L803:C17

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
h.await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L834:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L835:C85

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool: &'static Pool = Box::leak(Box::new(Pool::new(config, factory.clone()).unwrap()));
```

#### 🟠 [HIGH] `unwrap()` @ L865:C17

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
h.await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L926:C17

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
h.await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L955:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.execute("INSERT").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L979:C25

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.rollback().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L991:C32

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.execute("INSERT").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L992:C25

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.rollback().await.unwrap();
```

### `packages\sz-orm-core\tests\chaos_pool.rs`

#### 🟠 [HIGH] `unwrap()` @ L173:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L174:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool = Pool::new(config, factory.clone()).unwrap();
```

#### 🟠 [HIGH] `expect()` @ L189:C38

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let conn1 = pool.acquire().await.expect("第 1 个连接应成功");
```

#### 🟠 [HIGH] `expect()` @ L190:C38

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let conn2 = pool.acquire().await.expect("第 2 个连接应成功");
```

#### 🟠 [HIGH] `unwrap()` @ L222:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let conn1 = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L223:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let conn2 = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L233:C38

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let conn3 = pool.acquire().await.expect("释放后应能获取连接");
```

#### 🟠 [HIGH] `unwrap()` @ L260:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool = Pool::new(config, factory).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L294:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let conn = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L299:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.execute("INSERT INTO test VALUES (1)").await.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L309:C38

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let conn2 = pool.acquire().await.expect("池应能提供连接");
```

#### 🟠 [HIGH] `unwrap()` @ L332:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let conn1 = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L333:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let conn2 = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L364:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let conn1 = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L365:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let conn2 = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L408:C51

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let conn = pool_c.acquire().await.expect("并发 acquire 应成功");
```

#### 🟠 [HIGH] `unwrap()` @ L420:C17

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
h.await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L460:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L461:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool = Pool::new(config, factory).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L487:C17

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
h.await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L527:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool = Pool::new(config, factory).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L530:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let conn1 = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L531:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let conn2 = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L532:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let conn3 = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L570:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool = Pool::new(config, factory).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L573:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let conn = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L579:C37

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let conn = pool.acquire().await.expect("回收后应能创建新连接");
```

#### 🟠 [HIGH] `unwrap()` @ L682:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool = Pool::new(config, factory).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L685:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let conn = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L715:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool = Pool::new(config, factory).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L717:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let conn = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L746:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool = Pool::new(config, factory).unwrap();
```

#### 🟠 [HIGH] `expect()` @ L763:C37

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let conn = pool.acquire().await.expect("第 4 次 acquire 应成功");
```

### `packages\sz-orm-core\tests\common\soak.rs`

#### 🟠 [HIGH] `unwrap()` @ L173:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let window = self.latency_window.lock().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L242:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let last = self.snapshots.last().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L457:C31

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut w = window.lock().unwrap();
```

### `packages\sz-orm-core\tests\contracts\cache_contract.rs`

#### 🟠 [HIGH] `unwrap()` @ L22:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = cache.get("missing_key").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L29:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.set("k1", b"v1".to_vec(), None).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L30:C29

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let v = cache.get("k1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L37:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.set("k1", b"v1".to_vec(), None).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L38:C29

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(cache.get("k1").unwrap().is_some());
```

#### 🟠 [HIGH] `unwrap()` @ L40:C24

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.delete("k1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L41:C29

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(cache.get("k1").unwrap().is_none());
```

#### 🟠 [HIGH] `unwrap()` @ L47:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.set("k1", b"v1".to_vec(), None).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L48:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.set("k2", b"v2".to_vec(), None).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L50:C19

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.clear().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L52:C29

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(cache.get("k1").unwrap().is_none());
```

#### 🟠 [HIGH] `unwrap()` @ L53:C29

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(cache.get("k2").unwrap().is_none());
```

#### 🟠 [HIGH] `unwrap()` @ L59:C33

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!cache.exists("k1").unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L61:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.set("k1", b"v1".to_vec(), None).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L62:C32

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(cache.exists("k1").unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L64:C24

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.delete("k1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L65:C33

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!cache.exists("k1").unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L75:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L76:C29

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(cache.get("k1").unwrap().is_some());
```

#### 🟠 [HIGH] `unwrap()` @ L82:C25

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.get("k1").unwrap().is_none(),
```

#### 🟠 [HIGH] `unwrap()` @ L90:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.set("k1", b"v1".to_vec(), None).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L93:C29

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(cache.get("k1").unwrap().is_some());
```

#### 🟠 [HIGH] `unwrap()` @ L99:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.set("k1", b"v1".to_vec(), None).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L101:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.expire("k1", Duration::from_millis(50)).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L104:C29

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(cache.get("k1").unwrap().is_none());
```

#### 🟠 [HIGH] `unwrap()` @ L119:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L120:C31

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let ttl = cache.ttl("k1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L123:C17

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(ttl.unwrap() <= Duration::from_secs(10));
```

#### 🟠 [HIGH] `unwrap()` @ L140:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!cache.exists("any_key").unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L141:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(cache.get("any_key").unwrap().is_none());
```

#### 🟠 [HIGH] `unwrap()` @ L148:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!cache.exists("any_key").unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L149:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(cache.get("any_key").unwrap().is_none());
```

#### 🟠 [HIGH] `unwrap()` @ L156:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.set("k1", b"v1".to_vec(), None).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L158:C31

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let ttl = cache.ttl("k1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L168:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(cache.get("any_key").unwrap().is_none());
```

#### 🟠 [HIGH] `unwrap()` @ L175:C29

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(cache.get("k1").unwrap().is_none());
```

### `packages\sz-orm-core\tests\contracts\dialect_contract.rs`

#### 🟠 [HIGH] `unwrap()` @ L11:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let d = get_dialect(DbType::MySQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L18:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let d = get_dialect(DbType::PostgreSQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L24:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let d = get_dialect(DbType::Sqlite).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L30:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let d = get_dialect(DbType::Oracle).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L39:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mysql_d = get_dialect(DbType::MySQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L54:C33

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let d = get_dialect(db).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L70:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
get_dialect(DbType::MySQL).unwrap().auto_increment_keyword(),
```

#### 🟠 [HIGH] `unwrap()` @ L75:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `unwrap()` @ L79:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `unwrap()` @ L88:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let d = get_dialect(DbType::MySQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L101:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let d = get_dialect(DbType::Sqlite).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L112:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let d = get_dialect(DbType::Oracle).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L157:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let d = get_dialect(DbType::ClickHouse).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L182:C24

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let d = result.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L190:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let d = get_dialect(DbType::OceanBase).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L198:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(get_dialect(DbType::MySQL).unwrap().db_type(), DbType::MySQL);
```

#### 🟠 [HIGH] `unwrap()` @ L200:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
get_dialect(DbType::PostgreSQL).unwrap().db_type(),
```

#### 🟠 [HIGH] `unwrap()` @ L204:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
get_dialect(DbType::Sqlite).unwrap().db_type(),
```

#### 🟠 [HIGH] `unwrap()` @ L208:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
get_dialect(DbType::Oracle).unwrap().db_type(),
```

### `packages\sz-orm-core\tests\contracts\dynamic_sql_contract.rs`

#### 🟠 [HIGH] `unwrap()` @ L171:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parser = DynamicSqlParser::from_xml(xml).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L174:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sql = parser.build("find_users", &params).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L198:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parser = DynamicSqlParser::from_xml(xml).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L217:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parser = DynamicSqlParser::from_xml(xml).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L221:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sql = parser.build("find", &params).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L236:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parser = DynamicSqlParser::from_xml(xml).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L240:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sql = parser.build("find", &params).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L261:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parser = DynamicSqlParser::from_xml(xml).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L265:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sql = parser.build("find", &params).unwrap();
```

### `packages\sz-orm-core\tests\contracts\error_contract.rs`

#### 🟠 [HIGH] `unwrap()` @ L195:C23

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.commit().await.unwrap();
```

### `packages\sz-orm-core\tests\contracts\hooks_contract.rs`

#### 🟠 [HIGH] `unwrap()` @ L154:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
registry.dispatch(HookEvent::BeforeInsert, &ctx).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L158:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
registry.dispatch(HookEvent::BeforeUpdate, &ctx).unwrap();
```

### `packages\sz-orm-core\tests\contracts\migration_contract.rs`

#### 🟠 [HIGH] `unwrap()` @ L20:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sql = builder.build(DbType::MySQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L35:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sql = builder.build(DbType::MySQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L46:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mysql_sql = make().build(DbType::MySQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L47:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pg_sql = make().build(DbType::PostgreSQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L78:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L87:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L97:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

### `packages\sz-orm-core\tests\contracts\pool_contract.rs`

#### 🟠 [HIGH] `unwrap()` @ L28:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L29:C32

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
Pool::new(config, factory).unwrap()
```

#### 🟠 [HIGH] `unwrap()` @ L62:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let conn = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L89:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let _held = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L114:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pooled: PooledConnection = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L127:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pooled = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L186:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let conn = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L203:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let conn = pool.acquire().await.unwrap();
```

### `packages\sz-orm-core\tests\contracts\query_contract.rs`

#### 🟠 [HIGH] `unwrap()` @ L27:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let d = get_dialect(DbType::MySQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L56:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let d = get_dialect(DbType::MySQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L68:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let d = get_dialect(DbType::MySQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L83:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let d = get_dialect(DbType::MySQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L96:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let d = get_dialect(DbType::MySQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L112:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let d = get_dialect(DbType::MySQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L128:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let d = get_dialect(DbType::MySQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L141:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let d = get_dialect(DbType::MySQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L156:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let d = get_dialect(DbType::MySQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L170:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let d = get_dialect(DbType::MySQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L182:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let d = get_dialect(DbType::MySQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L195:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let d = get_dialect(DbType::MySQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L209:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let d = get_dialect(DbType::MySQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L226:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let d = get_dialect(DbType::MySQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L243:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let d = get_dialect(DbType::MySQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L258:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let d = get_dialect(DbType::MySQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L266:C75

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let builder = || QueryBuilder::<User>::new(get_dialect(DbType::MySQL).unwrap()).table("users");
```

#### 🟠 [HIGH] `unwrap()` @ L278:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let d = get_dialect(DbType::MySQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L288:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let d = get_dialect(DbType::MySQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L297:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let d = get_dialect(DbType::MySQL).unwrap();
```

### `packages\sz-orm-core\tests\contracts\queryable_contract.rs`

#### 🟠 [HIGH] `unwrap()` @ L13:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let v = Value::from_values(vec![Value::I64(42)]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L48:C87

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
<(Value, Value)>::from_values(vec![Value::I64(1), Value::String("x".into())]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L72:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L154:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let user = UserRow::from_row(row).unwrap();
```

### `packages\sz-orm-core\tests\e2e_batch_upsert.rs`

#### 🟠 [HIGH] `unwrap()` @ L70:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dialect = get_dialect(db_type).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L103:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let values_pos = sql.to_uppercase().find("VALUES").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L138:C32

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let (sql, params) = result.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L163:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L183:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L204:C32

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let (sql, params) = result.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L233:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L251:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L274:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L302:C32

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let (sql, params) = result.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L336:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L409:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let (sql, _) = result.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L441:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L464:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L492:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

### `packages\sz-orm-core\tests\e2e_eager_cycle.rs`

#### 🟠 [HIGH] `unwrap()` @ L351:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dialect = get_dialect(DbType::MySQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L374:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dialect = get_dialect(DbType::MySQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L397:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dialect = get_dialect(DbType::MySQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L405:C25

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let loaded = loaded.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L413:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dialect = get_dialect(DbType::MySQL).unwrap();
```

### `packages\sz-orm-core\tests\e2e_keyset.rs`

#### 🟠 [HIGH] `unwrap()` @ L63:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dialect = get_dialect(DbType::MySQL).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L235:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dialect = get_dialect(db_type).unwrap();
```

### `packages\sz-orm-core\tests\e2e_migration.rs`

#### 🟠 [HIGH] `unwrap()` @ L67:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let applied = migrator.migrate().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L81:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
migrator.migrate().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L97:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
migrator.migrate().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L98:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
migrator.rollback("003").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L114:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
migrator.migrate().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L115:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let rolled = migrator.down(Some("001")).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L134:C31

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
migrator1.migrate().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L146:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let re_applied = migrator2.migrate().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L165:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
migrator.up(Some("002")).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L287:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let first = migrator.migrate().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L290:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let second = migrator.migrate().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L304:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
migrator.migrate().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L320:C28

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
migrator.reset().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L363:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
migrator.migrate().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L381:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let applied = migrator.migrate().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L400:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
migrator.migrate().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L417:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
migrator.migrate().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L450:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
migrator.migrate().await.unwrap();
```

### `packages\sz-orm-core\tests\e2e_transaction.rs`

#### 🟠 [HIGH] `unwrap()` @ L59:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.begin_transaction().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L64:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L73:C25

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.commit().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L82:C17

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
bob.unwrap().get("name"),
```

#### 🟠 [HIGH] `unwrap()` @ L96:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.begin_transaction().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L101:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L110:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.rollback().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L130:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.begin_transaction().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L135:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L139:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L143:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L145:C25

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.commit().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L152:C15

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
alice.unwrap().get("age"),
```

#### 🟠 [HIGH] `unwrap()` @ L167:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.begin_transaction().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L171:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L174:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L177:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L179:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.rollback().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L191:C15

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
alice.unwrap().get("age"),
```

#### 🟠 [HIGH] `unwrap()` @ L204:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.begin_transaction().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L209:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L213:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sp = tx.savepoint().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L218:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L227:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.rollback_to_savepoint(&sp).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L228:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.release_savepoint(&sp).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L240:C23

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.commit().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L258:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.begin_transaction().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L264:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L267:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sp1 = tx.savepoint().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L272:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L275:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sp2 = tx.savepoint().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L280:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L288:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.rollback_to_savepoint(&sp2).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L289:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.release_savepoint(&sp2).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L300:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.rollback_to_savepoint(&sp1).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L301:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.release_savepoint(&sp1).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L311:C23

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.commit().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L323:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.begin_transaction().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L326:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sp = tx.savepoint().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L329:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L330:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.release_savepoint(&sp).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L333:C25

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.rollback().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L350:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.begin_transaction().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L358:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L390:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.begin_transaction().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L397:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sp = tx.savepoint().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L423:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.begin_transaction().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L428:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L521:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.begin_transaction().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L525:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L538:C25

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.rollback().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L655:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.begin_transaction().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L661:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L665:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L669:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L679:C25

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.rollback().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L686:C15

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
order.unwrap().get("amount"),
```

#### 🟠 [HIGH] `unwrap()` @ L699:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.begin_transaction().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L705:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L708:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L714:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let alice = alice.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L723:C25

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.rollback().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L728:C23

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let alice = alice.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L744:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.begin_transaction().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L748:C71

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let affected = tx.execute("DELETE FROM users WHERE id = 1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L758:C25

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.rollback().await.unwrap();
```

### `packages\sz-orm-core\tests\formal.rs`

#### 🟠 [HIGH] `unwrap()` @ L154:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.commit().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L186:C29

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.rollback().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L221:C23

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.commit().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L237:C26

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx2.rollback().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L264:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.commit().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L267:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.rollback().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L319:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let name = tx.savepoint().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L321:C61

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let n: u32 = name.trim_start_matches("sp_").parse().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L381:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L382:C77

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool: &'static Pool = Box::leak(Box::new(Pool::new(config, factory).unwrap()));
```

#### 🟠 [HIGH] `unwrap()` @ L409:C17

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
h.await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L429:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L430:C77

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool: &'static Pool = Box::leak(Box::new(Pool::new(config, factory).unwrap()));
```

#### 🟠 [HIGH] `unwrap()` @ L435:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
borrowed.push(pool.acquire().await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L449:C32

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let conn1 = borrowed.pop().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L450:C32

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let conn2 = borrowed.pop().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L462:C32

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let conn3 = borrowed.pop().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L481:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L482:C77

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool: &'static Pool = Box::leak(Box::new(Pool::new(config, factory).unwrap()));
```

#### 🟠 [HIGH] `unwrap()` @ L485:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let c1 = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L486:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let c2 = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L487:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let c3 = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L529:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L530:C77

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool: &'static Pool = Box::leak(Box::new(Pool::new(config, factory).unwrap()));
```

#### 🟠 [HIGH] `unwrap()` @ L533:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let conn = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L564:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L565:C77

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool: &'static Pool = Box::leak(Box::new(Pool::new(config, factory).unwrap()));
```

#### 🟠 [HIGH] `unwrap()` @ L635:C17

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
h.await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L698:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L699:C77

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool: &'static Pool = Box::leak(Box::new(Pool::new(config, factory).unwrap()));
```

#### 🟠 [HIGH] `unwrap()` @ L704:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conns.push(pool.acquire().await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L716:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conns2.push(pool.acquire().await.unwrap());
```

### `packages\sz-orm-core\tests\fuzz.rs`

#### 🟠 [HIGH] `unwrap()` @ L264:C79

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut builder = QueryBuilder::<FuzzModel>::new(get_dialect(db_type).unwrap());
```

### `packages\sz-orm-core\tests\integration_mysql.rs`

#### 🟠 [HIGH] `expect()` @ L47:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("mysql connect failed - is MySQL 9.6.0 running on 127.0.0.1:3306?");
```

#### 🟠 [HIGH] `expect()` @ L53:C46

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let dialect = get_dialect(DbType::MySQL).expect("mysql dialect");
```

#### 🟠 [HIGH] `expect()` @ L100:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("create table");
```

#### 🟠 [HIGH] `expect()` @ L104:C46

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let dialect = get_dialect(DbType::MySQL).expect("mysql dialect");
```

#### 🟠 [HIGH] `expect()` @ L114:C46

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let dialect = get_dialect(DbType::MySQL).expect("mysql dialect");
```

#### 🟠 [HIGH] `expect()` @ L146:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("insert 1");
```

#### 🟠 [HIGH] `expect()` @ L153:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("insert 2");
```

#### 🟠 [HIGH] `expect()` @ L160:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("insert 3");
```

#### 🟠 [HIGH] `expect()` @ L167:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("select");
```

#### 🟠 [HIGH] `expect()` @ L174:C46

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let dialect = get_dialect(DbType::MySQL).expect("mysql dialect");
```

#### 🟠 [HIGH] `unwrap()` @ L175:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let escaped = dialect.escape_string(v.as_str().unwrap());
```

#### 🟠 [HIGH] `expect()` @ L180:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("query row");
```

#### 🟠 [HIGH] `expect()` @ L197:C37

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let mut tx = pool.begin().await.expect("begin");
```

#### 🟠 [HIGH] `expect()` @ L217:C35

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
q.execute(&mut *tx).await.expect("batch insert");
```

#### 🟠 [HIGH] `expect()` @ L220:C23

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
tx.commit().await.expect("commit");
```

#### 🟠 [HIGH] `expect()` @ L235:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("count");
```

#### 🟠 [HIGH] `expect()` @ L244:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("query last");
```

#### 🟠 [HIGH] `expect()` @ L258:C37

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let mut tx = pool.begin().await.expect("begin");
```

#### 🟠 [HIGH] `expect()` @ L270:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("insert");
```

#### 🟠 [HIGH] `expect()` @ L272:C23

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
tx.commit().await.expect("commit");
```

#### 🟠 [HIGH] `expect()` @ L282:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("update");
```

#### 🟠 [HIGH] `expect()` @ L290:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("delete");
```

#### 🟠 [HIGH] `unwrap()` @ L298:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L311:C37

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let mut tx = pool.begin().await.expect("begin");
```

#### 🟠 [HIGH] `expect()` @ L322:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("insert");
```

#### 🟠 [HIGH] `expect()` @ L323:C23

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
tx.commit().await.expect("commit");
```

#### 🟠 [HIGH] `unwrap()` @ L329:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L342:C37

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let mut tx = pool.begin().await.expect("begin");
```

#### 🟠 [HIGH] `expect()` @ L353:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("insert");
```

#### 🟠 [HIGH] `expect()` @ L354:C25

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
tx.rollback().await.expect("rollback");
```

#### 🟠 [HIGH] `unwrap()` @ L360:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L374:C37

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let mut tx = pool.begin().await.expect("begin");
```

#### 🟠 [HIGH] `expect()` @ L386:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("insert");
```

#### 🟠 [HIGH] `expect()` @ L388:C23

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
tx.commit().await.expect("commit");
```

#### 🟠 [HIGH] `expect()` @ L390:C46

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let dialect = get_dialect(DbType::MySQL).expect("mysql dialect");
```

#### 🟠 [HIGH] `expect()` @ L403:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("page query");
```

#### 🟠 [HIGH] `expect()` @ L438:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("insert");
```

#### 🟠 [HIGH] `expect()` @ L442:C46

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let dialect = get_dialect(DbType::MySQL).expect("mysql dialect");
```

#### 🟠 [HIGH] `unwrap()` @ L452:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L463:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L488:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("insert 1");
```

#### 🟠 [HIGH] `expect()` @ L496:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("insert 2");
```

#### 🟠 [HIGH] `expect()` @ L499:C46

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let dialect = get_dialect(DbType::MySQL).expect("mysql dialect");
```

#### 🟠 [HIGH] `expect()` @ L508:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("json query");
```

#### 🟠 [HIGH] `expect()` @ L523:C37

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let mut tx = pool.begin().await.expect("begin");
```

#### 🟠 [HIGH] `expect()` @ L539:C35

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
q.execute(&mut *tx).await.expect("batch insert");
```

#### 🟠 [HIGH] `expect()` @ L541:C23

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
tx.commit().await.expect("commit");
```

#### 🟠 [HIGH] `expect()` @ L578:C50

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let (task_id, success, errors) = h.await.expect("task join");
```

#### 🟠 [HIGH] `expect()` @ L600:C37

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let mut tx = pool.begin().await.expect("begin");
```

#### 🟠 [HIGH] `expect()` @ L611:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("outer insert");
```

#### 🟠 [HIGH] `expect()` @ L618:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("sp1");
```

#### 🟠 [HIGH] `expect()` @ L625:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("inner1 insert");
```

#### 🟠 [HIGH] `expect()` @ L629:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("rollback sp1");
```

#### 🟠 [HIGH] `expect()` @ L633:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("release sp1");
```

#### 🟠 [HIGH] `expect()` @ L638:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("sp2");
```

#### 🟠 [HIGH] `expect()` @ L645:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("inner2 insert");
```

#### 🟠 [HIGH] `expect()` @ L649:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("release sp2");
```

#### 🟠 [HIGH] `expect()` @ L651:C23

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
tx.commit().await.expect("commit");
```

#### 🟠 [HIGH] `unwrap()` @ L657:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L664:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L674:C46

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let dialect = get_dialect(DbType::MySQL).expect("mysql dialect");
```

#### 🟠 [HIGH] `expect()` @ L712:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("insert value");
```

#### 🟠 [HIGH] `unwrap()` @ L719:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L788:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("create upsert table");
```

#### 🟠 [HIGH] `expect()` @ L816:C46

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let dialect = get_dialect(DbType::MySQL).expect("mysql dialect");
```

#### 🟠 [HIGH] `expect()` @ L822:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("build upsert sql");
```

#### 🟠 [HIGH] `expect()` @ L828:C28

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
q.execute(&pool).await.expect("execute upsert insert");
```

#### 🟠 [HIGH] `unwrap()` @ L834:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L842:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L868:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("seed insert");
```

#### 🟠 [HIGH] `expect()` @ L870:C46

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let dialect = get_dialect(DbType::MySQL).expect("mysql dialect");
```

#### 🟠 [HIGH] `expect()` @ L876:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("build upsert sql");
```

#### 🟠 [HIGH] `expect()` @ L882:C28

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
q.execute(&pool).await.expect("execute upsert update");
```

#### 🟠 [HIGH] `unwrap()` @ L888:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L895:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L920:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("seed");
```

#### 🟠 [HIGH] `expect()` @ L922:C46

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let dialect = get_dialect(DbType::MySQL).expect("mysql dialect");
```

#### 🟠 [HIGH] `expect()` @ L932:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("build upsert sql");
```

#### 🟠 [HIGH] `expect()` @ L938:C28

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
q.execute(&pool).await.expect("execute batch upsert");
```

#### 🟠 [HIGH] `unwrap()` @ L944:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L951:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L975:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("seed");
```

#### 🟠 [HIGH] `expect()` @ L977:C46

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let dialect = get_dialect(DbType::MySQL).expect("mysql dialect");
```

#### 🟠 [HIGH] `expect()` @ L983:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("build upsert sql");
```

#### 🟠 [HIGH] `expect()` @ L989:C28

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
q.execute(&pool).await.expect("execute upsert specific cols");
```

#### 🟠 [HIGH] `unwrap()` @ L996:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L1014:C46

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let dialect = get_dialect(DbType::MySQL).expect("mysql dialect");
```

#### 🟠 [HIGH] `expect()` @ L1021:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("build upsert sql");
```

#### 🟠 [HIGH] `expect()` @ L1027:C28

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
q.execute(&pool).await.expect("execute upsert with null");
```

#### 🟠 [HIGH] `unwrap()` @ L1033:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L1046:C46

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let dialect = get_dialect(DbType::MySQL).expect("mysql dialect");
```

#### 🟠 [HIGH] `expect()` @ L1057:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("build upsert sql");
```

#### 🟠 [HIGH] `expect()` @ L1065:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("execute upsert unicode/special");
```

#### 🟠 [HIGH] `unwrap()` @ L1071:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

### `packages\sz-orm-core\tests\integration_oracle.rs`

#### 🟠 [HIGH] `expect()` @ L42:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("oracle connect failed - is Oracle 23ai running on 127.0.0.1:1521?")
```

#### 🟠 [HIGH] `expect()` @ L66:C47

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let dialect = get_dialect(DbType::Oracle).expect("oracle dialect");
```

#### 🟠 [HIGH] `expect()` @ L102:C29

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
conn.execute(&sql, &[]).expect("create table");
```

#### 🟠 [HIGH] `expect()` @ L106:C47

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let dialect = get_dialect(DbType::Oracle).expect("oracle dialect");
```

#### 🟠 [HIGH] `expect()` @ L114:C47

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let dialect = get_dialect(DbType::Oracle).expect("oracle dialect");
```

#### 🟠 [HIGH] `expect()` @ L126:C47

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let dialect = get_dialect(DbType::Oracle).expect("oracle dialect");
```

#### 🟠 [HIGH] `expect()` @ L136:C47

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let dialect = get_dialect(DbType::Oracle).expect("oracle dialect");
```

#### 🟠 [HIGH] `expect()` @ L150:C47

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let dialect = get_dialect(DbType::Oracle).expect("oracle dialect");
```

#### 🟠 [HIGH] `unwrap()` @ L151:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let escaped = dialect.escape_string(v_str.as_str().unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L163:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dialect = get_dialect(DbType::Oracle).unwrap();
```

#### 🟠 [HIGH] `expect()` @ L173:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("insert 1");
```

#### 🟠 [HIGH] `expect()` @ L175:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("insert 2");
```

#### 🟠 [HIGH] `expect()` @ L177:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("insert 3");
```

#### 🟠 [HIGH] `expect()` @ L178:C19

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
conn.commit().expect("commit");
```

#### 🟠 [HIGH] `expect()` @ L195:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("query")
```

#### 🟠 [HIGH] `expect()` @ L204:C64

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let count: i64 = conn.query_row_as::<i64>(&count_sql, &[]).expect("count");
```

#### 🟠 [HIGH] `unwrap()` @ L219:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dialect = get_dialect(DbType::Oracle).unwrap();
```

#### 🟠 [HIGH] `expect()` @ L231:C49

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let mut stmt = conn.statement(&sql).build().expect("build statement");
```

#### 🟠 [HIGH] `expect()` @ L238:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("insert");
```

#### 🟠 [HIGH] `expect()` @ L240:C19

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
conn.commit().expect("commit");
```

#### 🟠 [HIGH] `expect()` @ L251:C64

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let count: i64 = conn.query_row_as::<i64>(&count_sql, &[]).expect("count");
```

#### 🟠 [HIGH] `expect()` @ L264:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("last name");
```

#### 🟠 [HIGH] `unwrap()` @ L279:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dialect = get_dialect(DbType::Oracle).unwrap();
```

#### 🟠 [HIGH] `expect()` @ L288:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("insert");
```

#### 🟠 [HIGH] `expect()` @ L289:C19

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
conn.commit().expect("commit 1");
```

#### 🟠 [HIGH] `expect()` @ L293:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("insert 2");
```

#### 🟠 [HIGH] `expect()` @ L294:C21

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
conn.rollback().expect("rollback");
```

#### 🟠 [HIGH] `expect()` @ L298:C64

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let count: i64 = conn.query_row_as::<i64>(&count_sql, &[]).expect("count");
```

### `packages\sz-orm-core\tests\integration_pg.rs`

#### 🟠 [HIGH] `expect()` @ L45:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("pg connect failed - is PostgreSQL 18 running?")
```

#### 🟠 [HIGH] `expect()` @ L50:C51

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let dialect = get_dialect(DbType::PostgreSQL).expect("pg dialect");
```

#### 🟠 [HIGH] `expect()` @ L97:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("create table");
```

#### 🟠 [HIGH] `expect()` @ L101:C51

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let dialect = get_dialect(DbType::PostgreSQL).expect("pg dialect");
```

#### 🟠 [HIGH] `expect()` @ L111:C51

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let dialect = get_dialect(DbType::PostgreSQL).expect("pg dialect");
```

#### 🟠 [HIGH] `expect()` @ L143:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("insert 1");
```

#### 🟠 [HIGH] `expect()` @ L150:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("insert 2");
```

#### 🟠 [HIGH] `expect()` @ L157:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("insert 3");
```

#### 🟠 [HIGH] `expect()` @ L163:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("select");
```

#### 🟠 [HIGH] `expect()` @ L170:C51

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let dialect = get_dialect(DbType::PostgreSQL).expect("pg dialect");
```

#### 🟠 [HIGH] `unwrap()` @ L171:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let escaped = dialect.escape_string(v.as_str().unwrap());
```

#### 🟠 [HIGH] `expect()` @ L176:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("query row");
```

#### 🟠 [HIGH] `expect()` @ L193:C37

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let mut tx = pool.begin().await.expect("begin");
```

#### 🟠 [HIGH] `expect()` @ L217:C35

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
q.execute(&mut *tx).await.expect("batch insert");
```

#### 🟠 [HIGH] `expect()` @ L220:C23

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
tx.commit().await.expect("commit");
```

#### 🟠 [HIGH] `expect()` @ L234:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("count");
```

#### 🟠 [HIGH] `expect()` @ L242:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("query last");
```

#### 🟠 [HIGH] `expect()` @ L255:C37

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let mut tx = pool.begin().await.expect("begin");
```

#### 🟠 [HIGH] `expect()` @ L267:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("insert");
```

#### 🟠 [HIGH] `expect()` @ L269:C23

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
tx.commit().await.expect("commit");
```

#### 🟠 [HIGH] `expect()` @ L278:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("update");
```

#### 🟠 [HIGH] `expect()` @ L285:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("delete");
```

#### 🟠 [HIGH] `unwrap()` @ L292:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L305:C37

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let mut tx = pool.begin().await.expect("begin");
```

#### 🟠 [HIGH] `expect()` @ L316:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("insert");
```

#### 🟠 [HIGH] `expect()` @ L317:C23

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
tx.commit().await.expect("commit");
```

#### 🟠 [HIGH] `unwrap()` @ L323:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L336:C37

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let mut tx = pool.begin().await.expect("begin");
```

#### 🟠 [HIGH] `expect()` @ L347:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("insert");
```

#### 🟠 [HIGH] `expect()` @ L348:C25

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
tx.rollback().await.expect("rollback");
```

#### 🟠 [HIGH] `unwrap()` @ L354:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L367:C37

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let mut tx = pool.begin().await.expect("begin");
```

#### 🟠 [HIGH] `expect()` @ L379:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("insert");
```

#### 🟠 [HIGH] `expect()` @ L381:C23

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
tx.commit().await.expect("commit");
```

#### 🟠 [HIGH] `expect()` @ L383:C51

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let dialect = get_dialect(DbType::PostgreSQL).expect("pg dialect");
```

#### 🟠 [HIGH] `expect()` @ L396:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("page query");
```

#### 🟠 [HIGH] `expect()` @ L431:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("insert");
```

#### 🟠 [HIGH] `expect()` @ L434:C51

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let dialect = get_dialect(DbType::PostgreSQL).expect("pg dialect");
```

#### 🟠 [HIGH] `unwrap()` @ L444:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L454:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L478:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("insert 1");
```

#### 🟠 [HIGH] `expect()` @ L486:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("insert 2");
```

#### 🟠 [HIGH] `expect()` @ L488:C51

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let dialect = get_dialect(DbType::PostgreSQL).expect("pg dialect");
```

#### 🟠 [HIGH] `expect()` @ L497:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("json query");
```

#### 🟠 [HIGH] `expect()` @ L512:C37

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let mut tx = pool.begin().await.expect("begin");
```

#### 🟠 [HIGH] `expect()` @ L532:C35

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
q.execute(&mut *tx).await.expect("batch insert");
```

#### 🟠 [HIGH] `expect()` @ L534:C23

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
tx.commit().await.expect("commit");
```

#### 🟠 [HIGH] `expect()` @ L570:C50

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let (task_id, success, errors) = h.await.expect("task join");
```

#### 🟠 [HIGH] `expect()` @ L592:C37

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let mut tx = pool.begin().await.expect("begin");
```

#### 🟠 [HIGH] `expect()` @ L603:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("outer insert");
```

#### 🟠 [HIGH] `expect()` @ L608:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("sp1");
```

#### 🟠 [HIGH] `expect()` @ L615:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("inner1 insert");
```

#### 🟠 [HIGH] `expect()` @ L619:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("rollback sp1");
```

#### 🟠 [HIGH] `expect()` @ L623:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("release sp1");
```

#### 🟠 [HIGH] `expect()` @ L628:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("sp2");
```

#### 🟠 [HIGH] `expect()` @ L635:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("inner2 insert");
```

#### 🟠 [HIGH] `expect()` @ L639:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("release sp2");
```

#### 🟠 [HIGH] `expect()` @ L641:C23

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
tx.commit().await.expect("commit");
```

#### 🟠 [HIGH] `unwrap()` @ L647:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L654:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L669:C51

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let dialect = get_dialect(DbType::PostgreSQL).expect("pg dialect");
```

#### 🟠 [HIGH] `expect()` @ L682:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("insert returning");
```

#### 🟠 [HIGH] `expect()` @ L749:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("create upsert table");
```

#### 🟠 [HIGH] `expect()` @ L776:C51

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let dialect = get_dialect(DbType::PostgreSQL).expect("pg dialect");
```

#### 🟠 [HIGH] `expect()` @ L782:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("build upsert sql");
```

#### 🟠 [HIGH] `expect()` @ L788:C28

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
q.execute(&pool).await.expect("execute upsert insert");
```

#### 🟠 [HIGH] `unwrap()` @ L794:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L802:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L828:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("seed insert");
```

#### 🟠 [HIGH] `expect()` @ L830:C51

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let dialect = get_dialect(DbType::PostgreSQL).expect("pg dialect");
```

#### 🟠 [HIGH] `expect()` @ L836:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("build upsert sql");
```

#### 🟠 [HIGH] `expect()` @ L842:C28

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
q.execute(&pool).await.expect("execute upsert update");
```

#### 🟠 [HIGH] `unwrap()` @ L848:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L855:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L880:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("seed");
```

#### 🟠 [HIGH] `expect()` @ L882:C51

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let dialect = get_dialect(DbType::PostgreSQL).expect("pg dialect");
```

#### 🟠 [HIGH] `expect()` @ L892:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("build upsert sql");
```

#### 🟠 [HIGH] `expect()` @ L898:C28

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
q.execute(&pool).await.expect("execute batch upsert");
```

#### 🟠 [HIGH] `unwrap()` @ L904:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L911:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L935:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("seed");
```

#### 🟠 [HIGH] `expect()` @ L937:C51

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let dialect = get_dialect(DbType::PostgreSQL).expect("pg dialect");
```

#### 🟠 [HIGH] `expect()` @ L943:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("build upsert sql");
```

#### 🟠 [HIGH] `expect()` @ L949:C28

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
q.execute(&pool).await.expect("execute upsert specific cols");
```

#### 🟠 [HIGH] `unwrap()` @ L956:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L974:C51

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let dialect = get_dialect(DbType::PostgreSQL).expect("pg dialect");
```

#### 🟠 [HIGH] `expect()` @ L981:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("build upsert sql");
```

#### 🟠 [HIGH] `expect()` @ L987:C28

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
q.execute(&pool).await.expect("execute upsert with null");
```

#### 🟠 [HIGH] `unwrap()` @ L993:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L1006:C51

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let dialect = get_dialect(DbType::PostgreSQL).expect("pg dialect");
```

#### 🟠 [HIGH] `expect()` @ L1017:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("build upsert sql");
```

#### 🟠 [HIGH] `expect()` @ L1025:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("execute upsert unicode/special");
```

#### 🟠 [HIGH] `unwrap()` @ L1031:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

### `packages\sz-orm-core\tests\integration_sqlite.rs`

#### 🟠 [HIGH] `expect()` @ L71:C47

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let conn = RusqliteConn::open_in_memory().expect("open sqlite in-memory");
```

#### 🟠 [HIGH] `expect()` @ L79:C47

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let dialect = get_dialect(DbType::Sqlite).expect("sqlite dialect");
```

#### 🟠 [HIGH] `expect()` @ L115:C28

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
conn.execute(&sql, []).expect("create table");
```

#### 🟠 [HIGH] `expect()` @ L120:C47

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let dialect = get_dialect(DbType::Sqlite).expect("sqlite dialect");
```

#### 🟠 [HIGH] `expect()` @ L143:C6

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("insert 1");
```

#### 🟠 [HIGH] `expect()` @ L148:C6

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("insert 2");
```

#### 🟠 [HIGH] `expect()` @ L153:C6

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("insert 3");
```

#### 🟠 [HIGH] `unwrap()` @ L158:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L161:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("query")
```

#### 🟠 [HIGH] `expect()` @ L170:C47

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let dialect = get_dialect(DbType::Sqlite).expect("sqlite dialect");
```

#### 🟠 [HIGH] `unwrap()` @ L171:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let escaped = dialect.escape_string(v_str.as_str().unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L174:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L175:C60

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let value: i64 = stmt2.query_row([], |row| row.get(0)).expect("query row");
```

#### 🟠 [HIGH] `expect()` @ L186:C31

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
conn.execute("BEGIN", []).expect("begin");
```

#### 🟠 [HIGH] `expect()` @ L190:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("prepare");
```

#### 🟠 [HIGH] `expect()` @ L197:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("insert");
```

#### 🟠 [HIGH] `expect()` @ L200:C32

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
conn.execute("COMMIT", []).expect("commit");
```

#### 🟠 [HIGH] `expect()` @ L212:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("count");
```

#### 🟠 [HIGH] `expect()` @ L222:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("query last");
```

#### 🟠 [HIGH] `expect()` @ L232:C31

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
conn.execute("BEGIN", []).expect("begin");
```

#### 🟠 [HIGH] `expect()` @ L238:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("insert");
```

#### 🟠 [HIGH] `expect()` @ L240:C32

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
conn.execute("COMMIT", []).expect("commit");
```

#### 🟠 [HIGH] `expect()` @ L245:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("update");
```

#### 🟠 [HIGH] `expect()` @ L251:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("delete");
```

#### 🟠 [HIGH] `unwrap()` @ L257:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L266:C31

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
conn.execute("BEGIN", []).expect("begin");
```

#### 🟠 [HIGH] `expect()` @ L271:C6

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("insert");
```

#### 🟠 [HIGH] `expect()` @ L272:C32

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
conn.execute("COMMIT", []).expect("commit");
```

#### 🟠 [HIGH] `unwrap()` @ L276:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L285:C31

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
conn.execute("BEGIN", []).expect("begin");
```

#### 🟠 [HIGH] `expect()` @ L290:C6

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("insert");
```

#### 🟠 [HIGH] `expect()` @ L292:C34

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
conn.execute("ROLLBACK", []).expect("rollback");
```

#### 🟠 [HIGH] `unwrap()` @ L296:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L305:C31

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
conn.execute("BEGIN", []).expect("begin");
```

#### 🟠 [HIGH] `expect()` @ L311:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("insert");
```

#### 🟠 [HIGH] `expect()` @ L313:C32

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
conn.execute("COMMIT", []).expect("commit");
```

#### 🟠 [HIGH] `expect()` @ L315:C47

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let dialect = get_dialect(DbType::Sqlite).expect("sqlite dialect");
```

#### 🟠 [HIGH] `unwrap()` @ L322:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut stmt = conn.prepare(&sql).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L325:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `expect()` @ L353:C6

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("insert");
```

#### 🟠 [HIGH] `expect()` @ L357:C47

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let dialect = get_dialect(DbType::Sqlite).expect("sqlite dialect");
```

#### 🟠 [HIGH] `unwrap()` @ L362:C65

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let count: i64 = conn.query_row(&sql, [], |row| row.get(0)).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L369:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L378:C31

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
conn.execute("BEGIN", []).expect("begin");
```

#### 🟠 [HIGH] `expect()` @ L383:C6

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("insert outer");
```

#### 🟠 [HIGH] `expect()` @ L386:C39

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
conn.execute("SAVEPOINT sp1", []).expect("sp1");
```

#### 🟠 [HIGH] `expect()` @ L391:C6

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("insert inner1");
```

#### 🟠 [HIGH] `expect()` @ L393:C41

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
conn.execute("ROLLBACK TO sp1", []).expect("rollback sp1");
```

#### 🟠 [HIGH] `expect()` @ L394:C37

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
conn.execute("RELEASE sp1", []).expect("release sp1");
```

#### 🟠 [HIGH] `expect()` @ L397:C39

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
conn.execute("SAVEPOINT sp2", []).expect("sp2");
```

#### 🟠 [HIGH] `expect()` @ L402:C6

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("insert inner2");
```

#### 🟠 [HIGH] `expect()` @ L403:C37

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
conn.execute("RELEASE sp2", []).expect("release sp2");
```

#### 🟠 [HIGH] `expect()` @ L405:C32

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
conn.execute("COMMIT", []).expect("commit");
```

#### 🟠 [HIGH] `unwrap()` @ L409:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L413:C74

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut stmt = conn.prepare("SELECT name FROM t_sp ORDER BY id").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L415:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `expect()` @ L431:C46

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let conn = RusqliteConn::open(&path).expect("open");
```

#### 🟠 [HIGH] `expect()` @ L436:C35

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
conn.execute("BEGIN", []).expect("begin");
```

#### 🟠 [HIGH] `expect()` @ L442:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("insert");
```

#### 🟠 [HIGH] `expect()` @ L444:C36

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
conn.execute("COMMIT", []).expect("commit");
```

#### 🟠 [HIGH] `expect()` @ L455:C57

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let conn = RusqliteConn::open(&*path_clone).expect("open");
```

#### 🟠 [HIGH] `expect()` @ L499:C18

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("send");
```

#### 🟠 [HIGH] `expect()` @ L540:C47

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let dialect = get_dialect(DbType::Sqlite).expect("sqlite dialect");
```

#### 🟠 [HIGH] `expect()` @ L576:C32

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
conn.execute(&sql, []).expect("insert value");
```

#### 🟠 [HIGH] `unwrap()` @ L581:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L632:C6

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("create upsert table");
```

#### 🟠 [HIGH] `expect()` @ L641:C47

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let dialect = get_dialect(DbType::Sqlite).expect("sqlite dialect");
```

#### 🟠 [HIGH] `expect()` @ L647:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("build upsert sql");
```

#### 🟠 [HIGH] `expect()` @ L655:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("execute upsert insert");
```

#### 🟠 [HIGH] `unwrap()` @ L660:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L670:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L687:C6

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("seed insert");
```

#### 🟠 [HIGH] `expect()` @ L689:C47

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let dialect = get_dialect(DbType::Sqlite).expect("sqlite dialect");
```

#### 🟠 [HIGH] `expect()` @ L696:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("build upsert sql");
```

#### 🟠 [HIGH] `expect()` @ L703:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("execute upsert update");
```

#### 🟠 [HIGH] `unwrap()` @ L708:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L718:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L734:C6

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("seed");
```

#### 🟠 [HIGH] `expect()` @ L736:C47

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let dialect = get_dialect(DbType::Sqlite).expect("sqlite dialect");
```

#### 🟠 [HIGH] `expect()` @ L747:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("build upsert sql");
```

#### 🟠 [HIGH] `expect()` @ L754:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("execute batch upsert");
```

#### 🟠 [HIGH] `unwrap()` @ L759:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L769:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L779:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L788:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L803:C6

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("seed");
```

#### 🟠 [HIGH] `expect()` @ L805:C47

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let dialect = get_dialect(DbType::Sqlite).expect("sqlite dialect");
```

#### 🟠 [HIGH] `expect()` @ L812:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("build upsert sql with specific columns");
```

#### 🟠 [HIGH] `expect()` @ L819:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("execute upsert");
```

#### 🟠 [HIGH] `unwrap()` @ L828:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L838:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L853:C6

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("seed");
```

#### 🟠 [HIGH] `expect()` @ L855:C47

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let dialect = get_dialect(DbType::Sqlite).expect("sqlite dialect");
```

#### 🟠 [HIGH] `expect()` @ L867:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("build upsert sql with null");
```

#### 🟠 [HIGH] `expect()` @ L874:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("execute upsert with null");
```

#### 🟠 [HIGH] `unwrap()` @ L883:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L893:C47

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let dialect = get_dialect(DbType::Sqlite).expect("sqlite dialect");
```

#### 🟠 [HIGH] `expect()` @ L905:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("build upsert sql");
```

#### 🟠 [HIGH] `expect()` @ L912:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("execute upsert with unicode");
```

#### 🟠 [HIGH] `unwrap()` @ L917:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L927:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L934:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("build upsert sql 2");
```

#### 🟠 [HIGH] `expect()` @ L941:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("execute upsert 2");
```

#### 🟠 [HIGH] `unwrap()` @ L945:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L954:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

### `packages\sz-orm-core\tests\jepsen.rs`

#### 🟠 [HIGH] `unwrap()` @ L59:C32

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.execute("INSERT").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L60:C23

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.commit().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L84:C32

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.execute("INSERT").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L85:C25

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.rollback().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L111:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sp1 = tx.savepoint().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L113:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sp2 = tx.savepoint().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L115:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sp3 = tx.savepoint().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L119:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.rollback_to_savepoint(&sp2).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L121:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.release_savepoint(&sp3).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L124:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sp4 = tx.savepoint().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L127:C23

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.commit().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L134:C23

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.commit().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L144:C25

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.rollback().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L153:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sp = tx.savepoint().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L154:C23

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.commit().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L166:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sp = tx.savepoint().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L167:C23

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.commit().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L185:C32

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.execute("INSERT").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L216:C32

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.execute("INSERT").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L246:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.execute("INSERT 1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L256:C25

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.rollback().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L265:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.execute("INSERT 1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L306:C18

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L314:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
mgr_clone.commit(&tx_id).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L318:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
mgr_clone.rollback(&tx_id).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L328:C17

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
h.await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L366:C6

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L392:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L393:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool = Pool::new(config, factory).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L423:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L424:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool = Pool::new(config, factory).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L427:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let c1 = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L428:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let c2 = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L458:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L459:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool = Arc::new(Pool::new(config, factory).unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L485:C17

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
h.await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L502:C32

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.execute("INSERT").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L516:C23

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.commit().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L530:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.execute("INSERT 1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L531:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sp1 = tx.savepoint().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L532:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.execute("INSERT 2").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L533:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let _sp2 = tx.savepoint().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L534:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.execute("INSERT 3").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L537:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.rollback_to_savepoint(&sp1).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L543:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.execute("INSERT 4").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L546:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sp3 = tx.savepoint().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L549:C23

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.commit().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L560:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sp1 = tx.savepoint().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L561:C32

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.execute("INSERT").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L564:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.release_savepoint(&sp1).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L567:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.execute("INSERT 2").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L568:C23

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.commit().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L579:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.execute(&format!("INSERT {}", i)).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L582:C23

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.commit().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L592:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.execute(&format!("INSERT {}", i)).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L595:C25

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.rollback().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L638:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.execute("INSERT").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L652:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.rollback().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L660:C17

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
h.await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L680:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sp = tx.savepoint().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L682:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.execute("INSERT").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L686:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.rollback_to_savepoint(&savepoints[9]).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L693:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.release_savepoint(sp).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L696:C23

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.commit().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L705:C23

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.commit().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L713:C25

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.rollback().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L724:C23

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.commit().await.unwrap();
```

### `packages\sz-orm-core\tests\param_binding.rs`

#### 🟠 [HIGH] `unwrap()` @ L55:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dialect = get_dialect(DbType::Sqlite).unwrap();
```

### `packages\sz-orm-core\tests\soak.rs`

#### 🟠 [HIGH] `expect()` @ L77:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("PoolConfig invalid");
```

#### 🟠 [HIGH] `expect()` @ L78:C52

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let pool = Arc::new(Pool::new(config, factory).expect("Pool::new failed"));
```

#### 🟠 [HIGH] `expect()` @ L170:C42

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let csv_path_str = csv_path.to_str().expect("CSV path not UTF-8");
```

#### 🟠 [HIGH] `expect()` @ L218:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("PoolConfig invalid");
```

#### 🟠 [HIGH] `expect()` @ L219:C52

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let pool = Arc::new(Pool::new(config, factory).expect("Pool::new failed"));
```

#### 🟠 [HIGH] `unwrap()` @ L279:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
monitor.snapshots().last().unwrap().ops_completed >= 50,
```

### `packages\sz-orm-core\tests\stress.rs`

#### 🟠 [HIGH] `unwrap()` @ L36:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L37:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool = Pool::new(config, factory).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L79:C17

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
h.await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L102:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
held_conns.push(pool.acquire().await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L130:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
pool.release(held_conns.pop().unwrap()).await;
```

#### 🟠 [HIGH] `unwrap()` @ L181:C17

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
h.await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L204:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let conn = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L206:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.execute("INSERT INTO t VALUES (1)").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L207:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.execute("INSERT INTO t VALUES (2)").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L224:C17

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
h.await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L232:C23

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.commit().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L282:C17

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
h.await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L302:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
held.push(pool.acquire().await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L308:C20

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(result.unwrap().is_err(), "should be Timeout error");
```

#### 🟠 [HIGH] `unwrap()` @ L311:C29

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
pool.release(held.pop().unwrap()).await;
```

#### 🟠 [HIGH] `unwrap()` @ L355:C17

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
h.await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L374:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
held.push(pool.acquire().await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L427:C17

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
h.await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L445:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conns.push(pool.acquire().await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L473:C17

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
h.await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L494:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L495:C79

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool: &'static Pool = &*Box::leak(Box::new(Pool::new(config, factory).unwrap()));
```

#### 🟠 [HIGH] `unwrap()` @ L520:C17

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
h.await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L554:C33

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
d.pop().unwrap() as u64
```

#### 🟠 [HIGH] `unwrap()` @ L567:C17

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
h.await.unwrap();
```

### `packages\sz-orm-crypto\src\lib.rs`

#### 🟠 [HIGH] `unwrap()` @ L802:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let encrypted = crypter.encrypt(plaintext).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L803:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let decrypted = crypter.decrypt(&encrypted).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L812:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let encrypted1 = crypter.encrypt(plaintext).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L813:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let encrypted2 = crypter.encrypt(plaintext).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L815:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(crypter.decrypt(&encrypted1).unwrap(), plaintext);
```

#### 🟠 [HIGH] `unwrap()` @ L816:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(crypter.decrypt(&encrypted2).unwrap(), plaintext);
```

#### 🟠 [HIGH] `unwrap()` @ L823:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let encrypted = crypter.encrypt(plaintext).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L824:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let decrypted = crypter.decrypt(&encrypted).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L839:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let encrypted = crypter.encrypt(b"").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L842:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let decrypted = crypter.decrypt(&encrypted).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L850:C60

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let encrypted = crypter.encrypt(b"sensitive data").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L861:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let hash = hasher.hash("password123").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L865:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(parts[0].parse::<u32>().unwrap(), 100_000);
```

#### 🟠 [HIGH] `unwrap()` @ L875:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let hash = hasher.hash("password123").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L876:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(hasher.verify("password123", &hash).unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L882:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let hash = hasher.hash("password123").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L883:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!hasher.verify("wrongpassword", &hash).unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L889:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let h1 = hasher.hash("pass1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L890:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let h2 = hasher.hash("pass2").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L897:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let h1 = hasher.hash("same").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L898:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let h2 = hasher.hash("same").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L900:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(hasher.verify("same", &h1).unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L901:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(hasher.verify("same", &h2).unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L915:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let hash = hasher.hash("secret").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L918:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(hasher.verify("secret", &hash).unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L1040:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let decoded = hex_decode(&encoded).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1058:C66

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let encrypted = crypter.encrypt_with_aad(plaintext, aad).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1059:C67

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let decrypted = crypter.decrypt_with_aad(&encrypted, aad).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1069:C66

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let encrypted = crypter.encrypt_with_aad(plaintext, aad).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1081:C59

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let encrypted_no_aad = crypter.encrypt(plaintext).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1082:C76

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let encrypted_empty_aad = crypter.encrypt_with_aad(plaintext, b"").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1084:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(crypter.decrypt(&encrypted_no_aad).unwrap(), plaintext);
```

#### 🟠 [HIGH] `unwrap()` @ L1086:C65

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
crypter.decrypt_with_aad(&encrypted_empty_aad, b"").unwrap(),
```

#### 🟠 [HIGH] `unwrap()` @ L1095:C67

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let encrypted = crypter.encrypt_with_aad(b"data", b"aad").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1105:C63

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let encrypted = crypter.encrypt_with_aad(b"", b"aad").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1108:C70

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let decrypted = crypter.decrypt_with_aad(&encrypted, b"aad").unwrap();
```

#### 🟠 [HIGH] `expect()` @ L1116:C54

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let crypter = RsaOaepCrypter::generate(2048).expect("RSA key generation");
```

#### 🟠 [HIGH] `unwrap()` @ L1118:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let encrypted = crypter.encrypt(plaintext).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1119:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let decrypted = crypter.decrypt(&encrypted).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1125:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let crypter = RsaOaepCrypter::generate(2048).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1127:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let enc1 = crypter.encrypt(plaintext).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1128:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let enc2 = crypter.encrypt(plaintext).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1132:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(crypter.decrypt(&enc1).unwrap(), plaintext);
```

#### 🟠 [HIGH] `unwrap()` @ L1133:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(crypter.decrypt(&enc2).unwrap(), plaintext);
```

#### 🟠 [HIGH] `unwrap()` @ L1138:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let crypter = RsaOaepCrypter::generate(2048).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1139:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let encrypted = crypter.encrypt(b"").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1140:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let decrypted = crypter.decrypt(&encrypted).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1146:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let crypter = RsaOaepCrypter::generate(2048).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1147:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let encrypted = crypter.encrypt(b"secret").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1156:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let crypter = RsaOaepCrypter::generate(2048).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1158:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let encrypted = crypter.encrypt(&plaintext).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1159:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let decrypted = crypter.decrypt(&encrypted).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1165:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let crypter = RsaOaepCrypter::generate(2048).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1173:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let crypter1 = RsaOaepCrypter::generate(2048).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1179:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let encrypted = crypter2.encrypt(plaintext).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1180:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let decrypted = crypter2.decrypt(&encrypted).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1186:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let crypter = RsaOaepCrypter::generate(2048).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1188:C63

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let encrypted = Crypter::encrypt(&crypter, plaintext).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1189:C64

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let decrypted = Crypter::decrypt(&crypter, &encrypted).unwrap();
```

#### 🟠 [HIGH] `expect()` @ L1387:C43

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let found = mgr.key_by_version(1).expect("v1 should exist");
```

#### 🟠 [HIGH] `unwrap()` @ L1394:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
mgr.rotate(b"v2".to_vec()).unwrap();
```

#### 🟠 [HIGH] `expect()` @ L1396:C41

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let old = mgr.key_by_version(1).expect("v1 should still be retained");
```

#### 🟠 [HIGH] `expect()` @ L1399:C41

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let new = mgr.key_by_version(2).expect("v2 should exist");
```

#### 🟠 [HIGH] `unwrap()` @ L1412:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
mgr.rotate(b"v2".to_vec()).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1413:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
mgr.rotate(b"v3".to_vec()).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1414:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
mgr.rotate(b"v4".to_vec()).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1417:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
mgr.rotate(b"v5".to_vec()).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1453:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
mgr.rotate(b"k2".to_vec()).unwrap();
```

#### 🟠 [HIGH] `expect()` @ L1473:C22

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
h.join().expect("thread panicked");
```

### `packages\sz-orm-dtx\src\cross_shard.rs`

#### 🟠 [HIGH] `unwrap()` @ L512:C25

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
coord.execute().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L582:C25

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
coord.execute().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L589:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let states = coord.participant_states().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L595:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let ids = coord.participant_ids().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L639:C25

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
coord.execute().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L645:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let states = coord.participant_states().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L771:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
coord.prepare_only().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L776:C24

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
coord.commit().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L803:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
coord.prepare_only().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L807:C26

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
coord.rollback().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L890:C25

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
coord.execute().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L901:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
for s in coord.participant_states().unwrap() {
```

### `packages\sz-orm-dtx\src\file_log.rs`

#### 🟠 [HIGH] `expect()` @ L68:C34

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let cur = guard.as_mut().expect("已确保初始化");
```

### `packages\sz-orm-dtx\src\grpc_coordinator.rs`

#### 🟠 [HIGH] `unwrap()` @ L296:C69

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let resp = coord.prepare("tx-1", &["p1".to_string()]).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L300:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
coord.commit("tx-1").await.unwrap();
```

### `packages\sz-orm-dtx\src\lib.rs`

#### 🟠 [HIGH] `unwrap()` @ L646:C20

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
t.commit().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L653:C22

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
t.rollback().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L667:C21

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
p.prepare().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L674:C20

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
p.commit().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L681:C22

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
p.rollback().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L719:C22

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.prepare().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L724:C21

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.commit().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L778:C22

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.prepare().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L807:C22

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.prepare().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L809:C23

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.rollback().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L823:C24

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
m.begin("tx1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L831:C24

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
m.begin("tx1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L845:C24

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
m.begin("tx3").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L846:C24

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
m.begin("tx1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L847:C24

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
m.begin("tx2").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L854:C28

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
m.begin("tx-flow").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L861:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L868:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L870:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
m.prepare("tx-flow").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L873:C29

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
m.commit("tx-flow").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L876:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let states = m.participant_states("tx-flow").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L886:C26

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
m.begin("tx-rb").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L891:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L892:C29

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
m.rollback("tx-rb").unwrap();
```

### `packages\sz-orm-dtx\src\recovery.rs`

#### 🟠 [HIGH] `expect()` @ L77:C48

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let entry = latest.get(&tx_id).expect("key 来自 keys()");
```

#### 🟠 [HIGH] `unwrap()` @ L140:C33

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
self.entries.lock().unwrap().push(e);
```

#### 🟠 [HIGH] `unwrap()` @ L146:C33

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
self.entries.lock().unwrap().push(entry.clone());
```

#### 🟠 [HIGH] `unwrap()` @ L150:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
Ok(self.entries.lock().unwrap().clone())
```

#### 🟠 [HIGH] `unwrap()` @ L175:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let actions = r.recover(&log).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L184:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let actions = r.recover(&log).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L193:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let actions = r.recover(&log).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L202:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let actions = r.recover(&log).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L214:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let actions = r.recover(&log).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L228:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let actions = r.recover(&log).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L248:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let actions = r.recover(&log).unwrap();
```

### `packages\sz-orm-dtx\src\saga.rs`

#### 🟠 [HIGH] `unwrap()` @ L756:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let last = entries.last().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1004:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = saga.execute().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1025:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1034:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1036:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = saga.execute().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1067:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1073:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1075:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = saga.execute().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1116:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1128:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1134:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1136:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = saga.execute().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1160:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1166:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1168:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = saga.execute().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1192:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1193:C24

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
saga.execute().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1202:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1203:C24

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
saga.execute().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1216:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1217:C24

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
saga.execute().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1250:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1252:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = saga.execute().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1278:C26

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
m.register(saga).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1280:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = m.execute("mgr-1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1289:C26

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
m.register(saga).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1305:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
m.register(Saga::new("c")).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1306:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
m.register(Saga::new("a")).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1307:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
m.register(Saga::new("b")).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1317:C26

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
m.register(saga).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1318:C28

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
m.execute("steps").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1320:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let states = m.step_states("steps").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1327:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
m.register(Saga::new("rm")).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1337:C26

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
m.register(saga).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1338:C28

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
m.execute("reset").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1341:C26

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
m.reset("reset").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1373:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1385:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1397:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1399:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = saga.execute().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1430:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1442:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1448:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1450:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = saga.execute().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1470:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
log.append(entry.clone()).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1471:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let entries = log.read_all("s1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1481:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let entries = log.read_all("nonexistent").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1496:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1505:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1506:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(log.read_all("s1").unwrap().len(), 1);
```

#### 🟠 [HIGH] `unwrap()` @ L1507:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(log.read_all("s2").unwrap().len(), 1);
```

#### 🟠 [HIGH] `unwrap()` @ L1516:C24

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
saga.execute().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1518:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let entries = log.read_all("log-1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1540:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1542:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1552:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let entries = log.read_all("log-2").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1574:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1576:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1586:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let entries = log.read_all("log-3").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1611:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1620:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1622:C62

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut saga = Saga::recover_from_log("rec-1", &log).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1628:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1630:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1631:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = saga.execute().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1647:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1656:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1658:C62

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut saga = Saga::recover_from_log("rec-2", &log).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1668:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1670:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1671:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = saga.resume_compensation().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1686:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1688:C58

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let saga = Saga::recover_from_log("rec-3", &log).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1703:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1705:C58

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let saga = Saga::recover_from_log("rec-4", &log).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1720:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1722:C58

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let saga = Saga::recover_from_log("rec-5", &log).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1760:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1762:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = saga.execute().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1783:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1785:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = saga.execute().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1797:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1801:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = saga.execute().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1811:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1828:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1830:C65

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut saga = Saga::recover_from_log("resume-2", &log).unwrap();
```

### `packages\sz-orm-dtx\src\sqlite_log.rs`

#### 🟠 [HIGH] `unwrap()` @ L148:C58

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let _log = SqliteTransactionLog::new(":memory:").unwrap();
```

### `packages\sz-orm-dtx\src\tcc.rs`

#### 🟠 [HIGH] `unwrap()` @ L1113:C23

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
p.try_phase().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1132:C23

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
p.try_phase().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1133:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
p.confirm_phase().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1143:C23

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
p.try_phase().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1144:C26

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
p.cancel_phase().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1153:C23

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
p.try_phase().unwrap();
```

#### 🟠 [HIGH] `expect()` @ L1157:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("cancel_phase must succeed for a tried participant");
```

#### 🟠 [HIGH] `unwrap()` @ L1239:C25

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
coord.execute().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1492:C25

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
coord.execute().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1503:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
m.register("tx-mgr-001").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1510:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1511:C33

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
m.execute("tx-mgr-001").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1518:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
m.register("tx-failed-1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1525:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1529:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
m.register("tx-success-1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1536:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1537:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
m.execute("tx-success-1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1554:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
m.register("tx-recover").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1570:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1575:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
m.retry_confirm("tx-recover").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1582:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
m.register("tx-dup").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1660:C25

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
coord.execute().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1794:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
coord.try_phase().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1827:C31

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
coord.retry_confirm().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1894:C23

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
p.try_phase().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1897:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
p.confirm_phase().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1904:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
p.confirm_phase().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1919:C23

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
p.try_phase().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1922:C26

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
p.cancel_phase().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1929:C26

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
p.cancel_phase().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1946:C23

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
p.try_phase().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1957:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(coord.retry_policy().unwrap().max_retries, 3);
```

#### 🟠 [HIGH] `unwrap()` @ L1985:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
coord.try_phase().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1989:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
coord.confirm_with_retry().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L2008:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
coord.try_phase().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L2030:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
coord.try_phase().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L2031:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
coord.confirm_with_retry().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L2051:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
coord.try_phase().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L2077:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
coord.try_phase().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L2078:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
coord.cancel_with_retry().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L2097:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
coord.try_phase().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L2112:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
coord.try_phase().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L2133:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
coord.try_phase().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L2156:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
coord.try_phase().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L2199:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
coord.try_phase().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L2200:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
coord.cancel_with_retry().unwrap();
```

### `packages\sz-orm-dtx\tests\stress.rs`

#### 🟠 [HIGH] `unwrap()` @ L22:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
manager.begin(&format!("tx-{}", i)).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L27:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
manager.commit(&format!("tx-{}", i)).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L56:C18

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.prepare().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L59:C17

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.commit().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L105:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L120:C18

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.prepare().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L131:C20

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx1.rollback().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L137:C19

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx2.prepare().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L138:C20

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx2.rollback().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L146:C20

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx3.rollback().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L154:C17

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx.commit().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L165:C20

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tx2.rollback().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L190:C33

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
m.begin(&tx_id).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L191:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
m.commit(&tx_id).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L196:C18

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
h.join().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L211:C26

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
manager.begin("dup").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L247:C25

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
manager.begin("tx").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L252:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L254:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let states = manager.participant_states("tx").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L260:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
manager.prepare("tx").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L261:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let states = manager.participant_states("tx").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L271:C25

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
manager.begin("tx").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L274:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L275:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
manager.prepare("tx").unwrap();
```

### `packages\sz-orm-es\src\extensions.rs`

#### 🟠 [HIGH] `unwrap()` @ L226:C31

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let hit = hit.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1130:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = executor.bulk(actions).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1133:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(executor.backend().count("docs").unwrap(), 2);
```

#### 🟠 [HIGH] `unwrap()` @ L1144:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = executor.bulk(vec![action]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1158:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1164:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1166:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(executor.backend().count("docs").unwrap(), 0);
```

#### 🟠 [HIGH] `unwrap()` @ L1179:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1187:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1191:C67

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let search_result = executor.backend().search(search_req).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1205:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1234:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = executor.bulk(actions).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1237:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(executor.backend().count("docs").unwrap(), 1);
```

#### 🟠 [HIGH] `unwrap()` @ L1268:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1271:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1274:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1277:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = suggester.suggest("docs", &req).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1298:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1302:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = suggester.suggest("docs", &req).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1311:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1313:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = suggester.suggest("docs", &req).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1322:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1325:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1327:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = suggester.suggest("docs", &req).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1328:C69

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let rust = result.options.iter().find(|o| o.text == "rust").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1330:C69

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let ruby = result.options.iter().find(|o| o.text == "ruby").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1339:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1341:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = suggester.suggest("docs", &req).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1436:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
backend.sync_to_es(docs).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1441:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1444:C72

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let tech = results[0].buckets.iter().find(|b| b.key == "tech").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1457:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1462:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1474:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1482:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1495:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1506:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1520:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1528:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1545:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1550:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1564:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1572:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1583:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1591:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1630:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1631:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let indices = manager.resolve_index("alias1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1640:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1641:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
manager.remove_alias("alias1", "index1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1642:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let indices = manager.resolve_index("alias1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1651:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1654:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1655:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let indices = manager.resolve_index("alias1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1666:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1669:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1670:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let aliases = manager.get_aliases("index1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1679:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1682:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1683:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let all = manager.list_aliases().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1692:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1695:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1696:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let indices = manager.resolve_index("alias1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1705:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1708:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1709:C61

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let write_index = manager.get_write_index("alias1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1718:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1719:C61

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let write_index = manager.get_write_index("alias1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1728:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1732:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1733:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let all = manager.list_aliases().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1748:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
manager.update_aliases(actions).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1749:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let all = manager.list_aliases().unwrap();
```

### `packages\sz-orm-es\src\lib.rs`

#### 🟠 [HIGH] `unwrap()` @ L855:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(retrieved.unwrap().len(), 2);
```

#### 🟠 [HIGH] `unwrap()` @ L872:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = sync.sync_to_es(docs).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L874:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(sync.count("users").unwrap(), 2);
```

#### 🟠 [HIGH] `unwrap()` @ L883:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L887:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L888:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(sync.count("idx").unwrap(), 1);
```

#### 🟠 [HIGH] `unwrap()` @ L891:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let res = sync.search(req).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L900:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L912:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L914:C70

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = sync.delete_from_es("i", vec!["1".to_string()]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L916:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(sync.count("i").unwrap(), 1);
```

#### 🟠 [HIGH] `unwrap()` @ L928:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L931:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L943:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L945:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let res = sync.search(req).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L957:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L959:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let res = sync.search(req).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L972:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L977:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let res = sync.search(req).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L989:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L994:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let res = sync.search(req).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1006:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1013:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let res = sync.search(req).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1024:C31

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
sync.sync_to_es(docs).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1029:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let res = sync.search(req).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1040:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let res2 = sync.search(req2).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1049:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let res3 = sync.search(req3).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1071:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1073:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(manager.count("idx").unwrap(), 1);
```

#### 🟠 [HIGH] `unwrap()` @ L1076:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let res = manager.search(req).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1081:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1083:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(manager.count("idx").unwrap(), 0);
```

#### 🟠 [HIGH] `unwrap()` @ L1093:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1095:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let res = sync.search(req).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1144:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1153:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let count = manager.count("empty_idx").unwrap();
```

### `packages\sz-orm-es\src\real_es.rs`

#### 🟠 [HIGH] `unwrap()` @ L416:C58

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let (field, value) = terms.iter().next().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L430:C59

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let (field, values) = terms.iter().next().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L444:C59

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let (field, range) = ranges.iter().next().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L615:C60

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(dsl["query"]["bool"]["must"].as_array().unwrap().len(), 2);
```

#### 🟠 [HIGH] `unwrap()` @ L639:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = parse_search_response(response).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L658:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = parse_search_response(response).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L668:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let serialized = serde_json::to_string(&doc).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L669:C74

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let deserialized: EsDocument = serde_json::from_str(&serialized).unwrap();
```

### `packages\sz-orm-graphql\src\extensions.rs`

#### 🟠 [HIGH] `unwrap()` @ L1311:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let json = serde_json::to_string(&kind).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1312:C60

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let de: RelationKind = serde_json::from_str(&json).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1335:C29

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = result.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1379:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let user = ds.find_by_id("User", "2").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1410:C63

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let user = ds.resolve_many_to_one("User", "id", "u1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1413:C64

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let user2 = ds.resolve_many_to_one("User", "id", "u2").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1503:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let after_cursor = conn1.page_info.end_cursor.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1621:C32

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(result.data.unwrap()["name"], "Alice");
```

#### 🟠 [HIGH] `unwrap()` @ L1630:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(result.error.unwrap().contains("no mutation handler"));
```

#### 🟠 [HIGH] `unwrap()` @ L1662:C32

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let data = result.data.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1688:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let updated = store.find_by_id("User", "1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1697:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(result.error.unwrap().contains("not found"));
```

#### 🟠 [HIGH] `unwrap()` @ L1736:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let event = event.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1783:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let event1 = handle1.try_recv().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1786:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let event2 = handle2.try_recv().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1803:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let e1 = handle.try_recv().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1804:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let e2 = handle.try_recv().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1805:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let e3 = handle.try_recv().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1835:C26

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(event.unwrap().payload["delayed"], true);
```

#### 🟠 [HIGH] `unwrap()` @ L1836:C23

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
thread.join().unwrap();
```

### `packages\sz-orm-graphql\src\lib.rs`

#### 🟠 [HIGH] `expect()` @ L404:C31

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let url = srv.start().expect("start should succeed");
```

#### 🟠 [HIGH] `unwrap()` @ L422:C24

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let v = result.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L424:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(v["name"].as_str().unwrap().contains("getUser"));
```

#### 🟠 [HIGH] `unwrap()` @ L433:C24

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let v = result.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L435:C33

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(v.as_array().unwrap().len(), 2);
```

#### 🟠 [HIGH] `expect()` @ L471:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("url must start with http://");
```

#### 🟠 [HIGH] `expect()` @ L477:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("connect should succeed");
```

#### 🟠 [HIGH] `expect()` @ L485:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("write should succeed");
```

#### 🟠 [HIGH] `expect()` @ L490:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("read should succeed");
```

#### 🟠 [HIGH] `expect()` @ L491:C43

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let text = String::from_utf8(raw).expect("response must be valid UTF-8");
```

#### 🟠 [HIGH] `expect()` @ L494:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("response must contain a header/body separator");
```

#### 🟠 [HIGH] `expect()` @ L499:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("status line must contain a numeric status code");
```

#### 🟠 [HIGH] `expect()` @ L500:C47

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let json = serde_json::from_str(body).expect("response body must be valid JSON");
```

#### 🟠 [HIGH] `expect()` @ L510:C31

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let url = srv.start().expect("start should succeed");
```

#### 🟠 [HIGH] `expect()` @ L521:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("name must be a string")
```

#### 🟠 [HIGH] `expect()` @ L531:C31

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let url = srv.start().expect("start should succeed");
```

#### 🟠 [HIGH] `expect()` @ L541:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("listUsers must be an array");
```

#### 🟠 [HIGH] `expect()` @ L553:C31

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let url = srv.start().expect("start should succeed");
```

#### 🟠 [HIGH] `expect()` @ L561:C48

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let errors = body["errors"].as_array().expect("errors must be an array");
```

#### 🟠 [HIGH] `expect()` @ L565:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("message must be a string")
```

#### 🟠 [HIGH] `expect()` @ L577:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("single query should succeed");
```

#### 🟠 [HIGH] `expect()` @ L581:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("name must be a string")
```

#### 🟠 [HIGH] `expect()` @ L585:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("list query should succeed");
```

#### 🟠 [HIGH] `expect()` @ L586:C36

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
assert_eq!(list.as_array().expect("result must be an array").len(), 2);
```

### `packages\sz-orm-graphql\src\real_graphql.rs`

#### 🟠 [HIGH] `unwrap()` @ L355:C24

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let v = result.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L358:C32

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
v["name"].as_str().unwrap().contains("real"),
```

#### 🟠 [HIGH] `unwrap()` @ L372:C24

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let v = result.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L374:C32

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let arr = v.as_array().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L378:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(arr[0]["name"].as_str().unwrap().contains("real"));
```

#### 🟠 [HIGH] `unwrap()` @ L388:C24

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let v = result.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L391:C32

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
v["name"].as_str().unwrap().contains("getUser"),
```

### `packages\sz-orm-grpc\src\lib.rs`

#### 🟠 [HIGH] `expect()` @ L681:C30

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("last_error must be Some in Err branch");
```

#### 🟠 [HIGH] `expect()` @ L689:C36

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
last_error.expect("last_error must be Some after Err branch assignment")
```

#### 🟠 [HIGH] `unwrap()` @ L814:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(handle.unwrap().address(), "localhost:50051");
```

#### 🟠 [HIGH] `unwrap()` @ L845:C29

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let client = client.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L921:C38

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let _handle = server.start().expect("server start");
```

#### 🟠 [HIGH] `expect()` @ L923:C53

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let client = UserGrpcClient::connect(&addr).expect("connect");
```

#### 🟠 [HIGH] `expect()` @ L924:C40

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let user = client.get_user(42).expect("get_user");
```

#### 🟠 [HIGH] `expect()` @ L929:C39

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let bob = client.get_user(43).expect("get_user bob");
```

#### 🟠 [HIGH] `expect()` @ L939:C38

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let _handle = server.start().expect("server start");
```

#### 🟠 [HIGH] `expect()` @ L941:C53

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let client = UserGrpcClient::connect(&addr).expect("connect");
```

#### 🟠 [HIGH] `expect()` @ L956:C38

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let _handle = server.start().expect("server start");
```

#### 🟠 [HIGH] `expect()` @ L958:C53

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let client = UserGrpcClient::connect(&addr).expect("connect");
```

#### 🟠 [HIGH] `expect()` @ L959:C41

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let users = client.list_users().expect("list_users");
```

#### 🟠 [HIGH] `expect()` @ L970:C53

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let client = UserGrpcClient::connect(&addr).expect("connect");
```

#### 🟠 [HIGH] `expect()` @ L984:C38

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let _handle = server.start().expect("server start");
```

#### 🟠 [HIGH] `expect()` @ L986:C53

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let client = UserGrpcClient::connect(&addr).expect("connect");
```

#### 🟠 [HIGH] `expect()` @ L996:C38

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let _handle = server.start().expect("server start");
```

#### 🟠 [HIGH] `expect()` @ L1006:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("call_unary");
```

#### 🟠 [HIGH] `unwrap()` @ L1015:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(svc.list_users().unwrap().len(), 1);
```

#### 🟠 [HIGH] `unwrap()` @ L1018:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(svc.list_users().unwrap().len(), 0);
```

#### 🟠 [HIGH] `expect()` @ L1026:C37

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let handle = server.start().expect("server start");
```

#### 🟠 [HIGH] `expect()` @ L1028:C53

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let client = UserGrpcClient::connect(&addr).expect("connect");
```

#### 🟠 [HIGH] `expect()` @ L1029:C39

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let resp = client.get_user(1).expect("get_user before stop");
```

#### 🟠 [HIGH] `expect()` @ L1035:C23

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
handle.stop().expect("stop");
```

#### 🟠 [HIGH] `expect()` @ L1058:C31

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
"127.0.0.1:0".parse().expect("valid socket addr")
```

#### 🟠 [HIGH] `expect()` @ L1071:C58

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let mut handle = server.start(bind_addr()).await.expect("server start");
```

#### 🟠 [HIGH] `expect()` @ L1076:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("connect");
```

#### 🟠 [HIGH] `expect()` @ L1077:C46

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let user = client.get_user(42).await.expect("get_user");
```

#### 🟠 [HIGH] `expect()` @ L1082:C29

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
handle.stop().await.expect("stop");
```

#### 🟠 [HIGH] `expect()` @ L1096:C58

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let mut handle = server.start(bind_addr()).await.expect("server start");
```

#### 🟠 [HIGH] `expect()` @ L1101:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("connect");
```

#### 🟠 [HIGH] `expect()` @ L1102:C47

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let users = client.list_users().await.expect("list_users");
```

#### 🟠 [HIGH] `expect()` @ L1109:C29

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
handle.stop().await.expect("stop");
```

#### 🟠 [HIGH] `expect()` @ L1120:C58

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let mut handle = server.start(bind_addr()).await.expect("server start");
```

#### 🟠 [HIGH] `expect()` @ L1125:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("connect");
```

#### 🟠 [HIGH] `expect()` @ L1135:C29

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
handle.stop().await.expect("stop");
```

#### 🟠 [HIGH] `expect()` @ L1148:C58

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let mut handle = server.start(bind_addr()).await.expect("server start");
```

#### 🟠 [HIGH] `expect()` @ L1155:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("connect1");
```

#### 🟠 [HIGH] `expect()` @ L1156:C67

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let mut client2 = RealGrpcClient::connect(addr_str).await.expect("connect2");
```

#### 🟠 [HIGH] `expect()` @ L1158:C45

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let u1 = client1.get_user(42).await.expect("get_user via client1");
```

#### 🟠 [HIGH] `expect()` @ L1159:C45

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let u2 = client2.get_user(42).await.expect("get_user via client2");
```

#### 🟠 [HIGH] `expect()` @ L1167:C47

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let list = client2.list_users().await.expect("list_users via client2");
```

#### 🟠 [HIGH] `expect()` @ L1171:C29

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
handle.stop().await.expect("stop");
```

#### 🟠 [HIGH] `expect()` @ L1237:C38

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let _handle = server.start().expect("server start");
```

#### 🟠 [HIGH] `expect()` @ L1242:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("server streaming");
```

#### 🟠 [HIGH] `expect()` @ L1261:C38

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let _handle = server.start().expect("server start");
```

#### 🟠 [HIGH] `expect()` @ L1263:C53

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let client = UserGrpcClient::connect(&addr).expect("connect");
```

#### 🟠 [HIGH] `expect()` @ L1264:C44

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let stream = client.stream_users().expect("stream_users");
```

#### 🟠 [HIGH] `expect()` @ L1276:C38

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let _handle = server.start().expect("server start");
```

#### 🟠 [HIGH] `expect()` @ L1278:C53

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let client = UserGrpcClient::connect(&addr).expect("connect");
```

#### 🟠 [HIGH] `expect()` @ L1279:C44

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let stream = client.stream_users().expect("stream_users");
```

#### 🟠 [HIGH] `expect()` @ L1384:C38

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let _handle = server.start().expect("server start");
```

#### 🟠 [HIGH] `expect()` @ L1407:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("call should succeed");
```

#### 🟠 [HIGH] `expect()` @ L1418:C38

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let _handle = server.start().expect("server start");
```

#### 🟠 [HIGH] `expect()` @ L1437:C38

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let _handle = server.start().expect("server start");
```

#### 🟠 [HIGH] `expect()` @ L1449:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("call should succeed");
```

#### 🟠 [HIGH] `expect()` @ L1460:C38

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let _handle = server.start().expect("server start");
```

#### 🟠 [HIGH] `expect()` @ L1681:C38

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let _handle = server.start().expect("server start");
```

#### 🟠 [HIGH] `expect()` @ L1698:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("should succeed after retries");
```

#### 🟠 [HIGH] `expect()` @ L1726:C38

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let _handle = server.start().expect("server start");
```

### `packages\sz-orm-health\src\advanced.rs`

#### 🟠 [HIGH] `expect()` @ L1084:C51

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let json = serde_json::to_string(&result).expect("serialize");
```

#### 🟠 [HIGH] `expect()` @ L1085:C61

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let back: ProbeResult = serde_json::from_str(&json).expect("deserialize");
```

### `packages\sz-orm-health\src\lib.rs`

#### 🟠 [HIGH] `expect()` @ L1184:C22

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
h.join().expect("thread panicked");
```

#### 🟠 [HIGH] `expect()` @ L1218:C51

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let json = serde_json::to_string(&report).expect("serialize");
```

#### 🟠 [HIGH] `expect()` @ L1219:C62

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let back: HealthReport = serde_json::from_str(&json).expect("deserialize");
```

#### 🟠 [HIGH] `expect()` @ L1304:C46

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let json = serde_json::to_string(&r).expect("serialize");
```

#### 🟠 [HIGH] `expect()` @ L1305:C62

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let back: HealthReport = serde_json::from_str(&json).expect("deserialize");
```

#### 🟠 [HIGH] `expect()` @ L1322:C65

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let back: HealthReport = serde_json::from_str(old_json).expect("deserialize legacy");
```

#### 🟠 [HIGH] `expect()` @ L1366:C31

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let m = alert.metrics.expect("metrics present");
```

#### 🟠 [HIGH] `expect()` @ L1374:C50

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let json = serde_json::to_string(&alert).expect("serialize");
```

#### 🟠 [HIGH] `expect()` @ L1375:C61

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let back: HealthAlert = serde_json::from_str(&json).expect("deserialize");
```

#### 🟠 [HIGH] `expect()` @ L1380:C33

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
assert_eq!(back.metrics.expect("metrics").p99_ms, Some(999.0));
```

#### 🟠 [HIGH] `expect()` @ L1817:C44

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let m = Arc::get_mut(&mut mgr).expect("unique ref");
```

#### 🟠 [HIGH] `expect()` @ L1836:C22

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
h.join().expect("thread panicked");
```

### `packages\sz-orm-lc\src\lib.rs`

#### 🟠 [HIGH] `expect()` @ L1298:C59

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let id = m.fields.iter().find(|f| f.name == "id").expect("id field");
```

#### 🟠 [HIGH] `expect()` @ L1306:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("name field");
```

#### 🟠 [HIGH] `unwrap()` @ L1787:C63

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let id_field = fields.iter().find(|f| f.name == "id").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1791:C67

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let name_field = fields.iter().find(|f| f.name == "name").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1796:C65

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let age_field = fields.iter().find(|f| f.name == "age").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1800:C65

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let bio_field = fields.iter().find(|f| f.name == "bio").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1875:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(schema["required"].as_array().unwrap().len(), 1);
```

#### 🟠 [HIGH] `unwrap()` @ L1950:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let set_clause = sql.split("WHERE").next().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L2064:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pk = model.primary_key().unwrap();
```

### `packages\sz-orm-limit\src\lib.rs`

#### 🟠 [HIGH] `unwrap()` @ L827:C24

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(result.unwrap().allowed);
```

#### 🟠 [HIGH] `unwrap()` @ L834:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r1 = limiter.acquire("key1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L837:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r2 = limiter.acquire("key1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L840:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r3 = limiter.acquire("key1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L848:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r1 = limiter.acquire("key-a").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L851:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r2 = limiter.acquire("key-b").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L859:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
limiter.acquire("reset-key").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L860:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
limiter.acquire("reset-key").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L862:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
limiter.reset("reset-key").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L864:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = limiter.acquire("reset-key").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L873:C24

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(result.unwrap().allowed);
```

#### 🟠 [HIGH] `unwrap()` @ L880:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r1 = limiter.acquire("key1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L884:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r2 = limiter.acquire("key1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L888:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r3 = limiter.acquire("key1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L896:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r1 = limiter.acquire("key-a").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L899:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r2 = limiter.acquire("key-b").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L907:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
limiter.acquire("reset-key").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L908:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
limiter.acquire("reset-key").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L910:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
limiter.reset("reset-key").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L912:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = limiter.acquire("reset-key").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L920:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r1 = limiter.try_acquire("key").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L923:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r2 = limiter.try_acquire("key").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L935:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r1 = limiter.acquire("zero-refill").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L939:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r2 = limiter.acquire("zero-refill").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L954:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r1 = limiter.acquire("neg-refill").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L957:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r2 = limiter.acquire("neg-refill").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L972:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r = limiter.acquire("key").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L980:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(limiter.acquire("key").unwrap().allowed);
```

#### 🟠 [HIGH] `unwrap()` @ L981:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(limiter.acquire("key").unwrap().allowed);
```

#### 🟠 [HIGH] `unwrap()` @ L982:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r3 = limiter.acquire("key").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L990:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(limiter.acquire("key-a").unwrap().allowed);
```

#### 🟠 [HIGH] `unwrap()` @ L991:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(limiter.acquire("key-b").unwrap().allowed);
```

#### 🟠 [HIGH] `unwrap()` @ L997:C32

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
limiter.acquire("key").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L998:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!limiter.acquire("key").unwrap().allowed);
```

#### 🟠 [HIGH] `unwrap()` @ L999:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
limiter.reset("key").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1000:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(limiter.acquire("key").unwrap().allowed);
```

#### 🟠 [HIGH] `unwrap()` @ L1006:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r1 = limiter.acquire("key").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1008:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r2 = limiter.acquire("key").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1010:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r3 = limiter.acquire("key").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1017:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r = limiter.acquire("key").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1024:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(limiter.try_acquire("key").unwrap().allowed);
```

#### 🟠 [HIGH] `unwrap()` @ L1025:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!limiter.try_acquire("key").unwrap().allowed);
```

#### 🟠 [HIGH] `unwrap()` @ L1033:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = backend.get("key").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1041:C73

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let (count1, reset1) = backend.incr_and_get("key", 60, now, 10).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1045:C68

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let (count2, _) = backend.incr_and_get("key", 60, now, 10).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1053:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
backend.incr_and_get("key", 60, now, 10).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1054:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = backend.get("key").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1056:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(result.unwrap().0, 1);
```

#### 🟠 [HIGH] `unwrap()` @ L1063:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
backend.incr_and_get("key", 60, now, 10).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1064:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(backend.get("key").unwrap().is_some());
```

#### 🟠 [HIGH] `unwrap()` @ L1065:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
backend.reset_key("key").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1066:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(backend.get("key").unwrap().is_none());
```

#### 🟠 [HIGH] `unwrap()` @ L1074:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
backend.incr_and_get("key", 60, now, 10).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1075:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
backend.incr_and_get("key", 60, now, 10).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1077:C72

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let (count, _) = backend.incr_and_get("key", 60, now + 61, 10).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1085:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r = limiter.acquire("key").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1093:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(limiter.acquire("key").unwrap().allowed);
```

#### 🟠 [HIGH] `unwrap()` @ L1094:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(limiter.acquire("key").unwrap().allowed);
```

#### 🟠 [HIGH] `unwrap()` @ L1095:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!limiter.acquire("key").unwrap().allowed);
```

#### 🟠 [HIGH] `unwrap()` @ L1106:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(limiter1.acquire("key").unwrap().allowed);
```

#### 🟠 [HIGH] `unwrap()` @ L1108:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(limiter2.acquire("key").unwrap().allowed);
```

#### 🟠 [HIGH] `unwrap()` @ L1110:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!limiter1.acquire("key").unwrap().allowed);
```

#### 🟠 [HIGH] `unwrap()` @ L1116:C32

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
limiter.acquire("key").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1117:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!limiter.acquire("key").unwrap().allowed);
```

#### 🟠 [HIGH] `unwrap()` @ L1118:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
limiter.reset("key").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1119:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(limiter.acquire("key").unwrap().allowed);
```

#### 🟠 [HIGH] `unwrap()` @ L1125:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(limiter.acquire("key-a").unwrap().allowed);
```

#### 🟠 [HIGH] `unwrap()` @ L1126:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(limiter.acquire("key-b").unwrap().allowed);
```

#### 🟠 [HIGH] `unwrap()` @ L1132:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r1 = limiter.acquire("key").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1134:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r2 = limiter.acquire("key").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1156:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(headers.retry_after.unwrap() > 0);
```

#### 🟠 [HIGH] `unwrap()` @ L1220:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(response.body["retry_after"].as_u64().unwrap() > 0);
```

#### 🟠 [HIGH] `unwrap()` @ L1249:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = multi.check_all("key").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1260:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(multi.check_all("key").unwrap().allowed);
```

#### 🟠 [HIGH] `unwrap()` @ L1262:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = multi.check_all("key").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1273:C32

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
multi.check_all("key").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1274:C32

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
multi.check_all("key").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1276:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!multi.check_all("key").unwrap().allowed);
```

#### 🟠 [HIGH] `unwrap()` @ L1283:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(multi.check_all("key").unwrap().allowed);
```

### `packages\sz-orm-limit\tests\stress.rs`

#### 🟠 [HIGH] `unwrap()` @ L23:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = limiter.acquire(&key).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L27:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = limiter.acquire(&key).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L42:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = limiter.acquire(&key).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L46:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = limiter.acquire(&key).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L65:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = l.acquire("shared-key").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L75:C18

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
h.join().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L106:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = l.acquire("shared-key").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L116:C18

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
h.join().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L136:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = limiter.acquire("key").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L139:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = limiter.acquire("key").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L142:C26

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
limiter.reset("key").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L144:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = limiter.acquire("key").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L155:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r1 = limiter.acquire("key").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L159:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r2 = limiter.try_acquire("key").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L169:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = limiter.acquire("key").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L172:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = limiter.acquire("key").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L178:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = limiter.acquire("key").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L188:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = limiter.acquire("key").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L191:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = limiter.acquire("key").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L197:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = limiter.acquire("key").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L209:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = limiter.acquire(&key).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L213:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = limiter.acquire(&key).unwrap();
```

### `packages\sz-orm-logger\src\advanced.rs`

#### 🟠 [HIGH] `expect()` @ L769:C50

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let rotated = rotator.rotated_content(0).expect("rotated[0] must exist");
```

#### 🟠 [HIGH] `expect()` @ L1123:C36

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let json = entry.to_json().expect("serialize");
```

#### 🟠 [HIGH] `expect()` @ L1124:C57

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let back = StructuredLogEntry::from_json(&json).expect("deserialize");
```

### `packages\sz-orm-macros\src\derive.rs`

#### 🟠 [HIGH] `unwrap()` @ L234:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let field_name = field.ident.as_ref().unwrap().to_string();
```

#### 🟠 [HIGH] `unwrap()` @ L335:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let ident = field.ident.clone().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L463:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
result.push(ch.to_lowercase().next().unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L514:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let ty: Type = syn::parse_str("Option<String>").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L520:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let ty: Type = syn::parse_str("Option<i64>").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L526:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let ty: Type = syn::parse_str("String").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L532:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let ty: Type = syn::parse_str("Vec<u8>").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L540:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let ty: Type = syn::parse_str("i64").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L546:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let ty: Type = syn::parse_str("i32").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L552:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let ty: Type = syn::parse_str("i16").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L558:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let ty: Type = syn::parse_str("i8").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L564:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let ty: Type = syn::parse_str("f32").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L570:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let ty: Type = syn::parse_str("f64").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L576:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let ty: Type = syn::parse_str("bool").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L582:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let ty: Type = syn::parse_str("String").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L588:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let ty: Type = syn::parse_str("Vec<u8>").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L594:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let ty: Type = syn::parse_str("Option<String>").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L600:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let ty: Type = syn::parse_str("Option<i64>").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L606:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let ty: Type = syn::parse_str("MyCustomType").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L620:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L626:C74

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let input: DeriveInput = syn::parse_str("struct Foo { a: i64 }").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L642:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L645:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let field = named.named.first().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L665:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L668:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let field = named.named.first().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L685:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L688:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let field = named.named.first().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L705:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L708:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let field = named.named.first().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L725:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L728:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let field = named.named.first().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L745:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L748:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let field = named.named.first().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L765:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L768:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let field = named.named.first().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L786:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L789:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let field = named.named.first().unwrap();
```

### `packages\sz-orm-macros\src\lib.rs`

#### 🟠 [HIGH] `unwrap()` @ L1789:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(detect_db_kind("sqlite::memory:").unwrap(), DbKind::Sqlite);
```

#### 🟠 [HIGH] `unwrap()` @ L1800:C70

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
detect_db_kind("oracle:sys:test123@127.0.0.1:1521/FREE").unwrap(),
```

#### 🟠 [HIGH] `unwrap()` @ L1833:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let p = parse_oracle_dsn(dsn).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1847:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let p = parse_oracle_dsn(dsn).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1857:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let p = parse_sqlserver_dsn(dsn).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1869:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let p = parse_sqlserver_dsn(dsn).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1879:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let (table, cols) = parse_create_table(sql).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1893:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let (table, cols) = parse_create_table(sql).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1907:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let (_, cols) = parse_create_table(sql).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1915:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let (_, cols) = parse_create_table(sql).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1924:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let (_, cols) = parse_create_table(sql).unwrap();
```

### `packages\sz-orm-mig\src\advanced.rs`

#### 🟠 [HIGH] `unwrap()` @ L1085:C32

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tracker.register(&mig).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1093:C32

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tracker.register(&mig).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1108:C32

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tracker.register(&mig).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1109:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tracker.record_applied(1, 100).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1118:C32

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tracker.register(&mig).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1127:C32

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tracker.register(&mig).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1137:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1140:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1143:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1144:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tracker.record_applied(1, 10).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1145:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tracker.record_applied(3, 10).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1155:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1158:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1161:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1162:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tracker.record_applied(2, 10).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1173:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1176:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1177:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tracker.record_applied(1, 10).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1179:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tracker.record_applied(2, 10).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1188:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1191:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1194:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1207:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1208:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let rec = tracker.get_record(1).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1307:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
executor.add_migration(mig).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1316:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
executor.add_migration(mig).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1331:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
executor.add_migration(mig).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1352:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
executor.add_migration(mig).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1368:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
executor.add_migration(mig).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1379:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
executor.add_migration(mig).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1390:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1393:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1407:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
executor.add_migration(mig).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1462:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let json = serde_json::to_string(&mig).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1464:C63

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let restored: Migration = serde_json::from_str(&json).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1479:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mig: Migration = serde_json::from_str(json).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1494:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
executor.add_migration(mig).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1514:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
executor.add_migration(mig).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1535:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(result.error.unwrap().contains("not found"));
```

#### 🟠 [HIGH] `unwrap()` @ L1542:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
executor.add_migration(mig).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1547:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(result.error.unwrap().contains("not reversible"));
```

#### 🟠 [HIGH] `unwrap()` @ L1564:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
executor.add_migration(mig).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1566:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let (sql, params) = executor.dry_run_up(1).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1576:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
executor.add_migration(mig).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1579:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let _ = executor.dry_run_up(1).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1599:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
executor.add_migration(mig).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1601:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let (sql, params) = executor.dry_run_down(1).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1611:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
executor.add_migration(mig).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1615:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let _ = executor.dry_run_down(1).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1626:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
executor.add_migration(mig).unwrap();
```

### `packages\sz-orm-mig\src\lib.rs`

#### 🟠 [HIGH] `unwrap()` @ L165:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = chain.transform(row).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L179:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let filtered = filter.transform(row).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L196:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let filtered = filter.transform(row).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L229:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(store.count("users").await.unwrap(), 3);
```

#### 🟠 [HIGH] `unwrap()` @ L244:C59

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let page1 = store.read_table("users", 0, 2).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L250:C59

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let page2 = store.read_table("users", 2, 2).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L255:C62

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let page3 = store.read_table("users", 100, 10).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L262:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let cols = store.table_columns("users").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L284:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L296:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L298:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(store.count("orders").await.unwrap(), 2);
```

#### 🟠 [HIGH] `unwrap()` @ L307:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L308:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(store.count("orders").await.unwrap(), 3);
```

#### 🟠 [HIGH] `unwrap()` @ L322:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(store.count("users").await.unwrap(), 3);
```

#### 🟠 [HIGH] `unwrap()` @ L335:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L336:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(store.count("users").await.unwrap(), 3);
```

#### 🟠 [HIGH] `unwrap()` @ L337:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let cols = store.table_columns("users").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L358:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L365:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let reports = migrator.migrate_all().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L376:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(writer.count("users").await.unwrap(), 3);
```

#### 🟠 [HIGH] `unwrap()` @ L377:C60

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let rows = writer.read_table("users", 0, 10).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L382:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(reader.count("users").await.unwrap(), 3);
```

#### 🟠 [HIGH] `unwrap()` @ L425:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L429:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L436:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let reports = migrator.migrate_all().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L446:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(writer.count("users").await.unwrap(), 1);
```

#### 🟠 [HIGH] `unwrap()` @ L447:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(writer.count("orders").await.unwrap(), 2);
```

#### 🟠 [HIGH] `unwrap()` @ L463:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L471:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let reports = migrator.migrate_all().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L476:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(writer.count("users").await.unwrap(), 0);
```

#### 🟠 [HIGH] `unwrap()` @ L500:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L504:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L512:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let reports = migrator.migrate_all().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L518:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(writer.count("users").await.unwrap(), 1);
```

#### 🟠 [HIGH] `unwrap()` @ L519:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(writer.count("broken").await.unwrap(), 1);
```

### `packages\sz-orm-mqtt\src\broker.rs`

#### 🟠 [HIGH] `unwrap()` @ L363:C32

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
plugin.connect().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L368:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L381:C32

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
plugin.connect().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L386:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L390:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L394:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L413:C32

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
plugin.connect().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L415:C60

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
plugin.subscribe("home/#", QoS::ExactlyOnce).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L428:C32

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
plugin.connect().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L430:C59

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
plugin.subscribe("home/#", QoS::AtMostOnce).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L431:C60

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
plugin.subscribe("home/#", QoS::ExactlyOnce).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L443:C32

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
plugin.connect().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L445:C59

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
plugin.subscribe("home/#", QoS::AtMostOnce).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L448:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
plugin.unsubscribe("home/#").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L465:C32

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
plugin.connect().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L470:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L474:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L478:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L491:C32

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
plugin.connect().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L496:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L502:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("retained message should exist");
```

#### 🟠 [HIGH] `unwrap()` @ L511:C32

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
plugin.connect().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L514:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
plugin.disconnect().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L525:C32

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
plugin.connect().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L530:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

### `packages\sz-orm-mqtt\src\lib.rs`

#### 🟠 [HIGH] `unwrap()` @ L119:C91

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let msg = MqttMessage::json_message("test", &serde_json::json!({"key": "value"})).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L120:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parsed: serde_json::Value = msg.json().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L175:C32

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
plugin.connect().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L176:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
plugin.disconnect().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L194:C32

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
plugin.connect().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L213:C32

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
plugin.connect().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L234:C32

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
plugin.connect().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L235:C65

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
plugin.subscribe("home/living", QoS::AtLeastOnce).await.unwrap();
```

### `packages\sz-orm-mqtt\src\qos2.rs`

#### 🟠 [HIGH] `unwrap()` @ L425:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
s.register_publish(100, "topic/a").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L433:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
s.register_publish(1, "t").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L441:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
s.register_publish(50, "test").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L444:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
s.receive_pubrec(50).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L447:C33

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
s.send_pubrel(50).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L450:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
s.receive_pubcomp(50).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L466:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
s.register_publish(1, "t").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L467:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
s.receive_pubrec(1).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L476:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
s.register_publish(1, "t").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L485:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
s.register_publish(1, "t").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L486:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
s.receive_pubrec(1).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L495:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
s.register_publish(3, "t").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L496:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
s.register_publish(1, "t").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L497:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
s.register_publish(2, "t").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L504:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
r.receive_publish(7, "t").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L507:C32

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
r.send_pubrec(7).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L510:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
r.receive_pubrel(7).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L513:C33

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
r.send_pubcomp(7).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L520:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
r.receive_publish(1, "t").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L528:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
r.receive_publish(1, "t").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L547:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
sim.sender.register_publish(1, "t").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L548:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
sim.receiver.receive_publish(1, "t").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L549:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
sim.receiver.send_pubrec(1).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L550:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
sim.sender.receive_pubrec(1).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L562:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
sim.simulate(1, "topic/a").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L563:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
sim.simulate(2, "topic/b").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L564:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
sim.simulate(3, "topic/c").await.unwrap();
```

### `packages\sz-orm-mqtt\src\real_broker.rs`

#### 🟠 [HIGH] `unwrap()` @ L278:C32

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
client.connect().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L292:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L299:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L301:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
client.unsubscribe("test/topic").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L305:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
client.disconnect().await.unwrap();
```

### `packages\sz-orm-mqtt\src\retained.rs`

#### 🟠 [HIGH] `expect()` @ L215:C40

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let msg = store.get("t").await.expect("should exist");
```

### `packages\sz-orm-mqtt\src\shared.rs`

#### 🟠 [HIGH] `unwrap()` @ L336:C76

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let (group, filter) = parse_shared_filter("$share/g1/home/+/temp").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L448:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let i1 = sub.select_next().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L449:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let i2 = sub.select_next().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L450:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let i3 = sub.select_next().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L451:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let i4 = sub.select_next().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L463:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let idx = sub.select_next().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L478:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L488:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L491:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L508:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L511:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L524:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L527:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L537:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L553:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L556:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L559:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L570:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L573:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L576:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L590:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L600:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L603:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L606:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L626:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L629:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L640:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L650:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L653:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L664:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L667:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

### `packages\sz-orm-mqtt\src\topics.rs`

#### 🟠 [HIGH] `unwrap()` @ L113:C66

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let filter = TopicFilter::new("home/living/temperature").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L121:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let filter = TopicFilter::new("home/#").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L128:C61

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let filter = TopicFilter::new("home/+/temperature").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L200:C61

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let filter = TopicFilter::new("home/+/temperature").unwrap();
```

### `packages\sz-orm-mqtt\src\will.rs`

#### 🟠 [HIGH] `unwrap()` @ L356:C24

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(got.unwrap().delay_seconds, 10);
```

### `packages\sz-orm-mqtt\tests\stress.rs`

#### 🟠 [HIGH] `unwrap()` @ L17:C28

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
plugin.connect().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L32:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L54:C22

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L59:C17

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
h.await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L73:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L79:C59

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
plugin.unsubscribe(&format!("topic-{}", i)).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L92:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L111:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L127:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L149:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L203:C32

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
plugin.connect().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L212:C18

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L214:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
plugin.disconnect().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L220:C28

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
plugin.connect().await.unwrap();
```

### `packages\sz-orm-mssql\src\lib.rs`

#### 🟠 [HIGH] `expect()` @ L419:C30

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
self.client.as_ref().expect("connection must exist")
```

#### 🟠 [HIGH] `expect()` @ L425:C30

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
self.client.as_mut().expect("connection must exist")
```

### `packages\sz-orm-observability\src\summary.rs`

#### 🟠 [HIGH] `unwrap()` @ L536:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!((s.quantile(0.5).unwrap() - 1.5).abs() < 1e-9);
```

#### 🟠 [HIGH] `unwrap()` @ L546:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!((s.quantile(0.5).unwrap() - 3.0).abs() < 1e-9);
```

#### 🟠 [HIGH] `unwrap()` @ L556:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!((s.quantile(0.99).unwrap() - 100.0).abs() < 1e-9);
```

#### 🟠 [HIGH] `unwrap()` @ L580:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!((s.quantile(0.0).unwrap() - 10.0).abs() < 1e-9);
```

#### 🟠 [HIGH] `unwrap()` @ L582:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!((s.quantile(1.0).unwrap() - 30.0).abs() < 1e-9);
```

#### 🟠 [HIGH] `unwrap()` @ L638:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!((s.quantile(0.5).unwrap() - 30.0).abs() < 1e-9);
```

#### 🟠 [HIGH] `unwrap()` @ L790:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
exporter.push("metric1 1\n").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L791:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
exporter.push("metric2 2\n").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L807:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
exporter.push_from_registry(&registry).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L822:C32

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
exporter.push("m 1\n").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L833:C32

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
exporter.push("m 1\n").unwrap();
```

### `packages\sz-orm-oracle\src\lib.rs`

#### 🟠 [HIGH] `expect()` @ L107:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("Failed to create Oracle blocking pool runtime");
```

#### 🟠 [HIGH] `expect()` @ L608:C28

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
self.conn.as_ref().expect("connection must exist")
```

#### 🟠 [HIGH] `expect()` @ L614:C28

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
self.conn.as_mut().expect("connection must exist")
```

### `packages\sz-orm-oracle\tests\benchmark.rs`

#### 🟠 [HIGH] `expect()` @ L49:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("Oracle connect failed - is Oracle 23ai running on 127.0.0.1:1521?"),
```

#### 🟠 [HIGH] `expect()` @ L57:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("PoolConfig invalid");
```

#### 🟠 [HIGH] `expect()` @ L58:C32

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
Pool::new(config, factory).expect("Pool::new failed")
```

#### 🟠 [HIGH] `expect()` @ L64:C41

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let mut conn = pool.acquire().await.expect("acquire");
```

#### 🟠 [HIGH] `expect()` @ L75:C6

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("CREATE TABLE");
```

#### 🟠 [HIGH] `expect()` @ L91:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("INSERT");
```

#### 🟠 [HIGH] `expect()` @ L99:C41

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let mut conn = pool.acquire().await.expect("acquire");
```

#### 🟠 [HIGH] `expect()` @ L121:C45

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let mut conn = pool.acquire().await.expect("acquire");
```

#### 🟠 [HIGH] `expect()` @ L126:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("query");
```

#### 🟠 [HIGH] `expect()` @ L139:C45

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let mut conn = pool.acquire().await.expect("acquire");
```

#### 🟠 [HIGH] `expect()` @ L141:C43

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let rows = conn.query(&sql).await.expect("query");
```

#### 🟠 [HIGH] `expect()` @ L153:C45

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let mut conn = pool.acquire().await.expect("acquire");
```

#### 🟠 [HIGH] `expect()` @ L155:C70

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let (columns, values_matrix) = conn.query_values(&sql).await.expect("query_values");
```

#### 🟠 [HIGH] `expect()` @ L172:C45

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let mut conn = pool.acquire().await.expect("acquire");
```

#### 🟠 [HIGH] `expect()` @ L177:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("query_values_with_params");
```

#### 🟠 [HIGH] `expect()` @ L194:C45

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let mut conn = pool.acquire().await.expect("acquire");
```

#### 🟠 [HIGH] `expect()` @ L199:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("update");
```

#### 🟠 [HIGH] `expect()` @ L212:C45

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let mut conn = pool.acquire().await.expect("acquire");
```

#### 🟠 [HIGH] `expect()` @ L216:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("delete");
```

#### 🟠 [HIGH] `expect()` @ L229:C45

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let mut conn = pool.acquire().await.expect("acquire");
```

#### 🟠 [HIGH] `expect()` @ L242:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("insert");
```

### `packages\sz-orm-postgis\src\extensions.rs`

#### 🟠 [HIGH] `unwrap()` @ L937:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
reg.register(def).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L945:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
reg.register(def).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L955:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
reg.register(def).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L956:C62

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let removed = reg.unregister("idx_cities_geom_gist").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L965:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L967:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L969:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L979:C61

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
reg.register(SpatialIndexDef::new_gist("t1", "g1")).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L980:C61

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
reg.register(SpatialIndexDef::new_gist("t2", "g2")).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L993:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(rel.st_disjoint(&p1, &p2).unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L1001:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!rel.st_disjoint(&p1, &p2).unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L1015:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!rel.st_disjoint(&inside, &poly).unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L1016:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(rel.st_disjoint(&outside, &poly).unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L1024:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(rel.st_equals(&p1, &p2).unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L1032:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!rel.st_equals(&p1, &p2).unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L1054:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(rel.st_covers(&poly, &p).unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L1067:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!rel.st_covers(&poly, &p).unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L1080:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(rel.st_covered_by(&p, &poly).unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L1092:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = agg.st_collect(&pts).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1103:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = agg.st_collect(&pts).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1123:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let envelope = agg.st_envelope(&poly).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1125:C76

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let (min_x, min_y, max_x, max_y) = compute_bounding_box(&envelope).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1137:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let envelope = agg.st_envelope(&p).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1150:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let centroid = agg.st_centroid(&p).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1164:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let centroid = agg.st_centroid(&poly).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1182:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let hull = agg.st_convex_hull(&poly).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1215:C67

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let transformed = ct.st_transform(&p, srid::WEB_MERCATOR).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1234:C71

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mercator = ct.st_transform(&original, srid::WEB_MERCATOR).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1235:C60

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let back = ct.st_transform(&mercator, srid::WGS84).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1251:C60

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let transformed = ct.st_transform(&p, srid::WGS84).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1276:C68

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let transformed = ct.st_transform(&ls, srid::WEB_MERCATOR).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1336:C72

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let (min_x, min_y, max_x, max_y) = compute_bounding_box(&poly).unwrap();
```

### `packages\sz-orm-postgis\src\memory.rs`

#### 🟠 [HIGH] `unwrap()` @ L180:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dist = pg.st_distance(&p1, &p2).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L195:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(pg.st_contains(&poly, &inside).await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L196:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!pg.st_contains(&poly, &outside).await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L208:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let area = pg.st_area(&poly).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L220:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let len = pg.st_length(&ls).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L232:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let buffer = pg.st_buffer(&p, 1_000.0).await.unwrap(); // 1km
```

#### 🟠 [HIGH] `unwrap()` @ L246:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = pg.st_union(&p1, &p2).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L284:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L285:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
pg.create_spatial_index("t", "geom").await.unwrap();
```

### `packages\sz-orm-postgis\src\postgis.rs`

#### 🟠 [HIGH] `expect()` @ L247:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("build memory failed");
```

#### 🟠 [HIGH] `expect()` @ L253:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("distance failed");
```

#### 🟠 [HIGH] `expect()` @ L266:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("build stub failed");
```

#### 🟠 [HIGH] `expect()` @ L273:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("stub distance failed");
```

### `packages\sz-orm-postgis\src\real_postgis.rs`

#### 🟠 [HIGH] `unwrap()` @ L166:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let first = chars.next().unwrap();
```

#### 🟠 [HIGH] `expect()` @ L355:C46

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let _real = RealPostgis::new(config).expect("new() should not connect");
```

### `packages\sz-orm-postgis\src\stub.rs`

#### 🟠 [HIGH] `unwrap()` @ L160:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dist = stub.st_distance(&p1, &p2).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L172:C59

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
stub.create_spatial_index("cities", "geom").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L185:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L199:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let area = stub.st_area(&p).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L211:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let area = stub.st_area(&p).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L213:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let length = stub.st_length(&p).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L215:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
stub.create_spatial_index("t", "g").await.unwrap();
```

### `packages\sz-orm-postgis\tests\integration.rs`

#### 🟠 [HIGH] `expect()` @ L21:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("build failed");
```

#### 🟠 [HIGH] `expect()` @ L47:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("build failed");
```

#### 🟠 [HIGH] `unwrap()` @ L59:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(wrapper.st_contains(&poly, &beijing).await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L63:C58

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!wrapper.st_contains(&poly, &shanghai).await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L66:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(wrapper.st_within(&beijing, &poly).await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L67:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!wrapper.st_within(&shanghai, &poly).await.unwrap());
```

#### 🟠 [HIGH] `expect()` @ L74:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("build failed");
```

#### 🟠 [HIGH] `unwrap()` @ L82:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let length = wrapper.st_length(&ls).await.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L91:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("build failed");
```

#### 🟠 [HIGH] `expect()` @ L97:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("buffer failed");
```

#### 🟠 [HIGH] `expect()` @ L114:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("build failed");
```

#### 🟠 [HIGH] `unwrap()` @ L125:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L129:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L150:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("build failed");
```

#### 🟠 [HIGH] `expect()` @ L171:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("build failed");
```

#### 🟠 [HIGH] `unwrap()` @ L191:C59

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(wrapper.st_contains(&poly, &p_in_outer).await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L195:C59

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!wrapper.st_contains(&poly, &p_in_hole).await.unwrap());
```

#### 🟠 [HIGH] `expect()` @ L202:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("build failed");
```

#### 🟠 [HIGH] `unwrap()` @ L210:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let area = wrapper.st_area(&poly).await.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L218:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("build failed");
```

#### 🟠 [HIGH] `unwrap()` @ L222:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let union = wrapper.st_union(&p1, &p2).await.unwrap();
```

### `packages\sz-orm-queue\src\activemq.rs`

#### 🟠 [HIGH] `unwrap()` @ L64:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L68:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `expect()` @ L69:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("message should exist");
```

#### 🟠 [HIGH] `unwrap()` @ L71:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.ack(&msg.id).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L77:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(queue.consume("empty").await.unwrap().is_none());
```

#### 🟠 [HIGH] `unwrap()` @ L83:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.subscribe("active-topic").await.unwrap();
```

### `packages\sz-orm-queue\src\kafka.rs`

#### 🟠 [HIGH] `unwrap()` @ L61:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.publish("topic-a", b"hello kafka").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L67:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `expect()` @ L68:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("message should exist");
```

#### 🟠 [HIGH] `unwrap()` @ L73:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.ack(&msg.id).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L80:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = queue.consume("empty-topic").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L94:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.subscribe("topic-a").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L95:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.subscribe("topic-a").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L96:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.subscribe("topic-b").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L104:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.publish("topic-a", b"first").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L105:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.publish("topic-a", b"second").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L106:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.publish("topic-a", b"third").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L108:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let m1 = queue.consume("topic-a").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L108:C58

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let m1 = queue.consume("topic-a").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L109:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let m2 = queue.consume("topic-a").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L109:C58

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let m2 = queue.consume("topic-a").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L110:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let m3 = queue.consume("topic-a").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L110:C58

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let m3 = queue.consume("topic-a").await.unwrap().unwrap();
```

### `packages\sz-orm-queue\src\lapin_rabbitmq.rs`

#### 🟠 [HIGH] `unwrap()` @ L242:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
q.connect().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L246:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
q.subscribe(topic).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L247:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
q.publish(topic, b"hello rabbitmq").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L252:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `expect()` @ L253:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("message should exist");
```

#### 🟠 [HIGH] `unwrap()` @ L256:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
q.ack(&msg.id).await.unwrap();
```

### `packages\sz-orm-queue\src\lib.rs`

#### 🟠 [HIGH] `unwrap()` @ L132:C87

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let msg = Message::json_message("test", &serde_json::json!({"key": "value"})).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L133:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parsed: serde_json::Value = msg.json().unwrap();
```

#### 🟠 [HIGH] `expect()` @ L168:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("consume 不应报错")
```

#### 🟠 [HIGH] `expect()` @ L169:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("消息应存在");
```

#### 🟠 [HIGH] `unwrap()` @ L226:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.publish("orders", b"order-1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L232:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `expect()` @ L233:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("message should exist");
```

#### 🟠 [HIGH] `unwrap()` @ L243:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = queue.consume("empty-topic").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L250:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.publish("topic", b"data").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L251:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let msg = queue.consume("topic").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L251:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let msg = queue.consume("topic").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L254:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.ack(&msg.id).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L268:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.subscribe("topic-a").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L269:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.subscribe("topic-a").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L270:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.subscribe("topic-b").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L279:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.publish("topic-a", b"a1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L280:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.publish("topic-b", b"b1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L281:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.publish("topic-a", b"a2").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L290:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.publish("topic", b"first").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L291:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.publish("topic", b"second").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L292:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.publish("topic", b"third").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L294:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let m1 = queue.consume("topic").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L294:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let m1 = queue.consume("topic").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L295:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let m2 = queue.consume("topic").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L295:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let m2 = queue.consume("topic").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L296:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let m3 = queue.consume("topic").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L296:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let m3 = queue.consume("topic").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L305:C60

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
wrapper.publish("wrapper-topic", b"payload").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L310:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `expect()` @ L311:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("message should exist");
```

#### 🟠 [HIGH] `unwrap()` @ L313:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
wrapper.ack(&msg.id).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L319:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
wrapper.subscribe("sub-topic").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L320:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
wrapper.publish("sub-topic", b"hello").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L325:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `expect()` @ L326:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("message should exist");
```

#### 🟠 [HIGH] `unwrap()` @ L334:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = wrapper.consume("empty").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L357:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
wrapper.publish("topic", b"data").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L361:C18

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `expect()` @ L362:C18

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("message should exist");
```

#### 🟠 [HIGH] `unwrap()` @ L364:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
wrapper.ack(&msg.id).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L382:C18

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L394:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let _ = queue.consume("limited").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L394:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let _ = queue.consume("limited").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L395:C58

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.publish("limited", b"after-consume").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L407:C18

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L416:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.publish("topic-a", b"a1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L417:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.publish("topic-a", b"a2").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L421:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.publish("topic-b", b"b1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L422:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.publish("topic-b", b"b2").await.unwrap();
```

### `packages\sz-orm-queue\src\nats.rs`

#### 🟠 [HIGH] `unwrap()` @ L61:C60

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.publish("nats.subject", b"hello nats").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L65:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `expect()` @ L66:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("message should exist");
```

#### 🟠 [HIGH] `unwrap()` @ L68:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.ack(&msg.id).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L74:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(queue.consume("empty").await.unwrap().is_none());
```

#### 🟠 [HIGH] `unwrap()` @ L80:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.subscribe("nats.subject").await.unwrap();
```

### `packages\sz-orm-queue\src\pulsar.rs`

#### 🟠 [HIGH] `unwrap()` @ L64:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L68:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `expect()` @ L69:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("message should exist");
```

#### 🟠 [HIGH] `unwrap()` @ L71:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.ack(&msg.id).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L77:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(queue.consume("empty").await.unwrap().is_none());
```

#### 🟠 [HIGH] `unwrap()` @ L83:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.subscribe("pulsar-topic").await.unwrap();
```

### `packages\sz-orm-queue\src\queue.rs`

#### 🟠 [HIGH] `expect()` @ L535:C46

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let mut msg = dq.remove(idx).expect("checked: idx exists");
```

#### 🟠 [HIGH] `unwrap()` @ L874:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.publish("topic1", b"hello").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L878:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `expect()` @ L879:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("msg should exist");
```

#### 🟠 [HIGH] `unwrap()` @ L881:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.ack(&msg.id).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L928:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.publish("topic", b"msg1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L929:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let msg = queue.consume("topic").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L929:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let msg = queue.consume("topic").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L933:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.nack(&msg.id).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L938:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let msg2 = queue.consume("topic").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L938:C58

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let msg2 = queue.consume("topic").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L954:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.publish("topic", b"data").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L957:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let msg = queue.consume("topic").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L957:C61

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let msg = queue.consume("topic").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L962:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.nack(&msg.id).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L974:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.publish("topic", b"payload").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L977:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let msg = queue.consume("topic").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L977:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let msg = queue.consume("topic").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L978:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.nack(&msg.id).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L983:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let msg = queue.consume("topic").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L983:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let msg = queue.consume("topic").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L985:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.nack(&msg.id).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L990:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let msg = queue.consume("topic").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L990:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let msg = queue.consume("topic").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L992:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.nack(&msg.id).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1001:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.publish("topic", b"msg").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1002:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let msg = queue.consume("topic").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1002:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let msg = queue.consume("topic").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1006:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.nack(&msg.id).await.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L1015:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("should have dead letter");
```

#### 🟠 [HIGH] `unwrap()` @ L1037:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.publish("topic", b"bad-msg").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1038:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let msg = queue.consume("topic").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1038:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let msg = queue.consume("topic").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1040:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.reject(&msg.id).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1051:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.publish("topic", b"msg").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1052:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let msg = queue.consume("topic").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1052:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let msg = queue.consume("topic").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1055:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.reject(&msg.id).await.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L1060:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("should have dead letter");
```

#### 🟠 [HIGH] `unwrap()` @ L1096:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.publish("topic", b"m1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1097:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.publish("topic", b"m2").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1099:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let m1 = queue.consume("topic").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1099:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let m1 = queue.consume("topic").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1100:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.reject(&m1.id).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1103:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let m2 = queue.consume("topic").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1103:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let m2 = queue.consume("topic").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1104:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.reject(&m2.id).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1112:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.publish("topic", b"first").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1113:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.publish("topic", b"second").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1115:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let m1 = queue.consume("topic").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1115:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let m1 = queue.consume("topic").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1116:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.reject(&m1.id).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1117:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let m2 = queue.consume("topic").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1117:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let m2 = queue.consume("topic").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1118:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.reject(&m2.id).await.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L1124:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("should have dead letter");
```

#### 🟠 [HIGH] `expect()` @ L1129:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("should have dead letter");
```

#### 🟠 [HIGH] `unwrap()` @ L1147:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.publish("topic", b"msg").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1150:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let m1 = queue.consume("topic").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1150:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let m1 = queue.consume("topic").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1151:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.nack(&m1.id).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1153:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let m2 = queue.consume("topic").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1153:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let m2 = queue.consume("topic").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1155:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.nack(&m2.id).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1161:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.requeue_dead_letter(&m1.id).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1166:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let m3 = queue.consume("topic").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1166:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let m3 = queue.consume("topic").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1388:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.publish("topic", b"m1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1389:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.publish("topic", b"m2").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1391:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.publish("topic", b"m3").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1396:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let m1 = queue.consume("topic").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1396:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let m1 = queue.consume("topic").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1398:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let m2 = queue.consume("topic").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1398:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let m2 = queue.consume("topic").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1410:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.publish("topic", b"m1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1417:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let m = queue.consume("topic").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1417:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let m = queue.consume("topic").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1429:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.publish("topic", b"m1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1444:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.publish("topic", b"m1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1455:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.consume("topic").await.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L1460:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("publish should complete after consume");
```

#### 🟠 [HIGH] `unwrap()` @ L1465:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let m = queue.consume("topic").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1465:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let m = queue.consume("topic").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1476:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.publish("topic", b"m1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1496:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.publish("topic-a", b"a1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1497:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.publish("topic-b", b"b1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1508:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.consume("topic-b").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1516:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.consume("topic-a").await.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L1519:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("publish should complete after topic-a consume");
```

### `packages\sz-orm-queue\src\rabbitmq.rs`

#### 🟠 [HIGH] `unwrap()` @ L61:C59

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.publish("amq-topic", b"hello rabbit").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L67:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `expect()` @ L68:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("message should exist");
```

#### 🟠 [HIGH] `unwrap()` @ L72:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.ack(&msg.id).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L79:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = queue.consume("empty").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L92:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.subscribe("amq-topic").await.unwrap();
```

### `packages\sz-orm-queue\src\real_activemq.rs`

#### 🟠 [HIGH] `unwrap()` @ L263:C31

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.connect().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L266:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.subscribe("test-queue").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L272:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L278:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `expect()` @ L279:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("message should exist");
```

#### 🟠 [HIGH] `unwrap()` @ L284:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.ack(&msg.id).await.unwrap();
```

### `packages\sz-orm-queue\src\real_kafka.rs`

#### 🟠 [HIGH] `unwrap()` @ L288:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.connect_producer().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L289:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.connect_consumer().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L292:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.subscribe("test-topic").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L298:C59

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.publish("test-topic", b"hello kafka").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L307:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `expect()` @ L308:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("message should exist");
```

#### 🟠 [HIGH] `unwrap()` @ L313:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.ack(&msg.id).await.unwrap();
```

### `packages\sz-orm-queue\src\real_nats.rs`

#### 🟠 [HIGH] `unwrap()` @ L247:C31

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.connect().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L250:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.subscribe("test-subject").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L253:C60

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.publish("test-subject", b"hello nats").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L259:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `expect()` @ L260:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("message should exist");
```

#### 🟠 [HIGH] `unwrap()` @ L265:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.ack(&msg.id).await.unwrap();
```

### `packages\sz-orm-queue\src\real_pulsar.rs`

#### 🟠 [HIGH] `unwrap()` @ L293:C31

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.connect().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L296:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.subscribe("test-topic").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L299:C60

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.publish("test-topic", b"hello pulsar").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L305:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `expect()` @ L306:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("message should exist");
```

#### 🟠 [HIGH] `unwrap()` @ L311:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.ack(&msg.id).await.unwrap();
```

### `packages\sz-orm-queue\src\redis_inmemory.rs`

#### 🟠 [HIGH] `unwrap()` @ L76:C60

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.publish("redis-topic", b"hello redis").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L80:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `expect()` @ L81:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("message should exist");
```

#### 🟠 [HIGH] `unwrap()` @ L83:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.ack(&msg.id).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L89:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(queue.consume("empty").await.unwrap().is_none());
```

#### 🟠 [HIGH] `unwrap()` @ L95:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.subscribe("redis-topic").await.unwrap();
```

### `packages\sz-orm-queue\src\redis_provider.rs`

#### 🟠 [HIGH] `unwrap()` @ L387:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L388:C59

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.publish("sz-orm-list", b"hello-list").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L392:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `expect()` @ L393:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("应有消息");
```

#### 🟠 [HIGH] `unwrap()` @ L396:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.ack(&msg.id).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L407:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L409:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.subscribe("sz-orm-pubsub").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L414:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L418:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `expect()` @ L419:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("应有消息");
```

#### 🟠 [HIGH] `unwrap()` @ L431:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L440:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L444:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `expect()` @ L445:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("应有消息");
```

#### 🟠 [HIGH] `unwrap()` @ L448:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.ack(&msg.id).await.unwrap();
```

### `packages\sz-orm-queue\src\rocketmq.rs`

#### 🟠 [HIGH] `unwrap()` @ L64:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L68:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `expect()` @ L69:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("message should exist");
```

#### 🟠 [HIGH] `unwrap()` @ L71:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.ack(&msg.id).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L77:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(queue.consume("empty").await.unwrap().is_none());
```

#### 🟠 [HIGH] `unwrap()` @ L83:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.subscribe("rocket-topic").await.unwrap();
```

### `packages\sz-orm-queue\src\rocketmq_provider.rs`

#### 🟠 [HIGH] `unwrap()` @ L202:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let json = serde_json::to_value(&req).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L212:C66

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let resp: ConsumeResponse = serde_json::from_value(json).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L223:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L227:C32

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
q.subscribe("t").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L248:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L249:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
q.publish("test-topic", b"hello-rocket").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L253:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `expect()` @ L254:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("应有消息");
```

#### 🟠 [HIGH] `unwrap()` @ L256:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
q.ack(&msg.id).await.unwrap();
```

### `packages\sz-orm-queue\tests\stress.rs`

#### 🟠 [HIGH] `unwrap()` @ L27:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L36:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `expect()` @ L37:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("msg must exist");
```

#### 🟠 [HIGH] `unwrap()` @ L45:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.ack(&msg.id).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L72:C61

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
q.publish(&topic, payload.as_bytes()).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L76:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let msg = q.consume(&topic).await.unwrap().expect("msg must exist");
```

#### 🟠 [HIGH] `expect()` @ L76:C60

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let msg = q.consume(&topic).await.unwrap().expect("msg must exist");
```

#### 🟠 [HIGH] `unwrap()` @ L84:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
q.ack(&msg.id).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L91:C17

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
h.await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L130:C18

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L154:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.publish("large", &payload).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L159:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let msg = queue.consume("large").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L159:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let msg = queue.consume("large").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L161:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.ack(&msg.id).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L186:C67

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
wrapper.publish("test", payload.as_bytes()).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L187:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let msg = wrapper.consume("test").await.unwrap().expect("msg");
```

#### 🟠 [HIGH] `expect()` @ L187:C66

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let msg = wrapper.consume("test").await.unwrap().expect("msg");
```

#### 🟠 [HIGH] `unwrap()` @ L189:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
wrapper.ack(&msg.id).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L195:C17

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
h.await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L206:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
queue.subscribe("hot-topic").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L216:C61

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = queue.consume("never-published").await.unwrap();
```

### `packages\sz-orm-rw\src\lib.rs`

#### 🟠 [HIGH] `unwrap()` @ L716:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
router.acquire("s1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L717:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
router.acquire("s1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L718:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
router.acquire("s2").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L724:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
router.acquire("s3").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L734:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
router.acquire("s1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L735:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
router.acquire("s1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L736:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
router.acquire("s2").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L742:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
router.release("s1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L743:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
router.release("s1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L765:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
router.release("s1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L773:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
router.acquire("s1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L774:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
router.acquire("s1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L813:C22

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
h.join().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L963:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
router.set_weight("s1", 10).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L977:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
router.set_weight("s1", 0).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L985:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
router.set_weight("s1", 9).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L986:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
router.set_weight("s2", 1).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1023:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
router.set_weight("s1", 100).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1024:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
router.set_weight("s2", 1).unwrap();
```

### `packages\sz-orm-scheduler\src\advanced.rs`

#### 🟠 [HIGH] `unwrap()` @ L933:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let expr = EnhancedCronExpr::parse("0 * * * *").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L945:C62

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let expr = EnhancedCronExpr::parse("0 0 * * * 2025").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L954:C65

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let expr = EnhancedCronExpr::parse("0 0 12 * * 1 2025").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L968:C64

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let expr = EnhancedCronExpr::parse("0 0 0 1 1 * 2025").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L971:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `unwrap()` @ L978:C64

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let expr = EnhancedCronExpr::parse("0 0 0 1 1 * 2025").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L981:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `unwrap()` @ L988:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let expr = EnhancedCronExpr::parse("* * * * *").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L996:C61

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let expr = EnhancedCronExpr::parse("0 0 0 * * 1 *").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L999:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `unwrap()` @ L1007:C61

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let expr = EnhancedCronExpr::parse("0 0 0 * * 1 *").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1010:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `unwrap()` @ L1033:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
dag.add_task(DagTask::new("a", "Task A")).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1034:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
dag.add_task(DagTask::new("b", "Task B")).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1041:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
dag.add_task(DagTask::new("a", "Task A")).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1056:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
dag.add_task(DagTask::new("a", "A")).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1057:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
dag.add_task(DagTask::new("b", "B")).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1060:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let task_b = dag.get_task("b").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1067:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
dag.add_task(DagTask::new("a", "A")).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1074:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
dag.add_task(DagTask::new("a", "A")).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1081:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
dag.add_task(DagTask::new("a", "A")).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1083:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1085:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
dag.add_dependency("b", "a").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1086:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let task_b = dag.get_task("b").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1094:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
dag.add_task(DagTask::new("a", "A")).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1096:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1098:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1106:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
dag.add_task(DagTask::new("a", "A")).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1108:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1110:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1112:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
dag.add_dependency("a", "c").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1119:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
dag.add_task(DagTask::new("a", "A")).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1121:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1123:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1126:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1128:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let order = dag.topological_sort().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1130:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pos_a = order.iter().position(|x| x == "a").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1131:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pos_b = order.iter().position(|x| x == "b").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1132:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pos_c = order.iter().position(|x| x == "c").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1133:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pos_d = order.iter().position(|x| x == "d").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1143:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
dag.add_task(DagTask::new("a", "A")).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1145:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1147:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1149:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1151:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let order = dag.topological_sort().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1153:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pos_a = order.iter().position(|x| x == "a").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1154:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pos_b = order.iter().position(|x| x == "b").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1155:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pos_c = order.iter().position(|x| x == "c").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1156:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pos_d = order.iter().position(|x| x == "d").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1166:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
dag.add_task(DagTask::new("a", "A")).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1167:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
dag.add_task(DagTask::new("b", "B")).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1168:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
dag.add_dependency("a", "b").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1169:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
dag.add_dependency("b", "a").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1176:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
dag.add_task(DagTask::new("a", "A")).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1178:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1188:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
dag.add_task(DagTask::new("a", "A")).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1190:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1192:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1194:C33

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
dag.mark_completed("a").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1204:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
dag.add_task(DagTask::new("a", "A")).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1205:C31

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
dag.mark_running("a").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1206:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(dag.get_task("a").unwrap().running);
```

#### 🟠 [HIGH] `unwrap()` @ L1207:C33

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
dag.mark_completed("a").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1208:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!dag.get_task("a").unwrap().running);
```

#### 🟠 [HIGH] `unwrap()` @ L1209:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(dag.get_task("a").unwrap().completed);
```

#### 🟠 [HIGH] `unwrap()` @ L1215:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
dag.add_task(DagTask::new("a", "A")).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1216:C31

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
dag.mark_running("a").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1217:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
dag.mark_failed("a").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1218:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(dag.get_task("a").unwrap().failed);
```

#### 🟠 [HIGH] `unwrap()` @ L1219:C31

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
dag.reset_failed("a").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1220:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!dag.get_task("a").unwrap().failed);
```

#### 🟠 [HIGH] `unwrap()` @ L1221:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!dag.get_task("a").unwrap().completed);
```

#### 🟠 [HIGH] `unwrap()` @ L1227:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
dag.add_task(DagTask::new("a", "A")).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1229:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1231:C33

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
dag.mark_completed("a").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1233:C33

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
dag.mark_completed("b").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1240:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
dag.add_task(DagTask::new("a", "A")).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1242:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1244:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1492:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let stats1 = mgr.get_stats("task1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1494:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let stats2 = mgr.get_stats("task2").unwrap();
```

### `packages\sz-orm-scheduler\src\lib.rs`

#### 🟠 [HIGH] `unwrap()` @ L312:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L533:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L595:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let expr = result.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L664:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
scheduler.schedule(task).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L681:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
scheduler.schedule(task).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L683:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
scheduler.pause("task1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L687:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
scheduler.resume("task1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L697:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L700:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L712:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `unwrap()` @ L714:C63

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let next = scheduler.next_run_time("* * * * *", from).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L727:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `unwrap()` @ L729:C63

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let next = scheduler.next_run_time("0 * * * *", from).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L743:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `unwrap()` @ L757:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
scheduler.schedule(task).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L779:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
scheduler.schedule(task).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L796:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L797:C31

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
scheduler.pause("t1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L814:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L820:C29

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
scheduler.start(50).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L830:C26

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
scheduler.stop().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L848:C31

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
scheduler.start(1000).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L851:C26

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
scheduler.stop().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L865:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L868:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L871:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L895:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `unwrap()` @ L903:C25

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(next.unwrap(), from + chrono::Duration::seconds(30));
```

#### 🟠 [HIGH] `unwrap()` @ L912:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `unwrap()` @ L914:C64

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let next = scheduler.next_run_time("30 * * * *", from).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L924:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `unwrap()` @ L926:C67

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let next = scheduler.next_run_time("10-12 * * * *", from).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L936:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `unwrap()` @ L938:C70

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let next = scheduler.next_run_time("10,20,30 * * * *", from).unwrap();
```

### `packages\sz-orm-scheduler\src\scheduler.rs`

#### 🟠 [HIGH] `unwrap()` @ L108:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
handler.handle(&sample_task("t1")).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L109:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
handler.handle(&sample_task("t2")).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L116:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
handler.handle(&sample_task("a")).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L117:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
handler.handle(&sample_task("b")).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L118:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
handler.handle(&sample_task("c")).unwrap();
```

### `packages\sz-orm-scheduler\tests\stress.rs`

#### 🟠 [HIGH] `unwrap()` @ L23:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
scheduler.schedule(task).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L28:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
scheduler.cancel(&format!("task-{}", i)).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L43:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
scheduler.schedule(task).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L147:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
scheduler.schedule(task).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L152:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
scheduler.pause(&format!("task-{}", i)).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L161:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
scheduler.resume(&format!("task-{}", i)).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L183:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
scheduler.schedule(task).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L196:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
scheduler.schedule(task).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L218:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
scheduler.schedule(task).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L235:C25

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
s.start(10).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L237:C22

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
s.stop().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L239:C23

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
handle.join().unwrap();
```

### `packages\sz-orm-search\src\extensions.rs`

#### 🟠 [HIGH] `unwrap()` @ L983:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let arr = json.as_array().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1057:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
fs.create_index("docs", &json!({})).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1060:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1063:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1070:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1074:C71

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = fs.faceted_search("docs", &query, &facets).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1086:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1093:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1100:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
fs.create_index("docs", &json!({})).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1107:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1114:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1121:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1126:C71

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = fs.faceted_search("docs", &query, &facets).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1134:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1141:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
fs.create_index("docs", &json!({})).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1148:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1155:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1162:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1166:C71

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = fs.faceted_search("docs", &query, &facets).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1178:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
fs.create_index("docs", &json!({})).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1186:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1191:C71

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = fs.faceted_search("docs", &query, &facets).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1200:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
fs.create_index("docs", &json!({})).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1204:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1207:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1210:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1213:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1217:C71

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = fs.faceted_search("docs", &query, &facets).await.unwrap();
```

### `packages\sz-orm-search\src\memory.rs`

#### 🟠 [HIGH] `unwrap()` @ L222:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L226:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
s.delete_index("test").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L236:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L238:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
s.index_doc("test", "1", &doc).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L239:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let got = s.get_doc("test", "1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L241:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let missing = s.get_doc("test", "999").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L250:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L253:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L254:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
s.delete_doc("test", "1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L264:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L267:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L270:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L273:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L275:C73

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = s.search("docs", &SearchQuery::new("hello")).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L285:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L292:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L299:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L302:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = s.search("docs", &q).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L312:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L320:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L323:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = s.search("docs", &q).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L333:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L340:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L347:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L354:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L357:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = s.search("docs", &q).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L368:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L371:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L374:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L375:C70

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let count = s.count("docs", &SearchQuery::match_all()).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L377:C71

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let count = s.count("docs", &SearchQuery::new("hello")).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L386:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L392:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
s.bulk_index("docs", &docs).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L393:C70

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let count = s.count("docs", &SearchQuery::match_all()).await.unwrap();
```

### `packages\sz-orm-search\src\search.rs`

#### 🟠 [HIGH] `expect()` @ L263:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("build failed");
```

#### 🟠 [HIGH] `expect()` @ L267:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("create_index failed");
```

#### 🟠 [HIGH] `expect()` @ L274:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("build failed");
```

#### 🟠 [HIGH] `expect()` @ L278:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("create_index failed");
```

### `packages\sz-orm-search\src\stub.rs`

#### 🟠 [HIGH] `unwrap()` @ L111:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L122:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L131:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = stub.search("docs", &q).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L141:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
stub.delete_doc("docs", "1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L142:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
stub.delete_index("docs").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L152:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
stub.refresh("docs").await.unwrap();
```

### `packages\sz-orm-search\tests\integration.rs`

#### 🟠 [HIGH] `unwrap()` @ L28:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
s.create_index("docs", &json!({})).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L37:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L44:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L51:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L54:C72

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = s.search("docs", &SearchQuery::new("rust")).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L60:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = s.search("docs", &q).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L68:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
s.create_index("docs", &json!({})).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L71:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L74:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let doc = s.get_doc("docs", "1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L78:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let missing = s.get_doc("docs", "999").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L82:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
s.delete_doc("docs", "1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L83:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let after = s.get_doc("docs", "1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L110:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
s.create_index("docs", &json!({})).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L118:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
s.create_index("docs", &json!({})).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L119:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
s.delete_index("docs").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L128:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
s.create_index("docs", &json!({})).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L136:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L143:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = s.search("docs", &q).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L155:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = s.search("docs", &q).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L165:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
s.create_index("docs", &json!({})).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L168:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L175:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L178:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L180:C70

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let total = s.count("docs", &SearchQuery::match_all()).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L184:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let count = s.count("docs", &q).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L191:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
s.create_index("docs", &json!({})).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L195:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
s.bulk_index("docs", &docs).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L196:C70

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let count = s.count("docs", &SearchQuery::match_all()).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L203:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
s.create_index("docs", &json!({})).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L205:C33

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
s.refresh("docs").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L218:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L221:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L222:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
s.delete_doc("docs", "1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L223:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
s.delete_index("docs").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L239:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = s.search("docs", &q).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L249:C70

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let count = s.count("docs", &SearchQuery::new("test")).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L256:C33

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
s.refresh("docs").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L265:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
s.index_doc("docs", "1", &json!({})).await.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L279:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("build failed");
```

#### 🟠 [HIGH] `expect()` @ L283:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("create_index failed");
```

#### 🟠 [HIGH] `unwrap()` @ L284:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
wrapper.delete_index("test").await.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L291:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("build failed");
```

#### 🟠 [HIGH] `expect()` @ L295:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("create_index failed");
```

#### 🟠 [HIGH] `unwrap()` @ L329:C60

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(dsl["query"]["bool"]["must"].as_array().unwrap().len(), 1);
```

#### 🟠 [HIGH] `unwrap()` @ L330:C62

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(dsl["query"]["bool"]["filter"].as_array().unwrap().len(), 2);
```

#### 🟠 [HIGH] `unwrap()` @ L331:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(dsl["sort"].as_array().unwrap().len(), 2);
```

#### 🟠 [HIGH] `unwrap()` @ L352:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(params["filter"].as_str().unwrap().contains("status"));
```

#### 🟠 [HIGH] `unwrap()` @ L354:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(params["sort"].as_array().unwrap().len(), 1);
```

#### 🟠 [HIGH] `unwrap()` @ L361:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let filter = params["filter"].as_str().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L380:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = s.get_doc("ghost", "1").await.unwrap();
```

### `packages\sz-orm-sharding\src\cross_shard_tx.rs`

#### 🟠 [HIGH] `unwrap()` @ L411:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(*rollback_count.lock().unwrap(), 2);
```

#### 🟠 [HIGH] `unwrap()` @ L473:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
c0.lock().unwrap().push("s0");
```

#### 🟠 [HIGH] `unwrap()` @ L488:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
c1.lock().unwrap().push("s2");
```

#### 🟠 [HIGH] `unwrap()` @ L498:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let committed = committed.lock().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L555:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut g = a.lock().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L571:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(*attempts.lock().unwrap(), 3);
```

### `packages\sz-orm-sharding\src\enhanced.rs`

#### 🟠 [HIGH] `unwrap()` @ L180:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L573:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(r1.route(key).unwrap(), r2.route(key).unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L573:C62

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(r1.route(key).unwrap(), r2.route(key).unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L580:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let first = router.route("user:123").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L582:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(router.route("user:123").unwrap(), first);
```

#### 🟠 [HIGH] `unwrap()` @ L592:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let node = router.route(&key).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L654:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let before = router1.route(&key).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L655:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let after = router2.route(&key).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L682:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(router.route(key).unwrap(), "only");
```

#### 🟠 [HIGH] `unwrap()` @ L710:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(r.route("cn").unwrap(), "shard_cn");
```

#### 🟠 [HIGH] `unwrap()` @ L711:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(r.route("us").unwrap(), "shard_us");
```

#### 🟠 [HIGH] `unwrap()` @ L712:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(r.route("eu").unwrap(), "shard_eu");
```

#### 🟠 [HIGH] `unwrap()` @ L721:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(r.route("cn").unwrap(), "shard_cn");
```

#### 🟠 [HIGH] `unwrap()` @ L722:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(r.route("unknown").unwrap(), "shard_default");
```

#### 🟠 [HIGH] `unwrap()` @ L745:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(r.route("cn").unwrap(), "shard_cn_v2");
```

#### 🟠 [HIGH] `unwrap()` @ L790:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = r.route("cn", "user:123").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L792:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = r.route("us", "user:456").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L800:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r1 = r.route("cn", "user:123").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L801:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r2 = r.route("cn", "user:123").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L821:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = r.route("unknown", "key").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L839:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = r.route("g1", "key").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L848:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(r.route("g", "k").unwrap(), "s0");
```

#### 🟠 [HIGH] `unwrap()` @ L900:C31

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(r.route(0).unwrap(), "s0");
```

#### 🟠 [HIGH] `unwrap()` @ L901:C33

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(r.route(999).unwrap(), "s0");
```

#### 🟠 [HIGH] `unwrap()` @ L902:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(r.route(1000).unwrap(), "s1");
```

#### 🟠 [HIGH] `unwrap()` @ L903:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(r.route(1999).unwrap(), "s1");
```

#### 🟠 [HIGH] `unwrap()` @ L904:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(r.route(2000).unwrap(), "s2");
```

#### 🟠 [HIGH] `unwrap()` @ L905:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(r.route(2999).unwrap(), "s2");
```

#### 🟠 [HIGH] `unwrap()` @ L916:C33

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(r.route(500).unwrap(), "s0");
```

#### 🟠 [HIGH] `unwrap()` @ L950:C33

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(r.route(500).unwrap(), "s0");
```

#### 🟠 [HIGH] `unwrap()` @ L951:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(r.route(1500).unwrap(), "s1");
```

#### 🟠 [HIGH] `unwrap()` @ L952:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(r.route(2500).unwrap(), "s2");
```

#### 🟠 [HIGH] `unwrap()` @ L970:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(r.route(-500).unwrap(), "neg");
```

#### 🟠 [HIGH] `unwrap()` @ L971:C33

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(r.route(500).unwrap(), "pos");
```

#### 🟠 [HIGH] `unwrap()` @ L1009:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let cn_user = router.route("cn", "user:12345").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1012:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(router.route("cn", "user:12345").unwrap(), cn_user);
```

#### 🟠 [HIGH] `unwrap()` @ L1016:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let us_user = router.route("us", "user:67890").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1019:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(router.route("us", "user:67890").unwrap(), us_user);
```

#### 🟠 [HIGH] `unwrap()` @ L1036:C59

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
before.insert(key.clone(), router.route(&key).unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L1047:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let new_shard = router.route(key).unwrap();
```

### `packages\sz-orm-sharding\src\lib.rs`

#### 🟠 [HIGH] `unwrap()` @ L410:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(router.route("any_key").unwrap(), "only");
```

#### 🟠 [HIGH] `unwrap()` @ L411:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(router.route("different").unwrap(), "only");
```

#### 🟠 [HIGH] `unwrap()` @ L414:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(router.route("any_key").unwrap(), "only");
```

#### 🟠 [HIGH] `unwrap()` @ L417:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(router.route("2026-07-18").unwrap(), "only");
```

#### 🟠 [HIGH] `unwrap()` @ L426:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let first = router.route("user:123").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L429:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
router.route("user:123").unwrap(),
```

#### 🟠 [HIGH] `unwrap()` @ L443:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
shards_hit.insert(router.route(&key).unwrap().to_string());
```

#### 🟠 [HIGH] `unwrap()` @ L455:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r1 = router.route("consistent_key").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L456:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r2 = router.route("consistent_key").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L457:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r3 = router.route("consistent_key").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L465:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let shard = router.route("").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L477:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(router.route("Hello").unwrap(), "s0");
```

#### 🟠 [HIGH] `unwrap()` @ L478:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(router.route("world").unwrap(), "s0");
```

#### 🟠 [HIGH] `unwrap()` @ L479:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(router.route("A").unwrap(), "s0");
```

#### 🟠 [HIGH] `unwrap()` @ L480:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(router.route("a").unwrap(), "s0");
```

#### 🟠 [HIGH] `unwrap()` @ L482:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(router.route("你好").unwrap(), "s1");
```

#### 🟠 [HIGH] `unwrap()` @ L483:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(router.route("é").unwrap(), "s1");
```

#### 🟠 [HIGH] `unwrap()` @ L492:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
shards_hit.insert(router.route("A").unwrap().to_string());
```

#### 🟠 [HIGH] `unwrap()` @ L494:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
shards_hit.insert(router.route("a").unwrap().to_string());
```

#### 🟠 [HIGH] `unwrap()` @ L496:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
shards_hit.insert(router.route("é").unwrap().to_string());
```

#### 🟠 [HIGH] `unwrap()` @ L508:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let first = router.route("hello").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L509:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(router.route("hello").unwrap(), first);
```

#### 🟠 [HIGH] `unwrap()` @ L510:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(router.route("hello").unwrap(), first);
```

#### 🟠 [HIGH] `unwrap()` @ L517:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(router.route("").unwrap(), "s0");
```

#### 🟠 [HIGH] `unwrap()` @ L524:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let shard1 = router.route("user:123").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L525:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let shard2 = router.route("user:456").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L526:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let shard3 = router.route("user:789").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L538:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(router.route("2026-07-01").unwrap(), "s0");
```

#### 🟠 [HIGH] `unwrap()` @ L540:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(router.route("2026-07-02").unwrap(), "s1");
```

#### 🟠 [HIGH] `unwrap()` @ L542:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(router.route("2026-07-03").unwrap(), "s2");
```

#### 🟠 [HIGH] `unwrap()` @ L544:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(router.route("2026-07-04").unwrap(), "s0");
```

#### 🟠 [HIGH] `unwrap()` @ L553:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
shards_hit.insert(router.route(&key).unwrap().to_string());
```

#### 🟠 [HIGH] `unwrap()` @ L568:C60

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let shard1 = router.route("log:2026-07-15:entry1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L569:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let shard2 = router.route("2026-07-15").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L576:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let first = router.route("2026-07-18").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L577:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(router.route("2026-07-18").unwrap(), first);
```

#### 🟠 [HIGH] `unwrap()` @ L584:C60

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let shard = router.route("plain_key_without_date").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L587:C59

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(router.route("plain_key_without_date").unwrap(), shard);
```

#### 🟠 [HIGH] `unwrap()` @ L594:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let july_15 = router.route("2026-07-15").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L595:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let aug_15 = router.route("2026-08-15").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L604:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let shard = router.route("2026-00-00").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L617:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let hash_shard = hash_router.route(key).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L618:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let date_shard = date_router.route(key).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L665:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(router.route("cn").unwrap(), "shard_cn");
```

#### 🟠 [HIGH] `unwrap()` @ L666:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(router.route("us").unwrap(), "shard_us");
```

#### 🟠 [HIGH] `unwrap()` @ L667:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(router.route("eu").unwrap(), "shard_eu");
```

#### 🟠 [HIGH] `unwrap()` @ L675:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(router.route("unknown").unwrap(), "shard_default");
```

#### 🟠 [HIGH] `unwrap()` @ L677:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(router.route("cn").unwrap(), "shard_cn");
```

#### 🟠 [HIGH] `unwrap()` @ L697:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r1 = router.route("k1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L698:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r2 = router.route("k1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L700:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(router.route("k2").unwrap(), "s_def");
```

#### 🟠 [HIGH] `unwrap()` @ L712:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(router.route("vip1").unwrap(), "vip_shard");
```

#### 🟠 [HIGH] `unwrap()` @ L713:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(router.route("vip2").unwrap(), "vip_shard");
```

#### 🟠 [HIGH] `unwrap()` @ L714:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(router.route("vip3").unwrap(), "vip_shard");
```

#### 🟠 [HIGH] `unwrap()` @ L726:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(router.route("any_non_listed").unwrap(), "normal_shard");
```

#### 🟠 [HIGH] `unwrap()` @ L745:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(router.route("gold").unwrap(), "premium_shard");
```

#### 🟠 [HIGH] `unwrap()` @ L746:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(router.route("silver").unwrap(), "standard_shard");
```

#### 🟠 [HIGH] `unwrap()` @ L757:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(router.route("user:1").unwrap(), "dir_shard_a");
```

#### 🟠 [HIGH] `unwrap()` @ L758:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(router.route("user:2").unwrap(), "dir_shard_b");
```

#### 🟠 [HIGH] `unwrap()` @ L775:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r1 = router.route("k").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L776:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r2 = router.route("k").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L793:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = router.route("user:123").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L809:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r1 = router.route("user:123").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L811:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(router.route("user:123").unwrap(), r1);
```

#### 🟠 [HIGH] `unwrap()` @ L833:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let ra = router.route("a").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L834:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let rb = router.route("b").unwrap();
```

### `packages\sz-orm-sharding\src\routing.rs`

#### 🟠 [HIGH] `unwrap()` @ L145:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let key = ext.extract(&"ignored").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L152:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let k1 = ext.extract(&42i32).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L153:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let k2 = ext.extract(&"other").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L163:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let key = ext.extract(&"ignored").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L173:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let key = ext.extract(&"x").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L182:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let key = ext.extract(&"ignored").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L192:C58

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = router.route_by_data(&"data", &ext).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L208:C58

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = router.route_by_data(&"data", &ext).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L221:C58

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = router.route_by_data(&"data", &ext).unwrap();
```

### `packages\sz-orm-sharding\src\scatter.rs`

#### 🟠 [HIGH] `unwrap()` @ L155:C73

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut shards: HashSet<String> = results.into_iter().map(|r| r.unwrap()).collect();
```

#### 🟠 [HIGH] `unwrap()` @ L175:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(results[0].as_ref().unwrap(), &"only");
```

#### 🟠 [HIGH] `unwrap()` @ L208:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let sum: i32 = results.into_iter().map(|r| r.unwrap()).sum();
```

#### 🟠 [HIGH] `unwrap()` @ L224:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let (shard, count) = r.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L253:C58

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let total: usize = results.into_iter().map(|r| r.unwrap()).sum();
```

#### 🟠 [HIGH] `unwrap()` @ L291:C33

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let (shard, ks) = r.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L315:C78

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let merged = ScatterGather::merge(results, |vs| Ok(vs.iter().sum())).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L330:C78

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let merged = ScatterGather::merge(results, |vs| Ok(vs.len() as i32)).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L337:C79

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let merged = ScatterGather::merge(results, |mut vs| Ok(vs.remove(0))).unwrap();
```

### `packages\sz-orm-sharding\tests\property.rs`

#### 🟠 [HIGH] `unwrap()` @ L27:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r1 = router.route(&key).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L28:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r2 = router.route(&key).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L29:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r3 = router.route(&key).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L64:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let before = router_before.route(key).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L65:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let after = router_after.route(key).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L126:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let node = router.route(&key).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L158:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r1 = router.route("g1", &key).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L159:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r2 = router.route("g1", &key).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L188:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r_composite = composite.route("g", key).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L189:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r_direct = direct.route(key).unwrap();
```

### `packages\sz-orm-sqlx\src\any_driver.rs`

#### 🟠 [HIGH] `unwrap()` @ L225:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
AnyBackend::from_dsn("sqlite::memory:").unwrap(),
```

#### 🟠 [HIGH] `unwrap()` @ L262:C62

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool = AnyPool::connect("sqlite::memory:").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L265:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = pool.create().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L271:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L274:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L277:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L283:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L291:C62

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool = AnyPool::connect("sqlite::memory:").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L292:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = pool.create().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L296:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L299:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.begin_transaction().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L302:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L305:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L306:C29

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.commit().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L308:C62

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let rows = conn.query("SELECT * FROM tx_test").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L314:C62

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool = AnyPool::connect("sqlite::memory:").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L315:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = pool.create().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L319:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L321:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.begin_transaction().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L324:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L325:C31

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.rollback().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L327:C60

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let rows = conn.query("SELECT * FROM tx_rb").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L333:C62

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool = AnyPool::connect("sqlite::memory:").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L334:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = pool.create().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L348:C62

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool = AnyPool::connect("sqlite::memory:").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L349:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = pool.create().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L353:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L357:C18

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L361:C58

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let rows = conn.query("SELECT * FROM cnt").await.unwrap();
```

### `packages\sz-orm-sqlx\src\enhanced.rs`

#### 🟠 [HIGH] `unwrap()` @ L723:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L735:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L745:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L798:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L809:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L819:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1021:C22

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
h.join().unwrap();
```

### `packages\sz-orm-sqlx\tests\benchmark.rs`

#### 🟠 [HIGH] `expect()` @ L31:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("SQLite connect failed"),
```

#### 🟠 [HIGH] `expect()` @ L39:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("PoolConfig invalid");
```

#### 🟠 [HIGH] `expect()` @ L40:C32

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
Pool::new(config, factory).expect("Pool::new failed")
```

#### 🟠 [HIGH] `expect()` @ L45:C41

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let mut conn = pool.acquire().await.expect("acquire");
```

#### 🟠 [HIGH] `expect()` @ L55:C6

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("CREATE TABLE");
```

#### 🟠 [HIGH] `expect()` @ L68:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("INSERT");
```

#### 🟠 [HIGH] `expect()` @ L74:C41

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let mut conn = pool.acquire().await.expect("acquire");
```

#### 🟠 [HIGH] `expect()` @ L95:C45

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let mut conn = pool.acquire().await.expect("acquire");
```

#### 🟠 [HIGH] `expect()` @ L100:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("query");
```

#### 🟠 [HIGH] `expect()` @ L113:C45

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let mut conn = pool.acquire().await.expect("acquire");
```

#### 🟠 [HIGH] `expect()` @ L115:C42

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let rows = conn.query(sql).await.expect("query");
```

#### 🟠 [HIGH] `expect()` @ L127:C45

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let mut conn = pool.acquire().await.expect("acquire");
```

#### 🟠 [HIGH] `expect()` @ L129:C69

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let (columns, values_matrix) = conn.query_values(sql).await.expect("query_values");
```

#### 🟠 [HIGH] `expect()` @ L146:C45

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let mut conn = pool.acquire().await.expect("acquire");
```

#### 🟠 [HIGH] `expect()` @ L151:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("query_values_with_params");
```

#### 🟠 [HIGH] `expect()` @ L168:C45

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let mut conn = pool.acquire().await.expect("acquire");
```

#### 🟠 [HIGH] `expect()` @ L173:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("update");
```

#### 🟠 [HIGH] `expect()` @ L186:C45

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let mut conn = pool.acquire().await.expect("acquire");
```

#### 🟠 [HIGH] `expect()` @ L190:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("delete");
```

#### 🟠 [HIGH] `expect()` @ L203:C45

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let mut conn = pool.acquire().await.expect("acquire");
```

#### 🟠 [HIGH] `expect()` @ L213:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("insert");
```

### `packages\sz-orm-sqlx\tests\differential_fuzz.rs`

#### 🟠 [HIGH] `expect()` @ L238:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("failed to create tokio runtime");
```

#### 🟠 [HIGH] `expect()` @ L242:C18

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("test DB setup failed");
```

#### 🟠 [HIGH] `expect()` @ L246:C18

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("differential test scenarios failed");
```

#### 🟠 [HIGH] `unwrap()` @ L260:C58

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let (mut conn, native_pool) = setup_db(&users).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L261:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
run_scenarios(&mut conn, &native_pool).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L267:C58

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let (mut conn, native_pool) = setup_db(&users).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L268:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
run_scenarios(&mut conn, &native_pool).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L276:C58

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let (mut conn, native_pool) = setup_db(&users).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L277:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
run_scenarios(&mut conn, &native_pool).await.unwrap();
```

### `packages\sz-orm-sqlx\tests\real_db_jepsen.rs`

#### 🟠 [HIGH] `unwrap()` @ L46:C77

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool_handle = Arc::new(MySqlPoolHandle::connect(&mysql_url()).await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L49:C59

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = pool_handle.pool().acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L56:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L63:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L70:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L79:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L80:C77

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool: &'static Pool = Box::leak(Box::new(Pool::new(config, factory).unwrap()));
```

#### 🟠 [HIGH] `unwrap()` @ L92:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.begin_transaction().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L96:C26

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L108:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L114:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L115:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.commit().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L118:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.rollback().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L121:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.rollback().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L129:C17

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
h.await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L133:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L137:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L152:C77

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool_handle = Arc::new(MySqlPoolHandle::connect(&mysql_url()).await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L155:C63

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let config = PoolConfigBuilder::new().max_size(3).build().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L156:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool = Pool::new(config, factory).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L158:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L164:C6

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L166:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.begin_transaction().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L174:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L177:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L180:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.execute("ROLLBACK TO SAVEPOINT sp_2").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L181:C25

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.commit().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L186:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L197:C77

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool_handle = Arc::new(MySqlPoolHandle::connect(&mysql_url()).await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L204:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L205:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool = Pool::new(config, factory).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L210:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conns.push(pool.acquire().await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L215:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
result.is_err() || result.unwrap().is_err(),
```

#### 🟠 [HIGH] `unwrap()` @ L223:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
pool.release(new_conn.unwrap()).await;
```

#### 🟠 [HIGH] `unwrap()` @ L232:C77

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool_handle = Arc::new(MySqlPoolHandle::connect(&mysql_url()).await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L235:C59

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = pool_handle.pool().acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L242:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L246:C63

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let config = PoolConfigBuilder::new().max_size(3).build().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L247:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool = Pool::new(config, factory).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L250:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L251:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.begin_transaction().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L260:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L262:C25

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.commit().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L267:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L275:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L286:C77

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool_handle = Arc::new(MySqlPoolHandle::connect(&mysql_url()).await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L289:C63

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let config = PoolConfigBuilder::new().max_size(3).build().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L290:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool = Pool::new(config, factory).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L292:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L298:C6

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L301:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L306:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L309:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L313:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L317:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L321:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L334:C71

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool_handle = Arc::new(PgPoolHandle::connect(&pg_url()).await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L337:C59

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = pool_handle.pool().acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L344:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L351:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L358:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L367:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L368:C77

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool: &'static Pool = Box::leak(Box::new(Pool::new(config, factory).unwrap()));
```

#### 🟠 [HIGH] `unwrap()` @ L378:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.begin_transaction().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L382:C26

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L394:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L400:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L401:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.commit().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L404:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.rollback().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L407:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.rollback().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L415:C17

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
h.await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L418:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L422:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L433:C71

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool_handle = Arc::new(PgPoolHandle::connect(&pg_url()).await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L436:C63

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let config = PoolConfigBuilder::new().max_size(3).build().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L437:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool = Pool::new(config, factory).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L439:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L445:C6

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L447:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.begin_transaction().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L454:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L457:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L459:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.execute("ROLLBACK TO SAVEPOINT sp_2").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L460:C25

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.commit().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L465:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L475:C71

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool_handle = Arc::new(PgPoolHandle::connect(&pg_url()).await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L482:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L483:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool = Pool::new(config, factory).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L487:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conns.push(pool.acquire().await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L491:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
result.is_err() || result.unwrap().is_err(),
```

#### 🟠 [HIGH] `unwrap()` @ L498:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
pool.release(new_conn.unwrap()).await;
```

#### 🟠 [HIGH] `unwrap()` @ L507:C71

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool_handle = Arc::new(PgPoolHandle::connect(&pg_url()).await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L510:C59

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = pool_handle.pool().acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L517:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L521:C63

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let config = PoolConfigBuilder::new().max_size(3).build().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L522:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool = Pool::new(config, factory).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L524:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L525:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.begin_transaction().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L534:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L536:C25

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.commit().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L541:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L548:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L559:C71

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool_handle = Arc::new(PgPoolHandle::connect(&pg_url()).await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L562:C63

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let config = PoolConfigBuilder::new().max_size(3).build().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L563:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool = Pool::new(config, factory).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L565:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L571:C6

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L574:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L578:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L581:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L585:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L589:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L593:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

### `packages\sz-orm-sqlx\tests\real_db_pool_tests.rs`

#### 🟠 [HIGH] `unwrap()` @ L40:C77

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool_handle = Arc::new(MySqlPoolHandle::connect(&mysql_url()).await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L47:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L48:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool = Pool::new(config, factory).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L52:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L54:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let rows = conn.query(&sql).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L70:C77

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool_handle = Arc::new(MySqlPoolHandle::connect(&mysql_url()).await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L73:C59

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = pool_handle.pool().acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L80:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L89:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L90:C77

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool: &'static Pool = Box::leak(Box::new(Pool::new(config, factory).unwrap()));
```

#### 🟠 [HIGH] `unwrap()` @ L111:C17

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
h.await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L120:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L128:C77

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool_handle = Arc::new(MySqlPoolHandle::connect(&mysql_url()).await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L131:C63

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let config = PoolConfigBuilder::new().max_size(3).build().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L132:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool = Pool::new(config, factory).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L134:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L140:C6

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L143:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.begin_transaction().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L149:C6

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L150:C25

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.commit().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L153:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.begin_transaction().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L159:C6

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L160:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.rollback().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L165:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L175:C77

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool_handle = Arc::new(MySqlPoolHandle::connect(&mysql_url()).await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L178:C63

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let config = PoolConfigBuilder::new().max_size(3).build().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L179:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool = Pool::new(config, factory).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L181:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L187:C6

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L189:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.begin_transaction().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L195:C6

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L196:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.execute("SAVEPOINT sp1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L202:C6

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L203:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.execute("ROLLBACK TO SAVEPOINT sp1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L204:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.execute("RELEASE SAVEPOINT sp1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L205:C25

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.commit().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L210:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L224:C77

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool_handle = Arc::new(MySqlPoolHandle::connect(&mysql_url()).await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L227:C59

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = pool_handle.pool().acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L234:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L243:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L244:C77

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool: &'static Pool = Box::leak(Box::new(Pool::new(config, factory).unwrap()));
```

#### 🟠 [HIGH] `unwrap()` @ L265:C17

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
h.await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L274:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L284:C71

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool_handle = Arc::new(PgPoolHandle::connect(&pg_url()).await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L291:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L292:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool = Pool::new(config, factory).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L295:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L297:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let rows = conn.query(&sql).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L308:C71

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool_handle = Arc::new(PgPoolHandle::connect(&pg_url()).await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L311:C59

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = pool_handle.pool().acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L318:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L327:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L328:C77

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool: &'static Pool = Box::leak(Box::new(Pool::new(config, factory).unwrap()));
```

#### 🟠 [HIGH] `unwrap()` @ L349:C17

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
h.await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L357:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L365:C71

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool_handle = Arc::new(PgPoolHandle::connect(&pg_url()).await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L368:C63

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let config = PoolConfigBuilder::new().max_size(3).build().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L369:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool = Pool::new(config, factory).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L371:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L377:C6

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L379:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.begin_transaction().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L385:C6

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L386:C25

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.commit().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L388:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.begin_transaction().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L394:C6

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L395:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.rollback().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L400:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L410:C71

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool_handle = Arc::new(PgPoolHandle::connect(&pg_url()).await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L413:C63

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let config = PoolConfigBuilder::new().max_size(3).build().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L414:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool = Pool::new(config, factory).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L416:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L422:C6

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L424:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.begin_transaction().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L430:C6

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L431:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.execute("SAVEPOINT sp1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L437:C6

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L438:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.execute("ROLLBACK TO SAVEPOINT sp1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L439:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.execute("RELEASE SAVEPOINT sp1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L440:C25

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.commit().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L445:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L455:C71

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool_handle = Arc::new(PgPoolHandle::connect(&pg_url()).await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L458:C59

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = pool_handle.pool().acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L465:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L474:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L475:C77

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool: &'static Pool = Box::leak(Box::new(Pool::new(config, factory).unwrap()));
```

#### 🟠 [HIGH] `unwrap()` @ L496:C17

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
h.await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L504:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L514:C77

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool_handle = Arc::new(MySqlPoolHandle::connect(&mysql_url()).await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L517:C59

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = pool_handle.pool().acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L524:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L528:C63

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let config = PoolConfigBuilder::new().max_size(5).build().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L529:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool = Pool::new(config, factory).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L532:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L533:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.begin_transaction().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L539:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.execute(&sql).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L541:C25

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.commit().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L547:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L560:C71

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool_handle = Arc::new(PgPoolHandle::connect(&pg_url()).await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L563:C59

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = pool_handle.pool().acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L570:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L574:C63

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let config = PoolConfigBuilder::new().max_size(5).build().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L575:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool = Pool::new(config, factory).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L578:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L579:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.begin_transaction().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L585:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.execute(&sql).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L587:C25

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.commit().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L593:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

### `packages\sz-orm-sqlx\tests\sqlx_adapter_tests.rs`

#### 🟠 [HIGH] `expect()` @ L12:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("sqlite memory connect failed");
```

#### 🟠 [HIGH] `unwrap()` @ L32:C21

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let conn = conn.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L39:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = factory.create().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L44:C23

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(result.unwrap(), 0, "DDL rows_affected should be 0");
```

#### 🟠 [HIGH] `unwrap()` @ L50:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = factory.create().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L53:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L56:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L59:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L64:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L76:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = factory.create().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L79:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L81:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.begin_transaction().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L84:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L85:C25

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.commit().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L87:C58

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let rows = conn.query("SELECT * FROM test_tc").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L94:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = factory.create().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L97:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L99:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.begin_transaction().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L102:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L103:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.rollback().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L105:C58

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let rows = conn.query("SELECT * FROM test_tr").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L112:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = factory.create().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L113:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.begin_transaction().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L116:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.rollback().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L122:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = factory.create().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L134:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = factory.create().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L145:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = factory.create().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L155:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = factory.create().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L156:C24

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.close().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L169:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = factory.create().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L172:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L174:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.begin_transaction().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L177:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L178:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.execute("SAVEPOINT sp1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L181:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L182:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.execute("ROLLBACK TO sp1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L183:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.execute("RELEASE sp1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L184:C25

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conn.commit().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L189:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L200:C74

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool_handle = SqlitePoolHandle::connect("sqlite::memory:").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L207:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L208:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool = sz_orm_core::Pool::new(config, factory).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L210:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L213:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L216:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L219:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn2 = pool.acquire().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L220:C61

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let rows = conn2.query("SELECT * FROM test_pool").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L228:C83

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let pool_handle = Arc::new(SqlitePoolHandle::connect("sqlite::memory:").await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L233:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = factory.create().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L236:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L245:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L247:C68

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
Box::leak(Box::new(sz_orm_core::Pool::new(config, factory).unwrap()));
```

#### 🟠 [HIGH] `unwrap()` @ L268:C17

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
h.await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L280:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = factory.create().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L283:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L286:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L289:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L294:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L303:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = factory.create().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L317:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = factory.create().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L320:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L333:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("execute_with_params INSERT should succeed");
```

#### 🟠 [HIGH] `unwrap()` @ L339:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L352:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = factory.create().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L355:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L358:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L361:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L364:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L373:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("query_with_params SELECT should succeed");
```

#### 🟠 [HIGH] `unwrap()` @ L382:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = factory.create().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L385:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L388:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L398:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("query_with_params with malicious input should not error");
```

#### 🟠 [HIGH] `unwrap()` @ L406:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = factory.create().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L409:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L412:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L415:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L424:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L431:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L434:C62

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let rows = conn.query("SELECT id, name FROM t_ud").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L443:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = factory.create().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L446:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L450:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("empty params should fall back to execute()");
```

#### 🟠 [HIGH] `unwrap()` @ L458:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut conn = factory.create().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L469:C6

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L484:C6

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("insert with various types should succeed");
```

#### 🟠 [HIGH] `unwrap()` @ L492:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

### `packages\sz-orm-storage\src\advanced.rs`

#### 🟠 [HIGH] `unwrap()` @ L846:C61

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let etag = upload.upload_part(1, b"hello".to_vec()).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L852:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let combined = upload.complete().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L862:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
upload.upload_part(1, vec![0u8; 10]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L863:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
upload.upload_part(2, vec![1u8; 10]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L864:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
upload.upload_part(3, vec![2u8; 5]).unwrap(); // 最后一个分片可以小于 part_size
```

#### 🟠 [HIGH] `unwrap()` @ L868:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let combined = upload.complete().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L877:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
upload.upload_part(1, vec![0u8; 10]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L896:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
upload.upload_part(1, b"data".to_vec()).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L897:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
upload.complete().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L906:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
upload.upload_part(1, vec![0u8; 10]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L907:C24

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
upload.abort().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L927:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
upload.upload_part(1, vec![0u8; 10]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L929:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
upload.upload_part(2, vec![0u8; 10]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L931:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
upload.upload_part(3, vec![0u8; 10]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L939:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
upload.upload_part(1, b"data".to_vec()).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L940:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
upload.complete().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L954:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L956:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let session = mgr.get_session(&upload_id).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L968:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L969:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
mgr.upload_part(&upload_id, 1, vec![0u8; 100]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L970:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
mgr.upload_part(&upload_id, 2, vec![1u8; 100]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L971:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let combined = mgr.complete(&upload_id).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L973:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let data = mgr.get_completed_data("file.bin").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L982:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L983:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
mgr.upload_part(&upload_id, 1, vec![0u8; 100]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L984:C31

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
mgr.abort(&upload_id).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L985:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let session = mgr.get_session(&upload_id).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L992:C60

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let id1 = mgr.initiate("a", "text/plain", 100, 50).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L993:C61

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let _id2 = mgr.initiate("b", "text/plain", 100, 50).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L996:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
mgr.upload_part(&id1, 1, vec![0u8; 50]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L997:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
mgr.upload_part(&id1, 2, vec![0u8; 50]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L998:C28

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
mgr.complete(&id1).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1026:C60

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let id1 = mgr.initiate("a", "text/plain", 100, 50).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1027:C60

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let id2 = mgr.initiate("b", "text/plain", 100, 50).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1029:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
mgr.upload_part(&id1, 1, vec![0u8; 50]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1030:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
mgr.upload_part(&id1, 2, vec![0u8; 50]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1031:C28

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
mgr.complete(&id1).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1032:C25

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
mgr.abort(&id2).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1040:C67

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let upload_id = mgr.initiate("f", "text/plain", 300, 100).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1041:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
mgr.upload_part(&upload_id, 1, vec![0u8; 100]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1042:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
mgr.upload_part(&upload_id, 2, vec![0u8; 100]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1043:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let snapshot = mgr.get_session(&upload_id).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1205:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1208:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let status = refresher.get_status(&id).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1217:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1218:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let request = refresher.get_request(&id).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1245:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1248:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1251:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1273:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1274:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(refresher.get_status(&id).unwrap(), RefreshStatus::Done);
```

#### 🟠 [HIGH] `unwrap()` @ L1289:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1292:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1295:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1306:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1309:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

### `packages\sz-orm-storage\src\aliyun.rs`

#### 🟠 [HIGH] `unwrap()` @ L87:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L90:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let data = storage.get("a.txt").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L105:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
storage.put("key", b"data", "text/plain").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L106:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(storage.exists("key").await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L108:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
storage.delete("key").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L109:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!storage.exists("key").await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L115:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
storage.put("key", b"v1", "text/plain").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L116:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
storage.put("key", b"v2", "text/plain").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L117:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(storage.get("key").await.unwrap(), b"v2");
```

#### 🟠 [HIGH] `unwrap()` @ L260:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let url = backend.as_ref().unwrap().url_for("a.txt");
```

#### 🟠 [HIGH] `unwrap()` @ L273:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L286:C86

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let backend = RealAliyunOss::new(&bucket, &region, &access_key, &secret_key).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L290:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
backend.put(key, data, "text/plain").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L291:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let fetched = backend.get(key).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L294:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(backend.exists(key).await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L296:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
backend.delete(key).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L297:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!backend.exists(key).await.unwrap());
```

### `packages\sz-orm-storage\src\huawei.rs`

#### 🟠 [HIGH] `unwrap()` @ L86:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L89:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let data = storage.get("a.txt").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L104:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
storage.put("key", b"data", "text/plain").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L105:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(storage.exists("key").await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L107:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
storage.delete("key").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L108:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!storage.exists("key").await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L114:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
storage.put("key", b"v1", "text/plain").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L115:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
storage.put("key", b"v2", "text/plain").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L116:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(storage.get("key").await.unwrap(), b"v2");
```

#### 🟠 [HIGH] `unwrap()` @ L122:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
storage.put("a", b"a-data", "text/plain").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L123:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
storage.put("b", b"b-data", "text/plain").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L125:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(storage.get("a").await.unwrap(), b"a-data");
```

#### 🟠 [HIGH] `unwrap()` @ L126:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(storage.get("b").await.unwrap(), b"b-data");
```

#### 🟠 [HIGH] `unwrap()` @ L128:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
storage.delete("a").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L129:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!storage.exists("a").await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L130:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(storage.exists("b").await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L271:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let url = backend.as_ref().unwrap().url_for("a.txt");
```

#### 🟠 [HIGH] `unwrap()` @ L284:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L297:C86

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let backend = RealHuaweiObs::new(&bucket, &region, &access_key, &secret_key).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L301:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
backend.put(key, data, "text/plain").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L302:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let fetched = backend.get(key).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L305:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(backend.exists(key).await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L307:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
backend.delete(key).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L308:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!backend.exists(key).await.unwrap());
```

### `packages\sz-orm-storage\src\lib.rs`

#### 🟠 [HIGH] `unwrap()` @ L172:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
storage.put(key, data, content_type).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L174:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let retrieved = storage.get(key).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L186:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
storage.put(key, b"test", "text/plain").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L188:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
storage.delete(key).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L190:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let exists = storage.exists(key).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L202:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!storage.exists(key).await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L204:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
storage.put(key, b"test", "text/plain").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L205:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(storage.exists(key).await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L226:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let wrapper = builder.build().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L238:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let wrapper = builder.build().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L250:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let wrapper = builder.build().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L260:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let wrapper = builder.build().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L269:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let wrapper = builder.build().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L279:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let wrapper = builder.build().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L288:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let wrapper = builder.build().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L298:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L303:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L306:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let data = wrapper.get("k.txt").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L309:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(wrapper.exists("k.txt").await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L310:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
wrapper.delete("k.txt").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L311:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!wrapper.exists("k.txt").await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L320:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L325:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L326:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(wrapper.get("k").await.unwrap(), b"aliyun-via-builder");
```

#### 🟠 [HIGH] `unwrap()` @ L335:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L340:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L341:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(wrapper.get("k").await.unwrap(), b"tencent-via-builder");
```

#### 🟠 [HIGH] `unwrap()` @ L342:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(wrapper.exists("k").await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L350:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L355:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L356:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(wrapper.get("k").await.unwrap(), b"qiniu-via-builder");
```

#### 🟠 [HIGH] `unwrap()` @ L365:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L370:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L371:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(wrapper.get("k").await.unwrap(), b"huawei-via-builder");
```

#### 🟠 [HIGH] `unwrap()` @ L379:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L384:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L385:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(wrapper.get("k").await.unwrap(), b"upyun-via-builder");
```

#### 🟠 [HIGH] `unwrap()` @ L396:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L410:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L411:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let data = wrapper.get("test.txt").await.unwrap();
```

### `packages\sz-orm-storage\src\local.rs`

#### 🟠 [HIGH] `unwrap()` @ L112:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L113:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let data = storage.get("file.txt").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L139:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L140:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(storage.exists("delete.txt").await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L142:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
storage.delete("delete.txt").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L143:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!storage.exists("delete.txt").await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L152:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!storage.exists("nope.txt").await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L164:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L165:C62

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let data = storage.get("nested/deep/file.txt").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L176:C71

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let url = storage.put("url.txt", b"data", "text/plain").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L191:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L195:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L196:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let data = storage.get("overwrite.txt").await.unwrap();
```

### `packages\sz-orm-storage\src\qiniu.rs`

#### 🟠 [HIGH] `unwrap()` @ L84:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L88:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let data = storage.get("file.txt").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L103:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
storage.put("key", b"data", "text/plain").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L104:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(storage.exists("key").await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L106:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
storage.delete("key").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L107:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!storage.exists("key").await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L113:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
storage.put("key", b"v1", "text/plain").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L114:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
storage.put("key", b"v2", "text/plain").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L115:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(storage.get("key").await.unwrap(), b"v2");
```

#### 🟠 [HIGH] `unwrap()` @ L612:C83

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let backend = RealQiniuKodo::new("bucket", "ak", "sk", "cdn.example.com").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L667:C95

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let backend = RealQiniuKodo::new(&bucket, &access_key, &secret_key, &download_domain).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L671:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
backend.put(key, data, "text/plain").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L672:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let fetched = backend.get(key).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L675:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(backend.exists(key).await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L677:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
backend.delete(key).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L678:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!backend.exists(key).await.unwrap());
```

### `packages\sz-orm-storage\src\s3.rs`

#### 🟠 [HIGH] `unwrap()` @ L87:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L91:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let data = storage.get("file.txt").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L106:C61

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
storage.put("del.txt", b"data", "text/plain").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L107:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(storage.exists("del.txt").await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L109:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
storage.delete("del.txt").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L110:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!storage.exists("del.txt").await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L116:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!storage.exists("nope").await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L122:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
storage.put("key", b"v1", "text/plain").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L123:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
storage.put("key", b"v2", "text/plain").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L124:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let data = storage.get("key").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L131:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
storage.put("a", b"a-data", "text/plain").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L132:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
storage.put("b", b"b-data", "text/plain").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L133:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
storage.put("c", b"c-data", "text/plain").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L135:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(storage.get("a").await.unwrap(), b"a-data");
```

#### 🟠 [HIGH] `unwrap()` @ L136:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(storage.get("b").await.unwrap(), b"b-data");
```

#### 🟠 [HIGH] `unwrap()` @ L137:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(storage.get("c").await.unwrap(), b"c-data");
```

#### 🟠 [HIGH] `unwrap()` @ L139:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
storage.delete("b").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L140:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(storage.exists("a").await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L141:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!storage.exists("b").await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L142:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(storage.exists("c").await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L288:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let url = backend.as_ref().unwrap().url_for("a.txt");
```

#### 🟠 [HIGH] `unwrap()` @ L301:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L318:C103

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let backend = S3Backend::with_endpoint(&bucket, &region, &endpoint, &access_key, &secret_key).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L322:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
backend.put(key, data, "text/plain").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L323:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let fetched = backend.get(key).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L326:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(backend.exists(key).await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L328:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
backend.delete(key).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L329:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!backend.exists(key).await.unwrap());
```

### `packages\sz-orm-storage\src\s3_compat.rs`

#### 🟠 [HIGH] `unwrap()` @ L618:C25

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let b = backend.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L635:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L649:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L663:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L690:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L695:C60

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
backend.put_object(&key, data, "text/plain").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L696:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let fetched = backend.get_object(&key).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L699:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(backend.head_object(&key).await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L701:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let listed = backend.list_objects(None).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L704:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
backend.delete_object(&key).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L705:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!backend.head_object(&key).await.unwrap());
```

### `packages\sz-orm-storage\src\s3_sdk.rs`

#### 🟠 [HIGH] `unwrap()` @ L164:C31

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let storage = storage.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L185:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L199:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L206:C85

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let storage = S3SdkStorage::new("my-bucket", "us-west-2", None, "ak", "sk").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L221:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L236:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L254:C91

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
S3SdkStorage::new(&bucket, &region, Some(endpoint), &access_key, &secret_key).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L259:C63

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let url = storage.put(&key, data, "text/plain").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L262:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let fetched = storage.get(&key).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L265:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(storage.exists(&key).await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L267:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
storage.delete(&key).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L268:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!storage.exists(&key).await.unwrap());
```

### `packages\sz-orm-storage\src\signing.rs`

#### 🟠 [HIGH] `expect()` @ L32:C51

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let mut mac = HmacSha256::new_from_slice(key).expect("HMAC accepts any key length");
```

#### 🟠 [HIGH] `expect()` @ L58:C49

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let mut mac = HmacSha1::new_from_slice(key).expect("HMAC accepts any key length");
```

### `packages\sz-orm-storage\src\tencent.rs`

#### 🟠 [HIGH] `unwrap()` @ L87:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L91:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let data = storage.get("a.txt").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L106:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
storage.put("key", b"data", "text/plain").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L107:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(storage.exists("key").await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L109:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
storage.delete("key").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L110:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!storage.exists("key").await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L116:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
storage.put("key", b"v1", "text/plain").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L117:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
storage.put("key", b"v2", "text/plain").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L118:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(storage.get("key").await.unwrap(), b"v2");
```

#### 🟠 [HIGH] `unwrap()` @ L124:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
storage.put("a", b"a-data", "text/plain").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L125:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
storage.put("b", b"b-data", "text/plain").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L127:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(storage.get("a").await.unwrap(), b"a-data");
```

#### 🟠 [HIGH] `unwrap()` @ L128:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(storage.get("b").await.unwrap(), b"b-data");
```

#### 🟠 [HIGH] `unwrap()` @ L130:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
storage.delete("a").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L131:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!storage.exists("a").await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L132:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(storage.exists("b").await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L279:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let url = backend.as_ref().unwrap().url_for("a.txt");
```

#### 🟠 [HIGH] `unwrap()` @ L292:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L305:C86

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let backend = RealTencentCos::new(&bucket, &region, &secret_id, &secret_key).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L309:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
backend.put(key, data, "text/plain").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L310:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let fetched = backend.get(key).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L313:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(backend.exists(key).await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L315:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
backend.delete(key).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L316:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!backend.exists(key).await.unwrap());
```

### `packages\sz-orm-storage\src\upyun.rs`

#### 🟠 [HIGH] `unwrap()` @ L85:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L89:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let data = storage.get("file.txt").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L104:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
storage.put("key", b"data", "text/plain").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L105:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(storage.exists("key").await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L107:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
storage.delete("key").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L108:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!storage.exists("key").await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L114:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
storage.put("key", b"v1", "text/plain").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L115:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
storage.put("key", b"v2", "text/plain").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L116:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(storage.get("key").await.unwrap(), b"v2");
```

#### 🟠 [HIGH] `unwrap()` @ L446:C75

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let backend = RealUpYun::new("my-bucket", "operator", "password").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L455:C72

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let backend = RealUpYun::new("bucket", "operator", "password").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L468:C63

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let backend = RealUpYun::new("my-bucket", "op", "pw").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L483:C69

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let backend = RealUpYun::new(&bucket, &operator, &password).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L487:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
backend.put(key, data, "text/plain").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L488:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let fetched = backend.get(key).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L491:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(backend.exists(key).await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L493:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
backend.delete(key).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L494:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(!backend.exists(key).await.unwrap());
```

### `packages\sz-orm-storage\tests\stress.rs`

#### 🟠 [HIGH] `unwrap()` @ L81:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L88:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let data = storage.get(&key).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L95:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(storage.exists(&key).await.unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L101:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
storage.delete(&key).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L144:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L149:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let data = storage.get(&key).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L173:C66

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
s.put(&key, data.as_bytes(), "text/plain").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L174:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let got = s.get(&key).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L181:C17

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
h.await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L239:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L240:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let got = storage.get(&key).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L254:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L262:C18

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L263:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let got = s.get("builder-test.txt").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L284:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L285:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let got = storage.get(key).await.unwrap();
```

### `packages\sz-orm-swagger\src\lib.rs`

#### 🟠 [HIGH] `expect()` @ L1368:C46

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let users = spec.paths.get("/users").expect("/users should exist");
```

#### 🟠 [HIGH] `expect()` @ L1369:C36

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let get = users.get("get").expect("GET method should exist");
```

#### 🟠 [HIGH] `unwrap()` @ L1387:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let users = spec.paths.get("/users").unwrap();
```

#### 🟠 [HIGH] `expect()` @ L1457:C69

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let parsed: serde_json::Value = serde_json::from_str(&json).expect("should parse");
```

#### 🟠 [HIGH] `unwrap()` @ L1609:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(p.enum_values.as_ref().unwrap().len(), 2);
```

#### 🟠 [HIGH] `unwrap()` @ L1638:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let json = serde_json::to_string(&schema).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1639:C69

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parsed: serde_json::Value = serde_json::from_str(&json).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1799:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
tag.external_docs.as_ref().unwrap().url,
```

#### 🟠 [HIGH] `unwrap()` @ L1834:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let env_var = server.variables.get("env").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1897:C28

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(v.as_str().unwrap().contains("-"));
```

#### 🟠 [HIGH] `expect()` @ L1978:C42

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let components = spec.components.expect("components should exist");
```

#### 🟠 [HIGH] `expect()` @ L1987:C42

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let components = spec.components.expect("components should exist");
```

#### 🟠 [HIGH] `unwrap()` @ L2029:C35

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let body = p.request_body.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L2095:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let scopes = req.requirements.get("oauth2").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L2171:C69

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parsed: serde_json::Value = serde_json::from_str(&json).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L2207:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let json = serde_json::to_value(&schema).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L2232:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let json = serde_json::to_value(&schema).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L2237:C80

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
json.get("properties").is_none() || json["properties"].as_object().unwrap().is_empty()
```

### `packages\sz-orm-timeseries\src\extensions.rs`

#### 🟠 [HIGH] `unwrap()` @ L610:C59

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let epoch = DateTime::<Utc>::from_timestamp(0, 0).unwrap();
```

#### 🟠 [HIGH] `expect()` @ L792:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("bucket is non-empty (checked above)");
```

#### 🟠 [HIGH] `unwrap()` @ L861:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
Utc.with_ymd_and_hms(2026, 7, 20, 0, 0, 0).unwrap() + Duration::minutes(min)
```

#### 🟠 [HIGH] `unwrap()` @ L921:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
reg.register(def).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L929:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L938:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L939:C44

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let removed = reg.unregister("v1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L950:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L961:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L974:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L987:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L991:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1003:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1083:C28

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
reg.enable(config).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1091:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1100:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1101:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let config = reg.disable("cpu_usage").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1117:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1125:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let s = reg.stats("cpu_usage").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1134:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1136:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1192:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1200:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1209:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1210:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let removed = reg.unregister("cpu_usage").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1218:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
reg.register(RetentionPolicy::new("t1", 86400)).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1220:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1230:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1233:C58

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let (count, rows) = reg.purge_stats("cpu_usage").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1241:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
reg.register(RetentionPolicy::new("t1", 86400)).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1243:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1251:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
reg.register(RetentionPolicy::new("t1", 86400)).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1264:C63

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let t = Utc.with_ymd_and_hms(2026, 7, 20, 10, 35, 42).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1268:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
Utc.with_ymd_and_hms(2026, 7, 20, 10, 0, 0).unwrap()
```

#### 🟠 [HIGH] `unwrap()` @ L1274:C63

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let t = Utc.with_ymd_and_hms(2026, 7, 20, 10, 35, 42).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1278:C58

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
Utc.with_ymd_and_hms(2026, 7, 20, 10, 35, 0).unwrap()
```

#### 🟠 [HIGH] `unwrap()` @ L1284:C63

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let t = Utc.with_ymd_and_hms(2026, 7, 20, 10, 35, 42).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1286:C72

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(aligned, Utc.with_ymd_and_hms(2026, 7, 20, 0, 0, 0).unwrap());
```

#### 🟠 [HIGH] `unwrap()` @ L1291:C64

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let start = Utc.with_ymd_and_hms(2026, 7, 20, 0, 0, 0).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1292:C62

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let end = Utc.with_ymd_and_hms(2026, 7, 20, 3, 0, 0).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1303:C65

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let start = Utc.with_ymd_and_hms(2026, 7, 20, 0, 30, 0).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1304:C62

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let end = Utc.with_ymd_and_hms(2026, 7, 20, 2, 0, 0).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1310:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
Utc.with_ymd_and_hms(2026, 7, 20, 0, 0, 0).unwrap()
```

#### 🟠 [HIGH] `unwrap()` @ L1314:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
Utc.with_ymd_and_hms(2026, 7, 20, 1, 0, 0).unwrap()
```

#### 🟠 [HIGH] `unwrap()` @ L1402:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(parse_bucket_to_secs("30s").unwrap(), 30);
```

#### 🟠 [HIGH] `unwrap()` @ L1403:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(parse_bucket_to_secs("5m").unwrap(), 300);
```

#### 🟠 [HIGH] `unwrap()` @ L1404:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(parse_bucket_to_secs("1h").unwrap(), 3600);
```

#### 🟠 [HIGH] `unwrap()` @ L1405:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(parse_bucket_to_secs("1d").unwrap(), 86400);
```

#### 🟠 [HIGH] `unwrap()` @ L1406:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(parse_bucket_to_secs("1w").unwrap(), 86400 * 7);
```

#### 🟠 [HIGH] `unwrap()` @ L1411:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(parse_bucket_to_secs("15m").unwrap(), 900);
```

#### 🟠 [HIGH] `unwrap()` @ L1412:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(parse_bucket_to_secs("12h").unwrap(), 43200);
```

#### 🟠 [HIGH] `unwrap()` @ L1413:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(parse_bucket_to_secs("30d").unwrap(), 2592000);
```

#### 🟠 [HIGH] `unwrap()` @ L1444:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(secs_to_bucket_string(30).unwrap(), "30s");
```

#### 🟠 [HIGH] `unwrap()` @ L1445:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(secs_to_bucket_string(300).unwrap(), "5m");
```

#### 🟠 [HIGH] `unwrap()` @ L1446:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(secs_to_bucket_string(3600).unwrap(), "1h");
```

#### 🟠 [HIGH] `unwrap()` @ L1447:C49

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(secs_to_bucket_string(86400).unwrap(), "1d");
```

#### 🟠 [HIGH] `unwrap()` @ L1448:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(secs_to_bucket_string(86400 * 7).unwrap(), "1w");
```

#### 🟠 [HIGH] `unwrap()` @ L1460:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(secs_to_bucket_string(86400 * 7).unwrap(), "1w");
```

#### 🟠 [HIGH] `unwrap()` @ L1462:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(secs_to_bucket_string(86400 * 14).unwrap(), "2w");
```

### `packages\sz-orm-timeseries\src\memory.rs`

#### 🟠 [HIGH] `unwrap()` @ L234:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
Utc.with_ymd_and_hms(2026, 7, 20, 0, 0, 0).unwrap()
```

#### 🟠 [HIGH] `unwrap()` @ L243:C61

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
ts.insert_metric(&make_metric("cpu", 0, 0.5)).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L246:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L249:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L251:C64

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let start = Utc.with_ymd_and_hms(2026, 7, 20, 0, 0, 0).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L252:C63

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let end = Utc.with_ymd_and_hms(2026, 7, 20, 0, 15, 0).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L253:C62

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = ts.query_range("cpu", start, end).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L272:C63

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let start = Utc.with_ymd_and_hms(2026, 1, 1, 0, 0, 0).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L286:C63

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let start = Utc.with_ymd_and_hms(2026, 1, 1, 0, 0, 0).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L302:C63

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let start = Utc.with_ymd_and_hms(2026, 1, 1, 0, 0, 0).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L313:C63

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let start = Utc.with_ymd_and_hms(2026, 1, 1, 0, 0, 0).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L325:C63

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let start = Utc.with_ymd_and_hms(2026, 1, 1, 0, 0, 0).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L349:C18

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L352:C64

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let start = Utc.with_ymd_and_hms(2026, 7, 20, 0, 0, 0).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L353:C62

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let end = Utc.with_ymd_and_hms(2026, 7, 20, 1, 0, 0).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L357:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L375:C18

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L379:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
ts.downsample(&config).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L381:C64

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let start = Utc.with_ymd_and_hms(2026, 7, 20, 0, 0, 0).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L382:C63

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let end = Utc.with_ymd_and_hms(2026, 7, 20, 1, 30, 0).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L383:C66

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = ts.query_range("cpu_30m", start, end).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L390:C61

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
ts.insert_metric(&make_metric("cpu", 0, 0.5)).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L391:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
ts.drop_metric("cpu").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L398:C58

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(MemoryTimeseries::parse_bucket("30s").unwrap(), 30);
```

#### 🟠 [HIGH] `unwrap()` @ L399:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(MemoryTimeseries::parse_bucket("5m").unwrap(), 300);
```

#### 🟠 [HIGH] `unwrap()` @ L400:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(MemoryTimeseries::parse_bucket("1h").unwrap(), 3600);
```

#### 🟠 [HIGH] `unwrap()` @ L401:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(MemoryTimeseries::parse_bucket("1d").unwrap(), 86400);
```

#### 🟠 [HIGH] `unwrap()` @ L414:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
ts.insert_metrics(&metrics).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L415:C64

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let start = Utc.with_ymd_and_hms(2026, 7, 20, 0, 0, 0).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L416:C63

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let end = Utc.with_ymd_and_hms(2026, 7, 20, 0, 10, 0).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L417:C62

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = ts.query_range("cpu", start, end).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L424:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
ts.create_hypertable("metrics", "ts").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L427:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

### `packages\sz-orm-timeseries\src\safety.rs`

#### 🟠 [HIGH] `expect()` @ L22:C30

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let first = chars.next().expect("non-empty checked above");
```

#### 🟠 [HIGH] `expect()` @ L51:C30

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let first = chars.next().expect("non-empty checked above");
```

### `packages\sz-orm-timeseries\src\stub.rs`

#### 🟠 [HIGH] `unwrap()` @ L161:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
stub.create_hypertable("metrics", "ts").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L173:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
stub.insert_metric(&m).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L182:C64

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let start = Utc.with_ymd_and_hms(2026, 7, 20, 0, 0, 0).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L183:C62

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let end = Utc.with_ymd_and_hms(2026, 7, 20, 1, 0, 0).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L185:C64

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = stub.query_range("cpu", start, end).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L196:C64

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let start = Utc.with_ymd_and_hms(2026, 7, 20, 0, 0, 0).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L197:C62

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let end = Utc.with_ymd_and_hms(2026, 7, 20, 1, 0, 0).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L202:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L215:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L225:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
stub.downsample(&config).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L268:C64

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let start = Utc.with_ymd_and_hms(2026, 7, 20, 0, 0, 0).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L269:C62

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let end = Utc.with_ymd_and_hms(2026, 7, 20, 1, 0, 0).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L280:C64

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let start = Utc.with_ymd_and_hms(2026, 7, 20, 0, 0, 0).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L281:C62

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let end = Utc.with_ymd_and_hms(2026, 7, 20, 1, 0, 0).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L294:C64

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let start = Utc.with_ymd_and_hms(2026, 7, 20, 0, 0, 0).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L295:C62

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let end = Utc.with_ymd_and_hms(2026, 7, 20, 1, 0, 0).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L393:C64

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let start = Utc.with_ymd_and_hms(2026, 7, 20, 0, 0, 0).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L394:C62

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let end = Utc.with_ymd_and_hms(2026, 7, 20, 1, 0, 0).unwrap();
```

### `packages\sz-orm-timeseries\src\timeseries.rs`

#### 🟠 [HIGH] `expect()` @ L229:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("build failed");
```

#### 🟠 [HIGH] `expect()` @ L233:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("create_hypertable failed");
```

#### 🟠 [HIGH] `expect()` @ L240:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("build failed");
```

#### 🟠 [HIGH] `expect()` @ L244:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("create_hypertable failed");
```

### `packages\sz-orm-tracing\src\lib.rs`

#### 🟠 [HIGH] `unwrap()` @ L161:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `unwrap()` @ L169:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `expect()` @ L995:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("traceparent header must be present");
```

#### 🟠 [HIGH] `unwrap()` @ L1019:C25

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let span = span.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1034:C25

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let span = span.unwrap();
```

#### 🟠 [HIGH] `expect()` @ L1052:C55

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let span = SzTracer::parse_traceparent(valid).expect("valid traceparent must parse");
```

#### 🟠 [HIGH] `expect()` @ L1139:C52

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let span = tracer.extract_legacy(&headers).expect("legacy extract");
```

#### 🟠 [HIGH] `expect()` @ L1151:C50

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let extracted = tracer.extract(&headers).expect("roundtrip extract");
```

#### 🟠 [HIGH] `expect()` @ L1169:C45

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let span = tracer.extract(&headers).expect("extract");
```

#### 🟠 [HIGH] `expect()` @ L1185:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("should fall back to legacy");
```

#### 🟠 [HIGH] `expect()` @ L1225:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("traceparent must be present");
```

#### 🟠 [HIGH] `expect()` @ L1230:C50

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let extracted = tracer.extract(&headers).expect("extract should round-trip");
```

#### 🟠 [HIGH] `expect()` @ L1251:C50

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let extracted = tracer.extract(&headers).expect("extract should round-trip");
```

#### 🟠 [HIGH] `expect()` @ L1366:C41

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let p95 = hist.percentile(95.0).expect("p95 must exist");
```

#### 🟠 [HIGH] `expect()` @ L1376:C41

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let p99 = hist.percentile(99.0).expect("p99 must exist");
```

#### 🟠 [HIGH] `expect()` @ L1681:C41

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let alert = gauge.check_alert().expect("alert must fire when saturated");
```

#### 🟠 [HIGH] `expect()` @ L1760:C29

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
hook.notify(&alert).expect("notify must succeed");
```

#### 🟠 [HIGH] `unwrap()` @ L1772:C26

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
hook.notify(&a1).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1773:C26

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
hook.notify(&a2).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1774:C26

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
hook.notify(&a3).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1787:C29

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
hook.notify(&alert).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1816:C32

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
webhook.notify(&alert).unwrap();
```

#### 🟠 [HIGH] `expect()` @ L1848:C43

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let report = monitor.report("op").expect("report must exist");
```

#### 🟠 [HIGH] `expect()` @ L1869:C43

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let report = monitor.report("op").expect("report must exist");
```

#### 🟠 [HIGH] `expect()` @ L1888:C43

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let report = monitor.report("op").expect("report");
```

#### 🟠 [HIGH] `expect()` @ L1903:C40

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let r1 = monitor.report("op1").expect("op1 report");
```

#### 🟠 [HIGH] `expect()` @ L1904:C40

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let r2 = monitor.report("op2").expect("op2 report");
```

#### 🟠 [HIGH] `expect()` @ L1917:C43

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let report = monitor.report("op").expect("report");
```

#### 🟠 [HIGH] `expect()` @ L1929:C43

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let report = monitor.report("op").expect("report");
```

#### 🟠 [HIGH] `unwrap()` @ L1962:C22

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
h.join().unwrap();
```

#### 🟠 [HIGH] `expect()` @ L1965:C43

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let report = monitor.report("op").expect("op report");
```

#### 🟠 [HIGH] `expect()` @ L1983:C43

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let report = monitor.report("op").expect("report");
```

#### 🟠 [HIGH] `expect()` @ L1993:C43

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let report = monitor.report("op").expect("report");
```

#### 🟠 [HIGH] `unwrap()` @ L2003:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let r = monitor.report("op").unwrap();
```

### `packages\sz-orm-vector\src\extensions.rs`

#### 🟠 [HIGH] `unwrap()` @ L875:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
reg.register(def).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L884:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
reg.register(def).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L894:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
reg.register(def).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L895:C58

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let removed = reg.unregister("idx_docs_ivfflat").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L914:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L920:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L926:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L942:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L948:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L960:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L966:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1268:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1280:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1283:C73

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let results = batch_ops.batch_search("docs", &queries, 2).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1296:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1307:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1310:C63

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let results = batch_ops.batch_get("docs", &ids).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1324:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1344:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1349:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1363:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1374:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1379:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

### `packages\sz-orm-vector\src\memory.rs`

#### 🟠 [HIGH] `unwrap()` @ L245:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.create_collection("docs", 4, None).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L246:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(store.count("docs").await.unwrap(), 0);
```

#### 🟠 [HIGH] `unwrap()` @ L248:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.delete_collection("docs").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L249:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(store.count("docs").await.unwrap(), 0);
```

#### 🟠 [HIGH] `unwrap()` @ L255:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.create_collection("docs", 3, None).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L257:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.insert("docs", vec![rec]).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L258:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(store.count("docs").await.unwrap(), 1);
```

#### 🟠 [HIGH] `unwrap()` @ L260:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let fetched = store.get("docs", "r1").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L260:C62

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let fetched = store.get("docs", "r1").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L264:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(store.get("docs", "missing").await.unwrap().is_none());
```

#### 🟠 [HIGH] `unwrap()` @ L270:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.create_collection("docs", 3, None).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L280:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.create_collection("docs", 2, None).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L284:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L288:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L290:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(store.count("docs").await.unwrap(), 1);
```

#### 🟠 [HIGH] `unwrap()` @ L291:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let fetched = store.get("docs", "r1").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L291:C62

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let fetched = store.get("docs", "r1").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L301:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L307:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.insert("docs", records).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L309:C71

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let results = store.search("docs", &[1.0, 0.0, 0.0], 2).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L318:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.create_collection("docs", 2, None).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L326:C18

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L328:C66

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let results = store.search("docs", &[0.0, 1.0], 3).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L335:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.create_collection("docs", 2, None).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L346:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L350:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L352:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(store.count("docs").await.unwrap(), 1);
```

#### 🟠 [HIGH] `unwrap()` @ L361:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L366:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.insert("docs", records).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L368:C66

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let results = store.search("docs", &[0.0, 0.0], 2).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L380:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L385:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.insert("docs", records).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L387:C66

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let results = store.search("docs", &[1.0, 1.0], 2).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L396:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(result.unwrap(), 0);
```

#### 🟠 [HIGH] `unwrap()` @ L405:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.create_collection("docs", 2, None).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L406:C61

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = store.get("docs", "nonexistent").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L414:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let count = store.count("nonexistent").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L426:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.create_collection("docs", 2, None).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L430:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L445:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.create_collection("docs", 2, None).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L449:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L463:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.create_collection("docs", 2, None).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L467:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L472:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L481:C38

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(validate_top_k(1).unwrap(), 1);
```

#### 🟠 [HIGH] `unwrap()` @ L482:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(validate_top_k(100).unwrap(), 100);
```

#### 🟠 [HIGH] `unwrap()` @ L483:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(validate_top_k(crate::MAX_TOP_K).unwrap(), crate::MAX_TOP_K);
```

### `packages\sz-orm-vector\src\real_pg.rs`

#### 🟠 [HIGH] `unwrap()` @ L169:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let first = chars.next().unwrap();
```

#### 🟠 [HIGH] `expect()` @ L573:C53

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let _store = RealPgVectorStore::new(config).expect("new() should not connect");
```

### `packages\sz-orm-vector\tests\vector_tests.rs`

#### 🟠 [HIGH] `unwrap()` @ L10:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.create_collection("test_docs", 4, None).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L11:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(store.count("test_docs").await.unwrap(), 0);
```

#### 🟠 [HIGH] `unwrap()` @ L20:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L28:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.insert("docs", records).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L29:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(store.count("docs").await.unwrap(), 3);
```

#### 🟠 [HIGH] `unwrap()` @ L32:C67

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let results = store.search("docs", &[1.0, 0.0, 0.0], 3).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L38:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let record = store.get("docs", "a").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L38:C56

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let record = store.get("docs", "a").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L43:C69

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let removed = store.delete("docs", vec!["b".to_string()]).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L45:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(store.count("docs").await.unwrap(), 2);
```

#### 🟠 [HIGH] `unwrap()` @ L48:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.delete_collection("docs").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L49:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(store.count("docs").await.unwrap(), 0);
```

#### 🟠 [HIGH] `unwrap()` @ L60:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L70:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L71:C64

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let results = store.search("euclid", &[0.0, 0.0], 2).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L78:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L88:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L89:C61

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let results = store.search("dot", &[1.0, 1.0], 2).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L97:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.create_collection("col_a", 2, None).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L98:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.create_collection("col_b", 3, None).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L103:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L107:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L109:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(store.count("col_a").await.unwrap(), 1);
```

#### 🟠 [HIGH] `unwrap()` @ L110:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(store.count("col_b").await.unwrap(), 1);
```

#### 🟠 [HIGH] `unwrap()` @ L116:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.create_collection("docs", 2, None).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L125:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L130:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L132:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(store.count("docs").await.unwrap(), 3);
```

#### 🟠 [HIGH] `unwrap()` @ L138:C52

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
store.create_collection("docs", 2, None).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L143:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L144:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(store.count("docs").await.unwrap(), 1);
```

#### 🟠 [HIGH] `unwrap()` @ L150:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L151:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(store.count("docs").await.unwrap(), 1);
```

#### 🟠 [HIGH] `unwrap()` @ L153:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let fetched = store.get("docs", "x").await.unwrap().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L153:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let fetched = store.get("docs", "x").await.unwrap().unwrap();
```

### `packages\sz-orm-wasm\src\advanced.rs`

#### 🟠 [HIGH] `unwrap()` @ L1190:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1195:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1200:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1215:C67

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
db.execute(WasmQuery::new("CREATE TABLE users (id INT)")).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1216:C68

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
db.execute(WasmQuery::new("CREATE TABLE orders (id INT)")).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1221:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1305:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1310:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1316:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1373:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1570:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1575:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1580:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1584:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = scheduler.run_next().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1595:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1603:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1604:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = scheduler.run_next().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1623:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1626:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1635:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1639:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1642:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1654:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1655:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let cancelled = scheduler.cancel("t1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1666:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1670:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1671:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
scheduler.run_next().unwrap(); // 已完成
```

#### 🟠 [HIGH] `unwrap()` @ L1672:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let cancelled = scheduler.cancel("t1").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1682:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1683:C43

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let result = scheduler.run_next().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1692:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1696:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1697:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
scheduler.run_next().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1700:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(stored.unwrap().task_id, "q");
```

#### 🟠 [HIGH] `unwrap()` @ L1707:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1711:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1714:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1717:C30

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
scheduler.run_next().unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1736:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.put("mod1", vec![1, 2, 3]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1753:C33

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.put("k", vec![1]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1765:C33

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.put("k", vec![1]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1777:C33

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.put("k", vec![1]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1778:C36

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.put("k", vec![2, 3]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1786:C33

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.put("k", vec![1]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1795:C33

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.put("a", vec![1]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1796:C33

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.put("b", vec![2]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1808:C33

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.put("a", vec![1]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1811:C33

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.put("b", vec![2]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1813:C33

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.put("c", vec![3]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1826:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.put("a", vec![1, 2, 3]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1828:C39

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.put("b", vec![4, 5, 6]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1843:C33

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.put("k", vec![1]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1851:C33

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.put("a", vec![1]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1852:C33

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.put("b", vec![2]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1861:C33

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.put("a", vec![1]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1890:C33

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.put("a", vec![1]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1891:C33

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.put("b", vec![2]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L1921:C33

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
cache.put("k", vec![1]).unwrap();
```

### `packages\sz-orm-wasm\src\lib.rs`

#### 🟠 [HIGH] `unwrap()` @ L478:C68

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let rows = db.query(WasmQuery::new("SELECT * FROM users")).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L487:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L490:C68

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let rows = db.query(WasmQuery::new("SELECT * FROM users")).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L498:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L504:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L507:C68

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let rows = db.query(WasmQuery::new("SELECT * FROM users")).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L517:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L530:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L533:C68

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let rows = db.query(WasmQuery::new("SELECT * FROM users")).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L543:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L558:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L566:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L577:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L582:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L589:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L597:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L602:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L609:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L617:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L626:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L636:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L641:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L648:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L651:C68

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let rows = db.query(WasmQuery::new("SELECT * FROM users")).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L661:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L673:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L680:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L683:C68

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let rows = db.query(WasmQuery::new("SELECT * FROM users")).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L695:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L700:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L702:C71

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let deleted = db.execute(WasmQuery::new("DELETE FROM users")).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L705:C68

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let rows = db.query(WasmQuery::new("SELECT * FROM users")).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L716:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L729:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L731:C71

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let rows = db.query(WasmQuery::new("SELECT * FROM products")).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L738:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L745:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L752:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L754:C71

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let rows = db.query(WasmQuery::new("SELECT * FROM products")).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L778:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L786:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L791:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L793:C69

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let rows = db.query(WasmQuery::new("SELECT * FROM users;")).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L801:C72

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let rows = db.query(WasmQuery::new("SELECT * FROM any_table")).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L823:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L836:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

### `packages\sz-orm-websocket\src\compression.rs`

#### 🟠 [HIGH] `expect()` @ L307:C10

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("zlib compression of in-memory buffer cannot fail");
```

#### 🟠 [HIGH] `unwrap()` @ L715:C60

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let decompressed = deflate_decompress(&compressed).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L736:C60

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let decompressed = deflate_decompress(&compressed).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L787:C63

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let decompressed = comp.decompress(&compressed).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L796:C57

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let decompressed = comp.decompress(&data).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L841:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
comp.decompress(&compressed1).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L842:C45

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
comp.decompress(&compressed2).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L861:C63

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let decompressed = comp.decompress(&compressed).await.unwrap();
```

### `packages\sz-orm-websocket\src\handler.rs`

#### 🟠 [HIGH] `unwrap()` @ L379:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
handler.on_connect(&conn).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L389:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
handler.on_connect(&conn).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L403:C61

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let response = handler.on_message(&conn, msg).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L406:C29

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let resp = response.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L424:C61

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let response = handler.on_message(&conn, msg).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L425:C29

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let resp = response.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L442:C61

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let response = handler.on_message(&conn, msg).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L443:C29

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let resp = response.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L461:C61

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let response = handler.on_message(&conn, msg).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L462:C29

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let resp = response.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L479:C61

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let response = handler.on_message(&conn, msg).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L480:C29

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let resp = response.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L499:C61

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let response = handler.on_message(&conn, msg).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L500:C29

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let resp = response.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L516:C61

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let response = handler.on_message(&conn, msg).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L525:C59

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
handler.on_message(&conn, make_text(b"m1")).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L538:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L539:C59

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
handler.on_message(&conn, make_text(b"m3")).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L551:C53

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let user_id = handler.authenticate("12345").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L558:C61

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let user_id = handler.authenticate("user_id:67890").unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L582:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
handler.on_connect(&conn).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L584:C60

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let retrieved = handler.get_connection("c1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L602:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
handler.on_connect(&conn1).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L603:C42

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
handler.on_connect(&conn2).await.unwrap();
```

### `packages\sz-orm-websocket\src\heartbeat.rs`

#### 🟠 [HIGH] `unwrap()` @ L458:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let state = tracker.state("c1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L477:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let state = tracker.state("c1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L586:C47

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let state = tracker.state("c1").await.unwrap();
```

### `packages\sz-orm-websocket\src\lib.rs`

#### 🟠 [HIGH] `unwrap()` @ L102:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap()
```

#### 🟠 [HIGH] `unwrap()` @ L106:C78

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parsed: serde_json::Value = serde_json::from_slice(&msg.payload).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L235:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
pusher.subscribe("conn1", "room1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L245:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
pusher.subscribe("conn1", "room1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L256:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
pusher.subscribe("conn1", "room1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L257:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
pusher.subscribe("conn1", "room2").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L268:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
pusher.subscribe("conn1", "room1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L273:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(result.unwrap(), 1, "应向 1 个连接推送成功");
```

#### 🟠 [HIGH] `unwrap()` @ L284:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(result.unwrap(), 2);
```

#### 🟠 [HIGH] `unwrap()` @ L295:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(result.unwrap(), 1, "应向 1 个 user_id=123 的连接推送成功");
```

#### 🟠 [HIGH] `unwrap()` @ L302:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
pusher.subscribe("conn1", "room1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L307:C27

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert_eq!(result.unwrap(), 1, "应向 1 个订阅了 room1 的连接推送成功");
```

### `packages\sz-orm-websocket\src\pool.rs`

#### 🟠 [HIGH] `unwrap()` @ L426:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let conn = pool.get("c1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L442:C41

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let conn = pool.get("c1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L461:C33

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conns.get_mut("c1").unwrap().user_id = Some(100);
```

#### 🟠 [HIGH] `unwrap()` @ L462:C33

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
conns.get_mut("c2").unwrap().user_id = Some(200);
```

### `packages\sz-orm-websocket\src\pusher.rs`

#### 🟠 [HIGH] `unwrap()` @ L321:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
sender.send("conn1", b"hello".to_vec()).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L322:C55

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
sender.send("conn1", b"world".to_vec()).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L335:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
sender.send("conn1", b"a".to_vec()).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L336:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
sender.send("conn1", b"b".to_vec()).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L343:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
sender.send("conn1", b"data".to_vec()).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L346:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
sender.close("conn1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L353:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
sender.send("conn1", b"a".to_vec()).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L354:C51

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
sender.send("conn2", b"b".to_vec()).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L372:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L389:C70

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let count = pusher.push_to_user(999, b"nope".to_vec()).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L403:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L415:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L437:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
pusher.subscribe("conn1", "room1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L438:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
pusher.subscribe("conn2", "room1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L439:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
pusher.subscribe("conn3", "room2").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L444:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L458:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L468:C67

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let count = pusher.broadcast(b"broadcast".to_vec()).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L482:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L496:C73

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let count = pusher.push_order_status(123, 789, "shipped").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L501:C78

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parsed: serde_json::Value = serde_json::from_slice(&messages[0]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L512:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
pusher.subscribe("conn1", "room1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L513:C50

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
pusher.subscribe("conn2", "room1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L518:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L524:C82

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let parsed: serde_json::Value = serde_json::from_slice(&messages[0]).unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L539:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L543:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L547:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

### `packages\sz-orm-websocket\src\server.rs`

#### 🟠 [HIGH] `unwrap()` @ L237:C46

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
sender.send(b"hello".to_vec()).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L238:C40

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let received = rx.recv().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L255:C37

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
server.start(handler).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L260:C29

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
server.stop().await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L267:C70

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let count = server.broadcast_to_all(b"hello".to_vec()).await.unwrap();
```

### `packages\sz-orm-websocket\src\subprotocol.rs`

#### 🟠 [HIGH] `expect()` @ L307:C14

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("candidates is non-empty");
```

#### 🟠 [HIGH] `expect()` @ L669:C45

- **上下文**: Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let header = outcome.header_value().expect("should accept");
```

### `packages\sz-orm-websocket\tests\stress.rs`

#### 🟠 [HIGH] `unwrap()` @ L29:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L48:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L56:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L83:C54

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let success = p.broadcast(msg).await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L90:C17

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
h.await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L118:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L138:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
pusher.subscribe("conn-1", "room-1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L142:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L149:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L166:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L183:C14

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L188:C34

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
sender.close("conn-1").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L199:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
pusher.subscribe("conn-1", "room-a").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L200:C48

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
pusher.subscribe("conn-2", "room-a").await.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L205:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟠 [HIGH] `unwrap()` @ L211:C10

- **上下文**: Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

### `packages\sz-orm-config\tests\env_source_test.rs`

#### 🟡 [MEDIUM] `unwrap()` @ L15:C33

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let data = src.load().await.unwrap();
```

#### 🟡 [MEDIUM] `expect()` @ L20:C35

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let ratio = data.get("ratio").expect("ratio 应存在");
```

#### 🟡 [MEDIUM] `unwrap()` @ L34:C33

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let data = src.load().await.unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L42:C33

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let data = src.load().await.unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L59:C33

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let data = src.load().await.unwrap();
```

#### 🟡 [MEDIUM] `expect()` @ L63:C39

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let float_val = data.get("float").expect("float 应存在");
```

#### 🟡 [MEDIUM] `unwrap()` @ L78:C33

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let data = src.load().await.unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L87:C33

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let data = src.load().await.unwrap();
```

### `packages\sz-orm-config\tests\file_source_test.rs`

#### 🟡 [MEDIUM] `unwrap()` @ L11:C35

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dir = tempfile::tempdir().unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L13:C49

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut file = std::fs::File::create(&path).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L18:C6

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L21:C33

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let data = src.load().await.unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L29:C35

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dir = tempfile::tempdir().unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L36:C6

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L39:C33

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let data = src.load().await.unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L47:C35

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dir = tempfile::tempdir().unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L56:C35

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dir = tempfile::tempdir().unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L58:C43

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
std::fs::write(&path, "key: value\n").unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L66:C35

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dir = tempfile::tempdir().unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L68:C46

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
std::fs::write(&path, "{not valid json").unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L76:C35

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dir = tempfile::tempdir().unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L82:C6

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L84:C33

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let data = src.load().await.unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L88:C34

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
assert!(data.get("features").unwrap().is_array());
```

#### 🟡 [MEDIUM] `unwrap()` @ L93:C35

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dir = tempfile::tempdir().unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L110:C6

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L112:C33

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let data = src.load().await.unwrap();
```

### `packages\sz-orm-dtx\tests\file_log_test.rs`

#### 🟡 [MEDIUM] `unwrap()` @ L26:C30

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dir = TempDir::new().unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L27:C65

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let log = FileTransactionLog::new(dir.path().to_path_buf()).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L29:C50

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
log.append(&entry("tx1", "Active", "begin")).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L30:C54

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
log.append(&entry("tx1", "Prepared", "prepare")).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L31:C50

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
log.append(&entry("tx2", "Active", "begin")).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L33:C34

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let entries = log.read_all().unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L44:C30

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dir = TempDir::new().unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L45:C65

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let log = FileTransactionLog::new(dir.path().to_path_buf()).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L47:C50

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
log.append(&entry("tx1", "Active", "begin")).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L48:C54

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
log.append(&entry("tx1", "Prepared", "prepare")).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L49:C50

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
log.append(&entry("tx2", "Active", "begin")).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L51:C25

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
log.truncate("tx1").unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L53:C34

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let entries = log.read_all().unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L61:C30

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dir = TempDir::new().unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L62:C65

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let log = FileTransactionLog::new(dir.path().to_path_buf()).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L63:C50

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
log.append(&entry("tx1", "Active", "begin")).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L64:C16

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
log.sync().unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L67:C66

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let log2 = FileTransactionLog::new(dir.path().to_path_buf()).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L68:C35

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let entries = log2.read_all().unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L76:C30

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dir = TempDir::new().unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L80:C69

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let log = FileTransactionLog::new(dir.path().to_path_buf()).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L82:C60

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
log.append(&entry("tx-commit", "Active", "begin")).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L83:C64

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
log.append(&entry("tx-commit", "Prepared", "prepare")).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L84:C65

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
log.append(&entry("tx-commit", "Committing", "commit")).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L86:C56

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
log.append(&entry("tx-rb", "Active", "begin")).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L87:C60

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
log.append(&entry("tx-rb", "Prepared", "prepare")).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L88:C64

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
log.append(&entry("tx-rb", "RollingBack", "rollback")).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L90:C62

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
log.append(&entry("tx-done", "Committed", "commit")).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L95:C65

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let log = FileTransactionLog::new(dir.path().to_path_buf()).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L96:C34

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let entries = log.read_all().unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L100:C42

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let actions = recovery.recover(&log).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L127:C30

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dir = TempDir::new().unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L128:C65

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let log = FileTransactionLog::new(dir.path().to_path_buf()).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L129:C34

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let entries = log.read_all().unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L136:C30

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dir = TempDir::new().unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L137:C65

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let log = FileTransactionLog::new(dir.path().to_path_buf()).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L139:C68

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
log.append(&entry(&format!("tx{}", i), "Active", "begin")).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L141:C34

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let entries = log.read_all().unwrap();
```

### `packages\sz-orm-dtx\tests\recovery_test.rs`

#### 🟡 [MEDIUM] `unwrap()` @ L21:C29

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
self.entries.lock().unwrap().push(e);
```

#### 🟡 [MEDIUM] `unwrap()` @ L27:C29

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
self.entries.lock().unwrap().push(entry.clone());
```

#### 🟡 [MEDIUM] `unwrap()` @ L31:C32

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
Ok(self.entries.lock().unwrap().clone())
```

#### 🟡 [MEDIUM] `unwrap()` @ L34:C47

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let mut entries = self.entries.lock().unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L57:C64

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let actions = TransactionRecovery::default().recover(&log).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L69:C64

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let actions = TransactionRecovery::default().recover(&log).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L94:C10

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L104:C10

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
.unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L122:C64

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let actions = TransactionRecovery::default().recover(&log).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L136:C64

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let actions = TransactionRecovery::default().recover(&log).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L152:C25

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
log.truncate("tx1").unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L154:C64

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let actions = TransactionRecovery::default().recover(&log).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L168:C64

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let actions = TransactionRecovery::default().recover(&log).unwrap();
```

### `packages\sz-orm-dtx\tests\sqlite_log_test.rs`

#### 🟡 [MEDIUM] `unwrap()` @ L26:C30

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dir = TempDir::new().unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L28:C58

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let log = SqliteTransactionLog::new(db_path.to_str().unwrap()).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L28:C68

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let log = SqliteTransactionLog::new(db_path.to_str().unwrap()).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L30:C50

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
log.append(&entry("tx1", "Active", "begin")).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L31:C54

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
log.append(&entry("tx1", "Prepared", "prepare")).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L32:C50

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
log.append(&entry("tx2", "Active", "begin")).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L34:C34

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let entries = log.read_all().unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L45:C30

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dir = TempDir::new().unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L47:C58

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let log = SqliteTransactionLog::new(db_path.to_str().unwrap()).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L47:C68

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let log = SqliteTransactionLog::new(db_path.to_str().unwrap()).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L49:C50

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
log.append(&entry("tx1", "Active", "begin")).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L50:C54

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
log.append(&entry("tx1", "Prepared", "prepare")).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L51:C50

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
log.append(&entry("tx2", "Active", "begin")).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L53:C25

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
log.truncate("tx1").unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L55:C34

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let entries = log.read_all().unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L63:C30

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dir = TempDir::new().unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L65:C58

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let log = SqliteTransactionLog::new(db_path.to_str().unwrap()).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L65:C68

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let log = SqliteTransactionLog::new(db_path.to_str().unwrap()).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L66:C50

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
log.append(&entry("tx1", "Active", "begin")).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L67:C16

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
log.sync().unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L73:C30

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dir = TempDir::new().unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L78:C62

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let log = SqliteTransactionLog::new(db_path.to_str().unwrap()).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L78:C72

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let log = SqliteTransactionLog::new(db_path.to_str().unwrap()).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L80:C60

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
log.append(&entry("tx-commit", "Active", "begin")).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L81:C64

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
log.append(&entry("tx-commit", "Prepared", "prepare")).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L82:C65

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
log.append(&entry("tx-commit", "Committing", "commit")).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L84:C56

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
log.append(&entry("tx-rb", "Active", "begin")).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L85:C60

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
log.append(&entry("tx-rb", "Prepared", "prepare")).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L86:C64

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
log.append(&entry("tx-rb", "RollingBack", "rollback")).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L88:C62

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
log.append(&entry("tx-done", "Committed", "commit")).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L93:C58

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let log = SqliteTransactionLog::new(db_path.to_str().unwrap()).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L93:C68

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let log = SqliteTransactionLog::new(db_path.to_str().unwrap()).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L94:C34

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let entries = log.read_all().unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L98:C42

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let actions = recovery.recover(&log).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L124:C30

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dir = TempDir::new().unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L126:C58

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let log = SqliteTransactionLog::new(db_path.to_str().unwrap()).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L126:C68

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let log = SqliteTransactionLog::new(db_path.to_str().unwrap()).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L127:C34

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let entries = log.read_all().unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L134:C30

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let dir = TempDir::new().unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L136:C58

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let log = SqliteTransactionLog::new(db_path.to_str().unwrap()).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L136:C68

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let log = SqliteTransactionLog::new(db_path.to_str().unwrap()).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L137:C50

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
log.append(&entry("tx1", "Active", "begin")).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L139:C33

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
log.truncate("nonexistent").unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L140:C34

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let entries = log.read_all().unwrap();
```

### `packages\sz-orm-es\tests\real_es_test.rs`

#### 🟡 [MEDIUM] `expect()` @ L56:C50

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let serialized = serde_json::to_string(&doc).expect("序列化 EsDocument 失败");
```

#### 🟡 [MEDIUM] `expect()` @ L57:C70

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let deserialized: EsDocument = serde_json::from_str(&serialized).expect("反序列化 EsDocument 失败");
```

#### 🟡 [MEDIUM] `expect()` @ L76:C54

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let serialized = serde_json::to_string(&request).expect("序列化 EsSearchRequest 失败");
```

#### 🟡 [MEDIUM] `expect()` @ L78:C43

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
serde_json::from_str(&serialized).expect("反序列化 EsSearchRequest 失败");
```

#### 🟡 [MEDIUM] `unwrap()` @ L93:C39

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let s = serde_json::to_string(&q).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L94:C47

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let d: EsQuery = serde_json::from_str(&s).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L99:C39

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let s = serde_json::to_string(&q).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L100:C47

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let d: EsQuery = serde_json::from_str(&s).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L109:C39

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let s = serde_json::to_string(&q).unwrap();
```

#### 🟡 [MEDIUM] `unwrap()` @ L111:C47

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result，或使用 match/if let

```rust
let _: EsQuery = serde_json::from_str(&s).unwrap();
```

### `packages\sz-orm-queue\tests\redis_provider_test.rs`

#### 🟡 [MEDIUM] `expect()` @ L67:C10

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("Redis 连接应成功");
```

#### 🟡 [MEDIUM] `expect()` @ L72:C10

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("publish 应成功");
```

#### 🟡 [MEDIUM] `expect()` @ L76:C10

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("consume 不应报错")
```

#### 🟡 [MEDIUM] `expect()` @ L77:C10

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("应有消息");
```

#### 🟡 [MEDIUM] `expect()` @ L81:C30

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
queue.ack(&msg.id).await.expect("ack 应成功");
```

#### 🟡 [MEDIUM] `expect()` @ L94:C6

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("Redis 连接应成功");
```

#### 🟡 [MEDIUM] `expect()` @ L99:C10

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("publish 应成功");
```

#### 🟡 [MEDIUM] `expect()` @ L103:C10

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("consume 不应报错")
```

#### 🟡 [MEDIUM] `expect()` @ L104:C10

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("应有消息");
```

#### 🟡 [MEDIUM] `expect()` @ L111:C30

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
queue.ack(&msg.id).await.expect("ack 应成功");
```

#### 🟡 [MEDIUM] `expect()` @ L124:C6

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("Redis 连接应成功");
```

#### 🟡 [MEDIUM] `expect()` @ L127:C34

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
queue.subscribe(topic).await.expect("subscribe 应成功");
```

#### 🟡 [MEDIUM] `expect()` @ L132:C10

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("publish 应成功");
```

#### 🟡 [MEDIUM] `expect()` @ L136:C10

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("consume 不应报错")
```

#### 🟡 [MEDIUM] `expect()` @ L137:C10

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("应有消息");
```

### `packages\sz-orm-queue\tests\rocketmq_provider_test.rs`

#### 🟡 [MEDIUM] `expect()` @ L27:C10

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("构造应成功");
```

#### 🟡 [MEDIUM] `expect()` @ L32:C36

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
q.subscribe("any-topic").await.expect("subscribe 应成功");
```

#### 🟡 [MEDIUM] `expect()` @ L40:C10

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("构造应成功");
```

#### 🟡 [MEDIUM] `expect()` @ L51:C10

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("构造应成功（不实际连接）");
```

#### 🟡 [MEDIUM] `expect()` @ L70:C6

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("构造应成功");
```

#### 🟡 [MEDIUM] `expect()` @ L74:C10

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("publish 应成功");
```

#### 🟡 [MEDIUM] `expect()` @ L78:C10

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("consume 不应报错")
```

#### 🟡 [MEDIUM] `expect()` @ L79:C10

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
.expect("应有消息");
```

#### 🟡 [MEDIUM] `expect()` @ L82:C30

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
queue.ack(&msg.id).await.expect("ack 应成功");
```

### `packages\sz-orm-storage\tests\signing_test.rs`

#### 🟡 [MEDIUM] `expect()` @ L202:C46

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let year: u32 = date_stamp[0..4].parse().expect("year should be parseable");
```

#### 🟡 [MEDIUM] `expect()` @ L209:C47

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let month: u32 = date_stamp[4..6].parse().expect("month should be parseable");
```

#### 🟡 [MEDIUM] `expect()` @ L216:C45

- **上下文**: 测试代码中的 Option/Result 为 None/Err 时 panic（带自定义消息）
- **建议**: 使用 ok_or()/ok_or_else() 转换为 Result

```rust
let day: u32 = date_stamp[6..8].parse().expect("day should be parseable");
```

### `packages\sz-orm-actix\src\lib.rs`

#### 🔵 [LOW] `comment_unwrap` @ L214:C4

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
/// 获取 `&mut Option<PooledConnection>`，再 `.as_mut().unwrap()` 得到
```

### `packages\sz-orm-ai\src\vector\hnsw.rs`

#### 🔵 [LOW] `comment_unwrap` @ L26:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
//! index.insert("v1", vec![1.0, 0.0, 0.0]).unwrap();
```

#### 🔵 [LOW] `comment_unwrap` @ L27:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
//! index.insert("v2", vec![0.0, 1.0, 0.0]).unwrap();
```

#### 🔵 [LOW] `comment_unwrap` @ L28:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
//! index.insert("v3", vec![1.0, 1.0, 0.0]).unwrap();
```

#### 🔵 [LOW] `comment_unwrap` @ L30:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
//! let results = index.search(&[1.0, 0.0, 0.0], 2, 50).unwrap();
```

#### 🔵 [LOW] `comment_panic` @ L1038:C8

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
// 不应 panic，且应产生不同的值
```

### `packages\sz-orm-ai\tests\ai_tests.rs`

#### 🔵 [LOW] `comment_panic` @ L197:C4

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
// dimension = 0 → 返回空向量，不 panic
```

#### 🔵 [LOW] `comment_panic` @ L519:C4

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
// 非法 JSON filter → 无匹配（不 panic）
```

#### 🔵 [LOW] `comment_panic` @ L540:C4

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
// 维度为 0 的 query 向量：cosine_similarity 返回 0（不 panic）
```

### `packages\sz-orm-auth\src\authorizer.rs`

#### 🔵 [LOW] `comment_panic` @ L64:C4

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
/// 不会 panic。
```

### `packages\sz-orm-back\src\backup.rs`

#### 🔵 [LOW] `comment_panic` @ L113:C8

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
// lock poisoned 时降级为 no-op，避免级联 panic 拖垮整个进程。
```

#### 🔵 [LOW] `comment_panic` @ L114:C8

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
// lock poisoned 意味着另一线程已 panic，该数据结构状态不可信。
```

#### 🔵 [LOW] `comment_panic` @ L129:C8

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
// lock poisoned 时返回空 Vec，避免级联 panic。
```

### `packages\sz-orm-batch\src\lib.rs`

#### 🔵 [LOW] `comment_unwrap` @ L77:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
/// // let sql = ops.batch_insert("users", &rows).unwrap();
```

### `packages\sz-orm-config\src\consul_source.rs`

#### 🔵 [LOW] `comment_panic` @ L208:C8

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
// 仅验证不 panic，具体值取决于 Consul 中的数据
```

### `packages\sz-orm-config\src\etcd_source.rs`

#### 🔵 [LOW] `comment_panic` @ L146:C8

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
// 仅验证不 panic，具体值取决于 etcd 中的数据
```

### `packages\sz-orm-core\src\behaviors.rs`

#### 🔵 [LOW] `comment_unwrap` @ L32:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
//! registry.before_insert(&ctx, &mut attrs).unwrap();
```

#### 🔵 [LOW] `comment_unwrap` @ L116:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
/// b.before_insert(&ctx, &mut attrs).unwrap();
```

#### 🔵 [LOW] `comment_unwrap` @ L195:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
/// b.before_insert(&ctx, &mut attrs).unwrap();
```

#### 🔵 [LOW] `comment_unwrap` @ L303:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
/// b.before_insert(&ctx, &mut attrs).unwrap();
```

#### 🔵 [LOW] `comment_unwrap` @ L451:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
/// registry.before_insert(&ctx, &mut attrs).unwrap();
```

#### 🔵 [LOW] `comment_unwrap` @ L552:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
/// registry.before_insert(&ctx, &mut attrs).unwrap();
```

### `packages\sz-orm-core\src\data_permission.rs`

#### 🔵 [LOW] `comment_unwrap` @ L196:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
/// let clause = rule.apply(&ctx).unwrap().unwrap();
```

#### 🔵 [LOW] `comment_unwrap` @ L248:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
/// let clause = rule.apply(&ctx).unwrap().unwrap();
```

#### 🔵 [LOW] `comment_unwrap` @ L300:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
/// let clause = rule.apply(&ctx).unwrap().unwrap();
```

#### 🔵 [LOW] `comment_unwrap` @ L391:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
/// let clause = rule.apply(&ctx).unwrap().unwrap();
```

#### 🔵 [LOW] `comment_unwrap` @ L449:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
/// let sql = interceptor.apply_to_select("SELECT * FROM orders", &ctx).unwrap();
```

#### 🔵 [LOW] `comment_panic` @ L1072:C8

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
// 不平衡括号不应导致 panic（depth > 0 时继续，但不影响安全性）
```

### `packages\sz-orm-core\src\dialect.rs`

#### 🔵 [LOW] `comment_unwrap` @ L1880:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
/// let dialect = get_dialect(DbType::MySQL).unwrap();
```

### `packages\sz-orm-core\src\dirty_attributes.rs`

#### 🔵 [LOW] `comment_unwrap` @ L225:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
/// let dialect = get_dialect(DbType::MySQL).unwrap();
```

#### 🔵 [LOW] `comment_unwrap` @ L233:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
/// let sql = build_dynamic_update(&*dialect, "users", "id", &Value::I64(1), &tracker).unwrap();
```

#### 🔵 [LOW] `comment_unwrap` @ L307:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
/// let dialect = get_dialect(DbType::MySQL).unwrap();
```

#### 🔵 [LOW] `comment_unwrap` @ L313:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
/// let sql = build_dynamic_insert(&*dialect, "users", &data).unwrap();
```

### `packages\sz-orm-core\src\dynamic_filter.rs`

#### 🔵 [LOW] `comment_unwrap` @ L255:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
/// registry.enable("active_only", params).unwrap();
```

#### 🔵 [LOW] `comment_unwrap` @ L493:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
/// assert_eq!(rendered.unwrap(), "status = 'active' AND age >= 18");
```

#### 🔵 [LOW] `comment_unwrap` @ L518:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
/// assert!(rendered.unwrap().contains("hello\\\\nworld"));
```

### `packages\sz-orm-core\src\dynamic_sql.rs`

#### 🔵 [LOW] `comment_unwrap` @ L34:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
//! let sql = parser.build("find_users", &params).unwrap();
```

### `packages\sz-orm-core\src\error.rs`

#### 🔵 [LOW] `comment_unwrap` @ L17:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
/// （parking_lot 的 RwLock 不会返回 PoisonError，无需 unwrap()）
```

#### 🔵 [LOW] `comment_panic` @ L845:C8

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
// 不设置 hook 时 trigger 应静默跳过（不 panic）
```

### `packages\sz-orm-core\src\find_with_related.rs`

#### 🔵 [LOW] `comment_unwrap` @ L25:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
//! let dialect = get_dialect(DbType::MySQL).unwrap();
```

#### 🔵 [LOW] `comment_unwrap` @ L384:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
/// let dialect = get_dialect(DbType::MySQL).unwrap();
```

#### 🔵 [LOW] `comment_unwrap` @ L391:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
/// println!("{}", loader.related_sql("orders").unwrap());  // 关联表 SQL
```

### `packages\sz-orm-core\src\hooks.rs`

#### 🔵 [LOW] `comment_panic` @ L561:C8

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
// lock poisoned 时降级为 no-op，避免级联 panic。
```

### `packages\sz-orm-core\src\hydration_plugin.rs`

#### 🔵 [LOW] `comment_unwrap` @ L40:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
//! let result = hydrate(&[row], HydrationMode::Scalar).unwrap();
```

### `packages\sz-orm-core\src\lib.rs`

#### 🔵 [LOW] `comment_unwrap` @ L85:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
//! let dialect = get_dialect(DbType::MySQL).unwrap();
```

#### 🔵 [LOW] `comment_unwrap` @ L96:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
//! QueryBuilder::<User>::new(get_dialect(DbType::MySQL).unwrap())
```

#### 🔵 [LOW] `comment_unwrap` @ L110:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
//! let update_sql = QueryBuilder::<User>::new(get_dialect(DbType::MySQL).unwrap())
```

#### 🔵 [LOW] `comment_unwrap` @ L115:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
//! let delete_sql = QueryBuilder::<User>::new(get_dialect(DbType::MySQL).unwrap())
```

### `packages\sz-orm-core\src\observer.rs`

#### 🔵 [LOW] `comment_unwrap` @ L49:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
//!         let mut logs = self.logs.lock().unwrap();
```

#### 🔵 [LOW] `comment_unwrap` @ L80:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
//! assert_eq!(logs.lock().unwrap().len(), 1);
```

### `packages\sz-orm-core\src\optimistic_lock.rs`

#### 🔵 [LOW] `comment_unwrap` @ L38:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
//! let dialect = get_dialect(DbType::MySQL).unwrap();
```

#### 🔵 [LOW] `comment_unwrap` @ L179:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
/// let dialect = get_dialect(DbType::MySQL).unwrap();
```

### `packages\sz-orm-core\src\query.rs`

#### 🔵 [LOW] `comment_unwrap` @ L608:C4

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
/// let dialect = get_dialect(DbType::MySQL).unwrap();
```

#### 🔵 [LOW] `comment_unwrap` @ L680:C4

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
/// let dialect = get_dialect(DbType::MySQL).unwrap();
```

### `packages\sz-orm-core\src\queryable.rs`

#### 🔵 [LOW] `comment_unwrap` @ L48:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
//! let row = UserRow::from_values(vec![Value::I64(42), Value::String("Alice".into())]).unwrap();
```

### `packages\sz-orm-core\src\quick_query.rs`

#### 🔵 [LOW] `comment_unwrap` @ L17:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
//! let dialect = get_dialect(DbType::MySQL).unwrap();
```

### `packages\sz-orm-core\src\repository.rs`

#### 🔵 [LOW] `comment_unwrap` @ L508:C4

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
/// let items = repo.find_by_with_or_filter(&and, &or).unwrap();
```

### `packages\sz-orm-core\src\result_map.rs`

#### 🔵 [LOW] `comment_unwrap` @ L58:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
//! let result = apply_result_map(&registry, "userMap", &row).unwrap();
```

### `packages\sz-orm-core\src\shadow.rs`

#### 🔵 [LOW] `comment_panic` @ L138:C4

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
/// 记录并触发 panic（用于测试环境）
```

#### 🔵 [LOW] `comment_panic` @ L216:C16

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
// H-5 修复：传播 handle_mismatch 的 Result（Panic 模式下返回 Internal 错误）
```

#### 🔵 [LOW] `comment_panic` @ L241:C16

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
// H-5 修复：传播 handle_mismatch 的 Result（Panic 模式下返回 Internal 错误）
```

#### 🔵 [LOW] `comment_panic` @ L256:C16

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
// H-5 修复：传播 handle_mismatch 的 Result（Panic 模式下返回 Internal 错误）
```

#### 🔵 [LOW] `comment_panic` @ L274:C12

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
// H-5 修复：传播 handle_mismatch 的 Result（Panic 模式下返回 Internal 错误）
```

#### 🔵 [LOW] `comment_panic` @ L391:C4

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
/// 返回 `Err` 仅在 `MismatchAction::Panic` 模式下（H-5 修复：将 panic! 改为
```

### `packages\sz-orm-core\src\type_handler.rs`

#### 🔵 [LOW] `comment_unwrap` @ L36:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
//! let parsed: String = registry.handle("created_at", &stored).unwrap();
```

#### 🔵 [LOW] `comment_unwrap` @ L40:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
//! let value = registry.to_value("created_at", &String::from("2026-07-19T10:00:00Z")).unwrap();
```

### `packages\sz-orm-core\tests\chaos_pool.rs`

#### 🔵 [LOW] `comment_panic` @ L183:C0

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
/// 验证：第 3 个连接请求应返回明确的 PoolError::Timeout，绝不 Panic。
```

#### 🔵 [LOW] `comment_panic` @ L197:C4

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
// 尝试获取第 3 个连接 — 应返回 Timeout，绝不 panic
```

#### 🔵 [LOW] `comment_panic` @ L287:C0

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
/// 验证：事务 Drop 的自动回滚机制不会导致 Panic，
```

#### 🔵 [LOW] `comment_panic` @ L390:C0

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
/// 多任务并发 acquire/release，验证在高压下不 Panic、不泄漏、
```

#### 🔵 [LOW] `comment_panic` @ L450:C0

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
/// 混沌场景 4b：高并发+短超时 — 超时不 panic
```

### `packages\sz-orm-core\tests\contracts\pool_contract.rs`

#### 🔵 [LOW] `comment_panic` @ L181:C0

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
// ===== §5.1 release 到已关闭的池不 panic 契约 =====
```

#### 🔵 [LOW] `comment_panic` @ L188:C4

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
// release 到已关闭池：应直接关闭连接，不 panic
```

### `packages\sz-orm-core\tests\contracts\transaction_contract.rs`

#### 🔵 [LOW] `comment_panic` @ L308:C0

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
// ===== §6.1 options() / state() 不 panic 契约 =====
```

#### 🔵 [LOW] `comment_panic` @ L313:C4

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
// Active 状态下 options()/state() 应可读取且不 panic
```

#### 🔵 [LOW] `comment_panic` @ L318:C4

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
// Committed 状态下 options()/state() 仍应可读取且不 panic
```

### `packages\sz-orm-core\tests\e2e_transaction.rs`

#### 🔵 [LOW] `comment_unwrap` @ L771:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
/// 注意：调用方需先 `conn.begin_transaction().await.unwrap()` 启动事务，
```

### `packages\sz-orm-core\tests\fuzz.rs`

#### 🔵 [LOW] `comment_panic` @ L384:C0

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
/// Fuzz Value 类型转换：验证不会 panic，且类型谓词与转换结果自洽
```

#### 🔵 [LOW] `comment_panic` @ L424:C8

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
// Display 和 Debug 不应 panic
```

### `packages\sz-orm-core\tests\jepsen.rs`

#### 🔵 [LOW] `comment_panic` @ L508:C4

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
// 无法直接验证状态（tx 已被 drop），但可以验证没有 panic
```

#### 🔵 [LOW] `comment_panic` @ L509:C4

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
// 这是一个行为验证：drop 不会 panic
```

### `packages\sz-orm-crypto\src\lib.rs`

#### 🔵 [LOW] `comment_panic` @ L47:C4

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
// 用 match 处理避免 panic，虽然 Err 分支不可达（RustCrypto 不变量保证）。
```

### `packages\sz-orm-dtx\src\cross_shard.rs`

#### 🔵 [LOW] `comment_unwrap` @ L38:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
//! coord.execute().unwrap();
```

### `packages\sz-orm-dtx\src\saga.rs`

#### 🔵 [LOW] `comment_unwrap` @ L33:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
//!     .with_compensation(move || { c1r.fetch_sub(1, Ordering::SeqCst); Ok(()) })).unwrap();
```

#### 🔵 [LOW] `comment_unwrap` @ L36:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
//!     .with_compensation(move || { c2r.fetch_sub(1, Ordering::SeqCst); Ok(()) })).unwrap();
```

#### 🔵 [LOW] `comment_unwrap` @ L38:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
//! saga.execute().unwrap();
```

#### 🔵 [LOW] `comment_unwrap` @ L838:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
/// 所有锁获取使用 `unwrap()`，若锁中毒（持有锁的线程 panic）将 panic。
```

#### 🔵 [LOW] `comment_panic` @ L838:C0

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
/// 所有锁获取使用 `unwrap()`，若锁中毒（持有锁的线程 panic）将 panic。
```

### `packages\sz-orm-dtx\src\tcc.rs`

#### 🔵 [LOW] `comment_unwrap` @ L50:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
//! coord.execute().unwrap();
```

#### 🔵 [LOW] `comment_panic` @ L913:C4

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
/// 内部锁中毒（线程 panic 导致状态损坏）
```

### `packages\sz-orm-es\src\lib.rs`

#### 🔵 [LOW] `comment_unwrap` @ L20:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
//! manager.sync_to_es(vec![EsDocument::new("idx", serde_json::json!({"k": "v"}))]).unwrap();
```

#### 🔵 [LOW] `comment_unwrap` @ L28:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
//! let client = RealEsClient::new("http://localhost:9200").unwrap();
```

### `packages\sz-orm-es\src\real_es.rs`

#### 🔵 [LOW] `comment_panic` @ L11:C0

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
//! 同步方法（会导致 panic）。如需在异步环境中使用，请将 `RealEsClient`
```

#### 🔵 [LOW] `comment_unwrap` @ L44:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
/// let client = RealEsClient::new("http://localhost:9200").unwrap();
```

### `packages\sz-orm-health\src\lib.rs`

#### 🔵 [LOW] `comment_panic` @ L215:C8

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
// lock poisoned 时降级为 no-op，避免级联 panic。
```

#### 🔵 [LOW] `comment_panic` @ L258:C8

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
// lock poisoned 时返回 false（未找到），避免级联 panic。
```

#### 🔵 [LOW] `comment_panic` @ L269:C8

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
// lock poisoned 时返回 Unknown，避免级联 panic。
```

#### 🔵 [LOW] `comment_panic` @ L537:C8

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
// lock poisoned 时返回空 Vec，避免级联 panic。
```

#### 🔵 [LOW] `comment_panic` @ L545:C8

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
// lock poisoned 时返回错误而非 panic。
```

#### 🔵 [LOW] `comment_panic` @ L581:C8

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
// lock poisoned 时返回空 Vec，避免级联 panic。
```

#### 🔵 [LOW] `comment_panic` @ L589:C8

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
// lock poisoned 时返回错误而非 panic。
```

### `packages\sz-orm-limit\src\lib.rs`

#### 🔵 [LOW] `comment_panic` @ L233:C12

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
// 避免除零产生 inf，inf as i64 触发 panic
```

#### 🔵 [LOW] `comment_panic` @ L927:C4

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
// ===== TDD RED：refill_rate=0 panic 修复测试 =====
```

#### 🔵 [LOW] `comment_panic` @ L932:C8

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
// capacity=1，第一次 acquire 应允许，第二次应拒绝且不 panic
```

#### 🔵 [LOW] `comment_panic` @ L938:C8

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
// 第二次 acquire：令牌耗尽且不补充，应拒绝而非 panic
```

#### 🔵 [LOW] `comment_panic` @ L951:C8

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
// refill_rate=-1.0 是错误配置，应被当作 0.0 处理而非 panic
```

### `packages\sz-orm-limit\tests\stress.rs`

#### 🔵 [LOW] `comment_panic` @ L91:C0

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
/// 注意：refill_rate=0.0 会导致源码 panic（1000.0/0.0=inf, inf as i64 未定义），
```

### `packages\sz-orm-logger\src\advanced.rs`

#### 🔵 [LOW] `comment_panic` @ L186:C0

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
/// 实现方需保证 `write` 不 panic（lock poisoned 时降级处理），
```

#### 🔵 [LOW] `comment_panic` @ L232:C8

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
// lock poisoned 时跳过写入而非 panic
```

#### 🔵 [LOW] `comment_panic` @ L321:C0

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
/// 每条日志会依次写入所有已注册的 sink。单个 sink 写入失败（panic）
```

### `packages\sz-orm-macros\src\derive.rs`

#### 🔵 [LOW] `comment_unwrap` @ L43:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
//!     .unwrap();
```

#### 🔵 [LOW] `comment_panic` @ L1394:C8

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
// 不应 panic
```

### `packages\sz-orm-macros\src\lib.rs`

#### 🔵 [LOW] `comment_unwrap` @ L1577:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
///     .unwrap();
```

### `packages\sz-orm-masking\src\lib.rs`

#### 🔵 [LOW] `comment_panic` @ L4:C0

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
//! 前缀/后缀保留规则。实现 Unicode 安全，对短输入有合理兜底，不会 panic。
```

#### 🔵 [LOW] `comment_panic` @ L407:C8

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
// Mixing CJK + emoji + ascii - just verify no panic and contains stars.
```

### `packages\sz-orm-mig\src\migrator.rs`

#### 🔵 [LOW] `comment_panic` @ L277:C4

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
/// If the internal RwLock is poisoned (due to a panic in another thread),
```

### `packages\sz-orm-oracle\src\lib.rs`

#### 🔵 [LOW] `comment_panic` @ L98:C4

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
/// 如果 tokio runtime 创建失败（通常是系统资源不足），会 panic。
```

### `packages\sz-orm-query-builder\src\lib.rs`

#### 🔵 [LOW] `comment_panic` @ L630:C4

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
/// 检测到 SQL 注入尝试时返回 `Err(QueryError::SqlInjection)`，不再 panic。
```

#### 🔵 [LOW] `comment_panic` @ L645:C4

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
/// 检测到 SQL 注入尝试时返回 `Err(QueryError::SqlInjection)`，不再 panic。
```

#### 🔵 [LOW] `comment_panic` @ L3088:C8

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
// 这些是合法的 WHERE 条件，不应触发 panic——同时验证生成 SQL 包含预期的 WHERE 子句
```

#### 🔵 [LOW] `comment_panic` @ L3130:C8

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
// 仅含 `/*` 无 `*/`，`||` 应 panic，`&&` 不会 panic
```

### `packages\sz-orm-queue\src\queue.rs`

#### 🔵 [LOW] `comment_panic` @ L120:C12

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
// 返回 0 作为兜底，避免 panic；生产环境应监控此事件
```

#### 🔵 [LOW] `comment_panic` @ L688:C20

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
// Block 策略应由 publish_with_block 处理；若误入此路径则返回错误而非 panic
```

### `packages\sz-orm-queue\tests\rocketmq_provider_test.rs`

#### 🔵 [LOW] `comment_panic` @ L46:C0

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
/// 未连接（无服务端）时 publish 应返回错误（而非 panic）
```

### `packages\sz-orm-queue\tests\stress.rs`

#### 🔵 [LOW] `comment_panic` @ L211:C0

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
/// 验证：消费空 topic 不会 panic
```

### `packages\sz-orm-rw\src\lib.rs`

#### 🔵 [LOW] `comment_panic` @ L799:C8

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
// 验证 AtomicUsize 在多线程下不会 panic，且计数正确
```

### `packages\sz-orm-scheduler\tests\stress.rs`

#### 🔵 [LOW] `comment_panic` @ L73:C0

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
/// 验证：next_run_time 在大量调用下对合法表达式不 panic，对非法表达式返回错误
```

#### 🔵 [LOW] `comment_panic` @ L116:C0

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
/// 验证：next_run_time 在多次调用下不 panic
```

#### 🔵 [LOW] `comment_panic` @ L227:C0

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
/// 验证：start/stop 后台线程不 panic
```

### `packages\sz-orm-search\src\meilisearch_provider.rs`

#### 🔵 [LOW] `comment_panic` @ L237:C8

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
// 这里测试构造不抛 panic
```

### `packages\sz-orm-sharding\src\enhanced.rs`

#### 🔵 [LOW] `comment_unwrap` @ L20:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
//! let shard = router.route("user:12345").unwrap();
```

#### 🔵 [LOW] `comment_unwrap` @ L28:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
//! assert_eq!(list.route("cn").unwrap(), "shard_cn");
```

### `packages\sz-orm-sharding\src\lib.rs`

#### 🔵 [LOW] `comment_panic` @ L108:C4

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
/// 工作线程 panic（用于 `ScatterGather` 并行场景）
```

### `packages\sz-orm-sharding\src\scatter.rs`

#### 🔵 [LOW] `comment_panic` @ L37:C4

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
/// 顺序与 `query_all()` 一致。若某线程 panic，对应位置为 `Err(ThreadPanic)`。
```

#### 🔵 [LOW] `comment_unwrap` @ L111:C4

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
/// let sum = ScatterGather::merge(results, |vs| Ok(vs.iter().sum())).unwrap();
```

### `packages\sz-orm-tracing\src\lib.rs`

#### 🔵 [LOW] `comment_panic` @ L1640:C8

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
// Debug formatting must contain expected fields, not just "not panic".
```

#### 🔵 [LOW] `comment_unwrap` @ L2070:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
/// # let rt = tokio::runtime::Runtime::new().unwrap();
```

#### 🔵 [LOW] `comment_unwrap` @ L2074:C0

- **上下文**: 注释中的 unwrap 关键词（可能是待办或说明）
- **建议**: 确认是否为待处理问题

```rust
/// let _guard = init_otlp_exporter(OtlpConfig::default()).await.unwrap();
```

### `packages\sz-orm-tracing\src\sampling.rs`

#### 🔵 [LOW] `comment_panic` @ L125:C4

- **上下文**: 注释中的 panic 关键词
- **建议**: 确认是否为待处理问题

```rust
/// 当 ratio 不在 [0.0, 1.0] 范围内时 panic。
```

## 修复优先级建议

1. **立即修复（CRITICAL）**：`panic!` / `unreachable!` / `unimplemented!` / `todo!`
   - 这些调用在生产环境中会导致进程终止
   - 替换为 `Result` 返回或适当的错误处理

2. **高优先级（HIGH）**：生产代码中的 `unwrap()` / `expect()`
   - 使用 `ok_or()` / `ok_or_else()` 转换为 `Result`
   - 使用 `match` 或 `if let` 显式处理 `None` / `Err` 分支

3. **中优先级（MEDIUM）**：测试代码中的 `unwrap()` / `expect()`
   - 测试代码允许使用，但建议添加断言消息

4. **低优先级（LOW）**：注释中的关键词
   - 确认是否为待处理问题，更新注释或创建 issue

---

## 2026-08-01 修复进展（Task 1.5 批量修复）

### 修复统计

| 类别 | 修复前 | 修复后 | 说明 |
|------|--------|--------|------|
| 生产代码 unwrap/expect/panic | ~30 | 12 | 剩余均为防御性不变量（见下文） |
| 测试代码 unwrap/expect | ~4700 | ~4700 | 允许保留 |
| 基准测试 unwrap | ~15 | ~15 | 允许保留 |

### 已修复文件

| 包 | 文件 | 修复内容 |
|----|------|----------|
| sz-orm-core | `queryable.rs` | 5 unwrap → 索引访问 |
| sz-orm-core | `find_with_related.rs` | 8 expect → Result 传播（API 变更） |
| sz-orm-core | `hydration_plugin.rs` | 4 expect → Option 处理 |
| sz-orm-core | `phinx_migration.rs` | 7 expect → `create()` 返回 Result |
| sz-orm-auth | `jwt.rs` | HMAC `new_from_slice` → Result 传播 |
| sz-orm-auth | `oauth2.rs` | `get_mut().unwrap()` → `if let Some` |
| sz-orm-audit | `lib.rs` | `mask_sql` chars().next().expect → if let Some |
| sz-orm-dtx | `file_log.rs` | guard.expect → ok_or_else |
| sz-orm-dtx | `recovery.rs` | hashmap.get().expect → ok_or_else |
| sz-orm-dtx | `saga.rs` | entries.last().unwrap() → 索引访问 |
| sz-orm-es | `extensions.rs` / `real_es.rs` | unwrap → expect（带注释） |
| sz-orm-grpc | `lib.rs` | 2 expect → 直接使用 err 变量 |
| sz-orm-oracle | `lib.rs` | `OracleBlockingPool::new()` → Result |
| sz-orm-postgis | `real_postgis.rs` | chars().next().unwrap() → ok_or_else |
| sz-orm-query-builder | `lib.rs` | `check_where_injection` 未使用 Result 警告修复 |
| sz-orm-scheduler | `lib.rs` | `register_handler`/`list_tasks` → Result |
| sz-orm-sharding | `enhanced.rs` | `.unwrap()` → `ok_or_else(NoNodes)` |
| sz-orm-timeseries | `safety.rs` / `extensions.rs` | expect → ok_or_else |
| sz-orm-tracing | `lib.rs` | get_spans/clear → match / if let Ok |
| sz-orm-vector | `real_pg.rs` | chars().next().unwrap() → ok_or_else |
| sz-orm-websocket | `compression.rs` / `subprotocol.rs` | 保留（防御性不变量） |
| examples | `migration.rs` / `production_app.rs` | 适配 API 变更 |

### 剩余 12 处生产代码 expect（均为防御性不变量）

这些调用均有前置守卫条件保证不会失败，`expect()` 消息明确记录了不变量：

1. **sz-orm-es** (4): `terms.iter().next()` after `len() == 1` 守卫
2. **sz-orm-mssql** (2): `Deref`/`DerefMut` trait 方法（无法返回 Result）
3. **sz-orm-oracle** (2): `Deref`/`DerefMut` trait 方法（无法返回 Result）
4. **sz-orm-queue** (1): `VecDeque::remove(idx)` where `idx` 由遍历保证存在
5. **sz-orm-timeseries** (1): Unix epoch 0 始终有效
6. **sz-orm-websocket** (2): zlib 内存压缩不会失败 / candidates 经 `is_empty()` 守卫

### 验证

```bash
cargo check --workspace --exclude sz-orm-es
# Finished `dev` profile [unoptimized + debuginfo] target(s)
```

### 最终验证（2026-08-01 Task 1.10）

- `cargo check --workspace --exclude sz-orm-es` ✅ 全工作空间编译通过
- `cargo test -p sz-orm-core --lib find_with_related` ✅ 31 测试全通过（含 5 个 SQL 注入拦截测试）
- `cargo clippy -p sz-orm-core` ✅ 无新增警告（find_with_related.rs 零冗余闭包）
- `find_with_related.rs` 生产代码：0 处 unwrap/expect（全部 8 处已转为 Result 传播）
- 剩余生产代码 expect（12 处）：均为防御性不变量或 trait 方法限制，详见上文
- 4 个 phinx_migration SQL 注入测试为**预存失败**（clean tree 同样失败，与本次修复无关）
