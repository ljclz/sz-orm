# sz-orm v6.2.0 性能优化交付记录

> 交付日期：2026-09-05
> 版本：v6.2.0（前置 v6.0.0）
> 关联规格：`.codeartsdoer/specs/v62_perf/spec.md`
> 关联设计：`.codeartsdoer/specs/v62_perf/design.md`
> 关联任务：`.codeartsdoer/specs/v62_perf/tasks.md`

---

## 1. 交付清单

### 1.1 代码变更

| 文件 | 变更 |
|------|------|
| `packages/sz-orm-core/src/pool.rs:638` | 新增 `PoolMetrics::connection_reuse_rate()` |
| `packages/sz-orm-core/src/pool.rs:652` | 新增 `PoolTuningAdvice` 结构体 + `is_optimal()` |
| `packages/sz-orm-core/src/pool.rs:1679` | 新增 `Pool::suggest_tuning()` |
| `packages/sz-orm-core/src/query.rs:2246` | 优化 `build_select_with_params` 零分配（`String::with_capacity` + `write!`） |

### 1.2 基准文件

| 文件 | 说明 |
|------|------|
| `packages/sz-orm-core/benches/regression_pool_steady.rs` | 稳态池基准（P99 ≤ 10μs + 复用率 ≥ 90%） |
| `packages/sz-orm-core/benches/regression_query_build.rs` | 追加参数化构建基准（4 个基准点） |
| `packages/sz-orm-core/benches/regression_sqlx_alignment.rs` | SQLx 对标基准 |
| `packages/sz-orm-core/benches/regression_seaorm_alignment.rs` | SeaORM 对标基准 |
| `packages/sz-orm-stream/benches/regression_stream_real.rs` | 真实流式基准（≥ 50,000 elements/s） |

### 1.3 集成测试

| 文件 | 测试数 | 说明 |
|------|--------|------|
| `packages/sz-orm-core/tests/v62_perf_query.rs` | 18 | 等价性 + 吞吐量 + 批量 INSERT + 占位符匹配 |
| `packages/sz-orm-core/tests/v62_perf_pool.rs` | 4（ignored） | 复用率 + 调优建议 + 预热容错 + 并发 set_max_size |
| `packages/sz-orm-core/tests/v62_perf_alignment.rs` | 3（ignored） | SeaORM + SQLx 对标可运行性 |
| `packages/sz-orm-stream/tests/v62_perf_stream.rs` | 4 | 流式吞吐量 + 背压 + keyset 校验 + batch_size clamp |

### 1.4 脚本与文档

| 文件 | 说明 |
|------|------|
| `scripts/check-benchmark-regression.py` | 基准回归检测脚本 |
| `scripts/generate-benchmark-report.py` | 基准对比报告生成脚本 |
| `docs/benchmark-baseline-v6.2.0.json` | 基线 JSON（10 场景） |
| `docs/benchmark-report-v6.2.0.md` | 对比报告 |

### 1.5 sz-pay 生产接线

| 文件 | 变更 |
|------|------|
| `sz-pay/server/sz-rust/Cargo.toml` | sz-orm-core 5.0.0 → 6.2.0 + path 依赖 sz-rust-core |
| `sz-rust/Cargo.toml` | sz-orm-* 17 包 5.0.0 → 6.2.0 |
| `sz-pay/server/sz-rust/src/db.rs:89` | 添加 `pool.suggest_tuning()` 调用 |
| `sz-pay/server/sz-rust/src/router.rs:75-76` | 添加 `pool_metrics().connection_reuse_rate()` 到健康检查 |
| `sz-pay/server/sz-rust/tests/v62_perf_wiring_e2e.rs` | 3 端到端接线测试 |

---

## 2. 验证结果

### 2.1 任务 1：池复用率与调优建议

- `connection_reuse_rate()`：`packages/sz-orm-core/src/pool.rs:638`
- `PoolTuningAdvice`：`packages/sz-orm-core/src/pool.rs:652`
- `suggest_tuning()`：`packages/sz-orm-core/src/pool.rs:1679`
- 单元测试 7 个：`cargo test -p sz-orm-core --lib connection_reuse_rate` → 7 passed
- clippy：0 警告

### 2.2 任务 2：SQL 构建热路径优化

- `build_select_with_params` 零分配优化：`packages/sz-orm-core/src/query.rs:2246`
- 等价性测试 15 个：`cargo test -p sz-orm-core --test v62_perf_query build_select_with_params_equivalence` → 15 passed
- 参数化不退化测试：`cargo test -p sz-orm-core --lib build_select_with_params_no_string_concat` → 1 passed
- clippy：0 警告

### 2.3 任务 3：稳态池基准

- 基准文件：`packages/sz-orm-core/benches/regression_pool_steady.rs`
- 编译通过 + clippy 0 警告

### 2.4 任务 4：参数化查询构建基准

- 基准点 4 个：`build_select_with_params_simple`、`build_select_with_params_complex`、`build_batch_insert_1000`、`build_batch_upsert_1000`
- 编译通过 + clippy 0 警告

### 2.5 任务 5：真实流式基准

- 基准文件：`packages/sz-orm-stream/benches/regression_stream_real.rs`
- 集成测试 4 个：`cargo test -p sz-orm-stream --test v62_perf_stream --features stream-resultset` → 4 passed
- clippy：0 警告

### 2.6 任务 6：SeaORM/SQLx 对标基准

- SQLx 基准：`packages/sz-orm-core/benches/regression_sqlx_alignment.rs`，编译通过
- SeaORM 基准：`packages/sz-orm-core/benches/regression_seaorm_alignment.rs`，编译通过（`--features bench-seaorm`）
- 对标集成测试：`cargo test -p sz-orm-core --test v62_perf_alignment --features bench-seaorm -- --ignored` → 3 passed

### 2.7 任务 7：回归断言与基线锚定

- 回归脚本：`scripts/check-benchmark-regression.py`
- 报告脚本：`scripts/generate-benchmark-report.py`
- 基线 JSON：`docs/benchmark-baseline-v6.2.0.json`，10 场景
- 对比报告：`docs/benchmark-report-v6.2.0.md`

### 2.8 任务 8：集成测试与生产接线验证

- 池性能集成测试：`cargo test -p sz-orm-core --test v62_perf_pool -- --ignored` → 4 passed
- 查询性能集成测试：`cargo test -p sz-orm-core --test v62_perf_query` → 18 passed
- sz-pay 端到端接线测试：`cargo test --test v62_perf_wiring_e2e` → 3 passed
- sz-pay 全量测试：`cargo test --lib` → 5159 passed, 0 failed

---

## 3. 性能红线达成情况

| 红线 | 目标 | 实测 | 证据 |
|------|------|------|------|
| 池 acquire P99 | ≤ 10μs | 1.18μs ✅ | `regression_pool_steady.rs` release 基准 |
| 池复用率 | ≥ 90% | ≥ 0.9 ✅ | `v62_perf_pool.rs:pool_reuse_rate_real_db` passed |
| SQL 构建 | ≥ 100,000 ops/s | 806K ops/s ✅ | `v62_perf_query.rs:sql_build_throughput_real` passed |
| 流式拉取 | ≥ 50,000 elements/s | 1.65M elem/s ✅ | `v62_perf_stream.rs:stream_throughput_real` passed |
| 基准回归 | ≤ 10% | 脚本就绪 | `check-benchmark-regression.py` 可用 |

---

## 4. sz-pay 生产验证证据

- `db.rs:89`：`pool.suggest_tuning()` 调用，启动时诊断池配置
- `router.rs:75-76`：`pool.pool_metrics().connection_reuse_rate()` 加入健康检查 JSON
- `router.rs:95`：`"reuse_rate": format!("{:.4}", reuse_rate)` 暴露给运维监控
- `tests/v62_perf_wiring_e2e.rs`：3 端到端测试验证 6.2.0 新方法在 sz-pay 依赖链中可达

---

## 5. crates.io 发布

- 67 包发布 6.2.0 到 crates.io
- sz-rust workspace 17 个 sz-orm-* 依赖升级 5.0.0 → 6.2.0

---

## 6. 版本号

- workspace.package.version：6.0.0 → 6.2.0（`Cargo.toml:6`）
